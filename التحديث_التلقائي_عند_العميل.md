# هل يستطيع العميل سحب التحديثات من GitHub؟ — الجواب الصريح

**التاريخ:** ٤ أغسطس ٢٠٢٦
**الجواب المختصر:** ❌ **لا. ليس الآن.** الكود جاهز، لكن **البنية غير مُفعّلة** — وبدون ٣ خطوات أدناه لن يصل أي تحديث لأي عميل.

---

## ١. ما هو **جاهز فعلاً** ✅

| العنصر | الحالة | الدليل |
|---|---|---|
| كود التحديث `src/main/updater.ts` | ✅ مكتوب بالكامل | ١٥٤ سطر |
| مُستدعى عند الإقلاع | ✅ `src/main/index.ts:258` | `startUpdater()` |
| مُعطّل في وضع التطوير | ✅ | `if (!app.isPackaged) return;` |
| لا يُزعج العميل عند انقطاع النت | ✅ | كل الأخطاء صامتة |
| لا يُغلق البرنامج فجأة | ✅ | يسأل: «إعادة التشغيل الآن / لاحقاً» |
| يبني مثبّت Windows | ✅ | `MakerSquirrel` في `forge.config.ts` |
| نسخة احتياطية قبل ترقية القاعدة | ✅ | `schemaVersion.ts` |
| يرفض قاعدة بيانات أحدث من البرنامج | ✅ | `SchemaTooNewError` |

**الخلاصة:** الجزء البرمجي سليم ومُختبر.

---

## ٢. ما هو **ناقص** — ولماذا لن يصل التحديث 🔴

الكود يستخدم خدمة `update.electronjs.org` المجانية. شروطها الرسمية ثلاثة:

> - Your app runs on macOS or Windows ✅
> - **Your app has a public GitHub repository** ❌
> - **Your builds are published to GitHub Releases** ❌

### فحصت المستودع الآن، والنتيجة:

```
isPrivate : true      ← 🔴 المستودع خاص
releases  : []        ← 🔴 لا يوجد أي إصدار منشور
tags      : (لا شيء)  ← 🔴 لا توجد وسوم إصدارات
```

| # | المشكلة | الأثر |
|---|---|---|
| 🔴 ١ | **المستودع خاص (private)** | الخدمة لا تستطيع قراءته إطلاقاً — كل طلب تحديث يفشل بصمت |
| 🔴 ٢ | **لا توجد إصدارات (Releases)** | لا يوجد ملف `.exe` لينزّله العميل |
| 🔴 ٣ | **لا يوجد Workflow للبناء** | لا شيء يبني المثبّت تلقائياً |

**النتيجة العملية:** لو ثبّت العميل البرنامج اليوم، سيبقى على نسخته للأبد. الكود سيحاول، وسيفشل بصمت (وهذا مقصود حتى لا يزعجه)، ولن تعرف أنت ولا هو.

---

## ٣. الحل — اختر مساراً واحداً

### 🅰 المسار السهل: اجعل المستودع عاماً (public)

**مناسب إن:** لا تمانع رؤية الناس للكود.

**⚠️ تحذير أمني قاطع — اقرأه قبل أي شيء:**

فحصت تاريخ Git، وهذه الأسرار **ما زالت موجودة فيه** ويمكن لأي شخص استخراجها لحظة جعله عاماً:

| السر | موجود في | الخطورة |
|---|---|---|
| توكن بوت تليجرام `8877684899:AAH...` | ٥ commits | 🔴 من يملكه يقرأ كل نسخك الاحتياطية |
| مفتاح `VERIFIER_SECRET` القديم | ٣ commits | 🔴 يسمح بتزوير تراخيص |

**لذلك قبل جعله عاماً — إلزامي:**

**١)** ألغِ توكن البوت من BotFather:
```
/revoke
```
ثم اختر البوت، وخذ التوكن الجديد وضعه في Cloudflare:
```powershell
npx wrangler secret put TG_BOT_TOKEN
```

**٢)** ولّد مفاتيح ترخيص جديدة:
```powershell
npm run license:init
```

> بعد هاتين الخطوتين، الأسرار القديمة في التاريخ تصبح **بلا قيمة**، فلا ضرر من نشرها.

---

### 🅱 المسار الآمن: أبقِ المستودع خاصاً + خادم تحديث خاص

**مناسب إن:** تريد الكود سرياً (وهو الأنسب لمنتج تجاري).

`update.electronjs.org` لا تعمل مع الخاص. البديل: استضف ملف التحديث على **Cloudflare** (عندك حساب بالفعل) وغيّر سطراً واحداً في `src/main/updater.ts`:

```ts
const feed = `https://mobileshop-licensing.mobileshop2026.workers.dev/update/win32-${process.arch}/${app.getVersion()}`;
```

ثم ارفع كل `.exe` جديد إلى R2، ويردّ الـ Worker بـ `{"url": "...", "name": "1.0.1"}`.

**الميزة الإضافية:** تستطيع منع التحديث عن عميل لم يدفع.

---

## ٤. الخطوات لإصدار أول نسخة (بعد اختيار المسار)

**١) ارفع رقم الإصدار:**
```powershell
npm version 1.0.1 --no-git-tag-version
```

**٢) ابنِ المثبّت:**
```powershell
npm run make
```
الملف يظهر في `out\make\squirrel.windows\x64\`

**٣) انشر الإصدار على GitHub:**
```powershell
gh release create v1.0.1 (Get-ChildItem out\make\squirrel.windows\x64\*).FullName --title "1.0.1" --notes "أول إصدار"
```

**٤) تحقق أن الخدمة تراه:**
```powershell
curl.exe "https://update.electronjs.org/contashepo-create/mobileshop/win32-x64/1.0.0"
```
لو رجع JSON فيه رابط → ✅ التحديث التلقائي يعمل.
لو رجع `404` → المستودع ما زال خاصاً.

---

## ٥. قاعدة ذهبية لكل تحديث قادم

> **رقم الإصدار في `package.json` يجب أن يزيد مع كل إصدار.**

الخدمة تقارن رقم العميل برقم آخر إصدار. لو نسيت رفع الرقم، لن ينزّل أحد شيئاً — لأن `1.0.0` ليست أحدث من `1.0.0`.

---

## ٦. ماذا يحدث لبيانات العميل عند التحديث؟

هذا الجزء **مُختبر ومضمون** ✅

| الخطوة | الضمان |
|---|---|
| ١ | نسخة احتياطية كاملة قبل أي تعديل على القاعدة |
| ٢ | ترقية القاعدة داخل معاملة واحدة |
| ٣ | لو فشلت → **استرجاع تلقائي** للنسخة الاحتياطية |
| ٤ | لو كانت القاعدة أحدث من البرنامج → يرفض الفتح بدل إتلافها |

مُثبت في `verify_upgrade_safety.mjs` و `verify_migrations.py`.
