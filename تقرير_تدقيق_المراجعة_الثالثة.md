# تدقيق المراجعة الثالثة — والعثور على خلل لم ترصده أي مراجعة

**التاريخ:** 2026-07-30
**النسخة المفحوصة:** `2b549ed`

---

## 1. ملاحظة منهجية مهمة

هذه المراجعة تصف **نسخة أقدم من الكود الحالي**. الدليل قاطع — أرقام الأسطر التي تستشهد بها لا توجد:

| ما ورد في المراجعة | الواقع في `2b549ed` |
|---|---|
| `index.ts:47` فيه `sandbox` | `sandbox` في السطر **73** |
| `migrations:483` فيه `DevicePassword` | في السطر **524** |
| `migrations:1129` فيه `admin123` | في السطر **1368** |
| `license.handlers.ts:259` يقرأ `activation_codes.dat` | السطر 259 = كشف تلاعب الساعة. **لا توجد أي قراءة** لهذا الملف في الكود كله |
| `database.handlers.ts:132` فيه `copyFileSync(dbPath,…)` | السطر 133 = تعليق يشرح **لماذا لا نستخدم** `copyFileSync` |

**أربعة بنود من قائمة «أولوية 1» أصلحتُها بالفعل في الدفعة السابقة (`2b549ed`):**

| البند | الحالة الفعلية |
|---|---|
| توحيد النسخ الاحتياطي على `db.backup()` | ✅ مُصلَح — لا يوجد `copyFileSync` لقاعدة مفتوحة إطلاقاً |
| `db:createNetwork` ينسخ قاعدة مفتوحة | ✅ مُصلَح — `await currentDb.backup(dbPath)` |
| التحقق من صحة SQLite في `db:changePath` | ✅ مُصلَح — يفحص بصمة `SQLite format 3` |
| حماية الرفع للسحابة | ✅ مُصلَح — يرفض أي رابط غير `https` |

**وبند «الترخيص لا يعمل بين الأجهزة» خاطئ للمرة الثانية.** لا يوجد أي `read` لملف `activation_codes.dat` في الكود. `verifyCode()` يتحقق من توقيع HMAC مضمَّن داخل الكود ومربوط بمعرّف الجهاز.

**و«لا توجد اختبارات آلية»:** المراجعة نفسها ذكرت في أعلاها أنها «شغّلت جميع أدوات التحقق» ونقلت نتائجها (205/205، 44/44، 34/34، 14/14) — ثم أدرجت غيابها كخلل. الفحوصات **33 مجموعة** تعمل بـ `npm run verify`.

---

## 2. 🔴 خلل حقيقي وخطير لم ترصده أي مراجعة

عثرتُ عليه بقراءة `connection.ts` سطراً سطراً، لا من أي تقرير.

الدالتان `getDb()` و `getDbPath()` كانتا تقرآن `db_settings.json` **كلٌّ بقاعدته الخاصة**:

- `getDb()` يشترط `fs.existsSync(settings.dbPath)` ويعود **صامتاً** للمسار الافتراضي
- `getDbPath()` يُرجع المسار المُعَد **دون أي تحقق**

قِستُ النتيجة فعلياً:

```
SCENARIO: shop is on a network share that is offline right now
  getDb()    will open : /tmp/waltest/ud/mobile_shop.db
  getDbPath()  reports : /tmp/waltest/ud/NETWORK/shop.db
  -> *** DISAGREE: a restore writes to a file the app will never open ***
```

### لماذا هذا خطير على محل حقيقي

قاعدة البيانات على مجلد شبكة، والشبكة منقطعة لحظة التشغيل:

1. البرنامج يفتح **قاعدة أخرى فارغة** دون أي تحذير
2. المالك يرى محله فارغاً ويظن أن البيانات ضاعت
3. يستعيد نسخة احتياطية — و`backup:restore` يكتبها إلى **المسار الشبكي غير المتاح**
4. البرنامج يقول «تمت الاستعادة» — والمحل ما زال فارغاً
5. وأسوأ: أي بيع يُسجَّل الآن يذهب إلى القاعدة الخطأ

### الإصلاح

- مُحلِّل **واحد** (`configuredDbPath`) تستخدمه الدالتان، فلا يمكن أن تفترقا
- `getDbPath()` يُرجع `db.name` من الاتصال المفتوح — أي الملف المستخدم فعلاً
- قاعدة غير متاحة → **رسالة خطأ صريحة** تُحذّر قبل إدخال أي بيانات
- ملف إعدادات تالف → يُسجَّل بدل ابتلاعه

---

## 3. بنود صحيحة — وموقفي منها بصراحة

| البند | موقفي |
|---|---|
| **`sandbox: false`** | ✅ صحيح. فحصتُ الجدوى: `preload.ts` لا يستورد سوى `electron` ولا يستخدم أي واجهة Node — فنيّاً `sandbox: true` آمن، ونوافذ الطباعة تعمل به بالفعل. **لكن** حاولتُ تنزيل Electron للاختبار الفعلي وفشل التنزيل هنا. **لن أغيّر إعداداً أمنياً في عملية الإقلاع دون تشغيل البرنامج والتأكد أنه يفتح.** خطر تعطيل البرنامج عند المالك أكبر من المكسب |
| **`DevicePassword` نص صريح** | ✅ صحيح. تحققتُ أنه **لا يُصدَّر** في CSV ولا يظهر في الطباعة — فالمخاطرة محصورة بمن يفتح ملف `.db` مباشرة. الحل النظيف SQLCipher ويحتاج إعادة بناء وهجرة |
| **`catch {}` الصامتة (49 موضعاً)** | ⚠️ صحيح جزئياً. أصلحتُ الموضعين في `connection.ts` (وهما الأخطر). أما الـ `ALTER TABLE` في الهجرات فابتلاع الخطأ فيها **مقصود وصحيح** — تعني «العمود موجود مسبقاً»، وهي محميّة بـ `verify_migrations.py` |
| **دفتر قيود `ledger_entries`** | ✅ أتفق معمارياً — إعادة هيكلة كبرى تحتاج جولة اختبار مستقلة |
| **`admin123`** | ⚠️ `setup:initialize` يفرض كلمة مرور ≥ 6 أحرف ويُغلق بعد الإعداد |

---

## 4. الإثبات

`scripts/verify_backup_integrity.mjs` → **28 فحصاً** (نصفها سلوكي على قواعد SQLite حقيقية)
اختبار الطفرات → **23 مكتشفة / 0 نجت**
الفحص الكامل → **33 مجموعة خضراء، exit 0**

---

## 5. الخلاصة

| # | البند | النتيجة |
|---|---|---|
| 1 | تباعد مسار قاعدة البيانات | 🔴 **جديد — أصلحتُه** |
| 2 | 4 بنود «أولوية 1» | ✅ مُصلَحة في `2b549ed` |
| 3 | الترخيص | ❌ ادعاء خاطئ (للمرة الثانية) |
| 4 | الاختبارات الآلية | ❌ ادعاء خاطئ |
| 5 | `sandbox` / `DevicePassword` / دفتر القيود | 🟠 صحيحة ومفتوحة بأسباب معلنة |

**الدرس:** المراجعة التي تُعيد سرد أخطاء مُصلَحة تُهدر الوقت وتصرف الانتباه عن خلل حقيقي — كتباعد المسار الذي كان يمكن أن يُفقد محلاً يوم عمل كامل.
