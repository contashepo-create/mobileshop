# تحليل وخطة: التحكم عن بُعد + بوت تليجرام + مزامنة سحابية

**التاريخ:** 2026-07-28
**الحالة:** تحليل وخطة — لم يُنفَّذ شيء بعد، بانتظار قرارك

---

# القسم الأول: تحليل فكرتك

فكرتك سليمة في جوهرها وقابلة للتنفيذ بالكامل. لكن فيها **خمس نقاط ستنكسر** لو نُفِّذت كما وُصفت حرفياً. سأشرح كل واحدة بما تحققت منه فعلياً.

## ✅ ما هو صحيح في فكرتك

| الفكرة | الحكم |
|---|---|
| برنامج محلي لتوليد الأكواد | **صحيح** — يعمل بلا إنترنت، والمفتاح السري لا يغادر جهازك |
| بوت تليجرام للتوليد من أي مكان | **صحيح** — يحل مشكلة «أنا في الشارع والعميل ينتظر» |
| قاعدة بيانات سحابية للمزامنة | **صحيح** — مصدر حقيقة واحد بين البوت والبرنامج المحلي |
| التطبيق يُبلّغ عن نفسه (Heartbeat) | **صحيح** — هكذا تعمل كل برامج SaaS فعلياً |
| رسائل فردية/جماعية للعملاء | **صحيح** — ميزة قياسية (Remote Config) |
| نسخ احتياطية على البوت | **صحيح** — تليجرام يقبل ملفات حتى 50MB مجاناً |

## ⚠️ الخمس نقاط التي ستنكسر

### 1. تصادم الأرقام التسلسلية — سيحدث حتماً

الكود الحالي يحمل رقماً تسلسلياً من 3 بايت. لو ولّدت من البوت والبرنامج المحلي في نفس اليوم:

```
البوت أصدر  : Serial #5  →  للعميل أحمد
المحلي أصدر : Serial #5  →  للعميل محمد     ← نفس الرقم!
```

**النتيجة:** منطق «تم استخدام هذا الكود» يخلط بين العميلين، ولا تعرف أي كود ذهب لمن.

**الحل المؤكد:** تقسيم النطاق ببِت واحد:

| المصدر | النطاق | العدد المتاح |
|---|---|---|
| البوت (متصل) | `1 … 8,388,607` | 8.3 مليون |
| المحلي (بلا إنترنت) | `8,388,608 … 16,777,215` | 8.3 مليون |

لا تصادم أبداً، حتى لو ولّدت من المكانين في نفس الثانية دون اتصال بينهما.

### 2. Supabase وRender **سيتوقفان** على الخطة المجانية

تحققت من هذا لأنه يحدد اختيار المنصة:

| المنصة | ما يحدث فعلياً |
|---|---|
| **Supabase مجاني** | يتوقف المشروع بعد **7 أيام** بلا نشاط. الاستئناف يستغرق ~30 ثانية |
| **Render مجاني** | يُوقف الخدمة بعد **15 دقيقة** خمول. أول طلب بعدها بطيء جداً |
| **Cloudflare Workers** | **لا توقف إطلاقاً** — 100 ألف طلب/يوم، بلا cold start |

بوت الترخيص خدمة تُستدعى **بشكل متقطع** — قد تمر أيام بلا طلب تفعيل. هذا **أسوأ نمط ممكن** لـ Supabase وRender.

**السيناريو الواقعي:** عميل يحتاج تفعيلاً عاجلاً يوم جمعة، والمشروع متوقف منذ 8 أيام → البوت لا يرد.

الحيلة الشائعة (ping كل 14 دقيقة) هشّة: GitHub Actions يُعطّل المهام المجدولة بعد 60 يوماً من خمول المستودع، فتتوقف الحماية بصمت.

### 3. مزامنة ثنائية الاتجاه = تعارضات

«تناسق ومزامنة بين البرنامج المحلي والبوت» — لو كلاهما يكتب في نفس السجل، ماذا لو عدّلت اسم عميل محلياً بينما البوت عدّله أيضاً؟

**الحل:** لا تجعلها ثنائية. **السحابة هي مصدر الحقيقة الوحيد**، والبرنامج المحلي:
- يقرأ منها دائماً
- يكتب فيها عند وجود إنترنت
- يعمل بلا إنترنت في وضع «توليد فقط» ثم يرفع ما ولّده لاحقاً

هذا يُلغي التعارض من أساسه بدل محاولة حلّه.

### 4. الخصوصية — قضية قانونية وأخلاقية

«التطبيق يرسل لي رسائل عن حالته» — هذا صحيح، لكن **ما الذي يُرسَل بالضبط؟**

معرّف الجهاز الحالي مشتقّ من MAC + CPU + hostname (مُجزَّأ بـ SHA-256، وهذا جيد ✅). لكن يجب أن يبقى الإرسال محصوراً في:

| ✅ يُرسَل | ❌ لا يُرسَل أبداً |
|---|---|
| معرّف الجهاز (hash) | أسماء العملاء أو أرقامهم |
| حالة الترخيص وتاريخ الانتهاء | الفواتير أو المبالغ |
| رقم إصدار التطبيق | أي بيانات محاسبية |
| اسم المحل (للتعرّف عليه) | كلمات المرور أو الأرصدة |
| آخر ظهور | محتوى قاعدة البيانات |

**سبب هذا ليس أخلاقياً فقط:** لو تسربت لوحة تحكمك، لا تريد أن تكون قد سرّبت معها بيانات كل عملائك المالية.

**وأهم من ذلك:** يجب إبلاغ العميل. سطر واحد في شاشة الترخيص: «يتصل البرنامج بخادم المطور للتحقق من الاشتراك وإرسال حالة التشغيل فقط».

### 5. «التحكم عن بُعد» — أين الخط الأحمر؟

هنا أنصحك بوضوح: **تحكّم في العرض، لا في البيانات.**

| ✅ آمن | ⚠️ خطر شديد |
|---|---|
| تغيير اسم البرنامج | حذف بيانات العميل عن بُعد |
| تحديث بيانات التواصل والدفع | تعديل الفواتير أو الأرصدة |
| رسائل فردية أو جماعية | تعطيل فوري بلا إنذار |
| ملاحظات الإصدار | الوصول لقاعدة بياناته |

**السبب عملي:** لو وُجد مسار في التطبيق يحذف البيانات بأمر خارجي، فأي اختراق لحسابك على تليجرام = تدمير محاسبة كل عملائك. الأمان الحقيقي أن هذا المسار **غير موجود من الأساس**.

للتعطيل: اترك الترخيص ينتهي بنفسه (المدة تحمي حقك تلقائياً).

---

# القسم الثاني: المعمارية المقترحة

```
┌──────────────────────┐         ┌──────────────────────┐
│  برنامجك المحلي       │◄───────►│   Cloudflare Worker  │
│  (Electron)          │  HTTPS  │   + D1 (SQLite)      │
│                      │         │   مصدر الحقيقة        │
│  • توليد أكواد        │         │   لا يتوقف أبداً       │
│  • لوحة العملاء       │         └──────────┬───────────┘
│  • تصدير Excel/DB    │                    │
│  • يعمل بلا إنترنت    │          ┌─────────┴─────────┐
└──────────────────────┘          │                   │
                          ┌───────▼──────┐   ┌────────▼────────┐
                          │ بوت تليجرام   │   │ تطبيقات العملاء  │
                          │ توليد + تنبيه │   │ heartbeat يومي   │
                          └──────────────┘   └─────────────────┘
```

## لماذا Cloudflare Workers + D1؟

| المعيار | Cloudflare | Supabase | Render | Firebase |
|---|---|---|---|---|
| **التوقف التلقائي** | ❌ لا يتوقف | ⚠️ 7 أيام | ⚠️ 15 دقيقة | ❌ لا يتوقف |
| **Cold start** | ❌ لا يوجد | ~30 ثانية | بطيء | متوسط |
| **الحد المجاني** | 100 ألف طلب/يوم | 500MB | 750 ساعة | محدود |
| **قاعدة البيانات** | D1: 5GB مجاناً | 500MB | منفصلة | Firestore |
| **مناسب لحالتك** | ✅ **الأفضل** | ⚠️ التوقف قاتل | ⚠️ التوقف قاتل | ⚠️ معقّد |

**الحساب الواقعي لحالتك:** 100 عميل × heartbeat يومي = **100 طلب/يوم**. الحد المجاني 100,000 — أي أنك تستخدم **0.1%**. لن تدفع شيئاً أبداً.

D1 هو SQLite — **نفس محرك برنامجك الحالي**، فالمخطط والاستعلامات متطابقة تقريباً.

---

# القسم الثالث: خطة التنفيذ على 5 مراحل

## المرحلة 1: تقسيم النطاقات (أساس كل ما بعدها)

قبل أي شيء، يجب منع تصادم الأرقام:

```
serial = (المصدر << 23) | العداد
  المصدر 0 = السحابة/البوت
  المصدر 1 = المحلي
```

**تعديل بسيط** على `licenseCrypto.ts` و`license-keygen.js`. بدونه كل ما بعده يبني على أساس معطوب.

## المرحلة 2: الخادم السحابي

جدولان في D1:

```sql
devices (
  device_id TEXT PRIMARY KEY,
  shop_name TEXT,
  app_version TEXT,
  license_expiry TEXT,
  first_seen TEXT,
  last_seen TEXT,
  status TEXT          -- trial | active | expired
)

licenses (
  serial INTEGER PRIMARY KEY,
  device_id TEXT,
  code TEXT,
  expiry_days INTEGER,
  issued_at TEXT,
  issued_by TEXT,      -- bot | local
  note TEXT
)
```

**ثلاث نقاط وصول:**

| المسار | من يستدعيه | الوظيفة |
|---|---|---|
| `POST /heartbeat` | تطبيق العميل | يبلّغ عن حالته، يستقبل الرسائل والإعدادات |
| `POST /issue` | البوت + برنامجك | يولّد كوداً ويسجّله |
| `GET /devices` | برنامجك | يجلب كل الأجهزة للوحة التحكم |

**الأمان:** كل نقطة تتطلب مفتاحاً في الترويسة. مفتاح التطبيقات **مختلف** عن مفتاحك الإداري — فحتى لو استُخرج من ملف التطبيق، لا يمنح صلاحية إصدار أكواد.

## المرحلة 3: بوت تليجرام

```
/new <device_id> 365    → يولّد كوداً ويرسله جاهزاً للنسخ
/devices                → قائمة الأجهزة وحالتها
/device <id>            → تفاصيل جهاز
/expiring               → اشتراكات تنتهي خلال 14 يوماً
/msg <id> <نص>          → رسالة لعميل واحد
/broadcast <نص>         → رسالة للجميع
/backup                 → يرسل قاعدة البيانات (.db + .xlsx)
```

**تنبيهات تلقائية تصلك:**
- 🆕 جهاز جديد ثبّت التطبيق
- ⏰ اشتراك ينتهي خلال 7 أيام
- ❌ اشتراك انتهى
- ⚠️ محاولة تفعيل فاشلة متكررة

**نقطة أمان حرجة:** البوت يجب أن يتحقق من `chat_id` الخاص بك فقط. بدون ذلك، أي شخص يجد البوت يولّد أكواداً مجانية.

## المرحلة 4: التطبيق عند العميل

**Heartbeat يومي** (مرة كل 24 ساعة، وعند الإقلاع):

```jsonc
// يُرسَل
{ "device_id": "a1b2…", "app_version": "1.0.0",
  "shop_name": "محل الأمل", "license_status": "active" }

// يُستقبَل
{ "messages": [{ "id": 12, "title": "تنبيه", "body": "…" }],
  "config": { "app_name": "…", "support_phone": "…", "payment_info": "…" },
  "latest_version": "1.1.0", "release_notes": "…" }
```

**قواعد التصميم الملزمة:**

1. **الفشل صامت** — لا إنترنت؟ التطبيق يعمل طبيعياً. الترخيص المحلي يبقى الحَكَم.
2. **لا يعطّل شيئاً** — الخادم يعرض معلومات فقط، لا يوقف البرنامج.
3. **تشغيل غير متزامن** — لا يؤخر إقلاع التطبيق إطلاقاً.

## المرحلة 5: البرنامج المحلي

**تطبيق Electron صغير منفصل** (وليس داخل برنامج العميل):

- لوحة بكل الأجهزة وحالتها وآخر ظهور
- توليد كود بضغطة (يعمل بلا إنترنت بنطاق `local`)
- تصدير Excel و`.db`
- رفع نسخة احتياطية للبوت
- **المفتاح السري هنا فقط** — لا يُشحن للعملاء إطلاقاً

**عند غياب الإنترنت:** يولّد بالنطاق المحلي ويضع الكود في طابور، ويرفعه تلقائياً عند عودة الاتصال.

---

# القسم الرابع: نظام الرسائل والإعدادات عن بُعد

## الرسائل (فردية + جماعية معاً)

```sql
messages (
  id INTEGER PRIMARY KEY,
  target TEXT,        -- 'all' أو device_id محدد
  title TEXT,
  body TEXT,
  severity TEXT,      -- info | warning | urgent
  created_at TEXT,
  expires_at TEXT
)

message_reads (message_id, device_id, read_at)
```

التطبيق يجلب الرسائل الموجّهة إليه (`target = 'all' OR target = <جهازه>`) ويعرضها في جرس التنبيهات، ويؤكد القراءة — **فتعرف من قرأ ومن لم يقرأ**.

## الإعدادات عن بُعد

```sql
remote_config (
  key TEXT,
  target TEXT,        -- 'all' أو device_id
  value TEXT,
  PRIMARY KEY (key, target)
)
```

**قاعدة الأولوية:** إعداد الجهاز يتفوق على الإعداد العام. هذا يحقق طلبك «فردي أو جماعي أو الاثنين معاً» بجدول واحد.

**قائمة بيضاء إلزامية** للمفاتيح المسموح تغييرها عن بُعد:

```
✅ app_name, support_phone, support_email, support_telegram,
   payment_info, release_notes, latest_version
❌ أي مفتاح محاسبي (owner_capital, allow_negative_*, db_path)
```

هذه القائمة **في كود التطبيق** لا في السحابة — فحتى لو اختُرق الخادم، لا يستطيع تغيير سلوك محاسبي.

---

# القسم الخامس: التقدير الزمني والتكلفة

| المرحلة | الوصف | الحجم |
|---|---|---|
| 1 | تقسيم النطاقات | صغيرة |
| 2 | Worker + D1 + نقاط الوصول | متوسطة |
| 3 | بوت تليجرام | متوسطة |
| 4 | Heartbeat + الرسائل في التطبيق | متوسطة |
| 5 | البرنامج المحلي | كبيرة |

**التكلفة الشهرية: صفر** ضمن الحدود المجانية (تستخدم 0.1% من حد Cloudflare بـ 100 عميل).

**ما تحتاجه منك:**
- حساب Cloudflare (مجاني)
- بوت من `@BotFather` (مجاني)
- `chat_id` الخاص بك

---

# القسم السادس: توصيتي

نفّذ **المراحل 1 → 2 → 3 → 4** أولاً. هذه وحدها تعطيك:

- توليد الأكواد من أي مكان عبر تليجرام ✅
- معرفة كل جهاز ثبّت التطبيق ✅
- تنبيهات الاشتراكات المنتهية ✅
- رسائل وإعدادات عن بُعد ✅
- نسخ احتياطية تلقائية ✅

**المرحلة 5 (البرنامج المحلي) أجّلها** — لأن البوت سيغطي 90% مما تحتاجه يومياً، وستكتشف بعد أسابيع من الاستخدام ما تريده فعلاً في اللوحة المحلية بدل تخمينه الآن.

---

# قرارات أحتاجها منك

**1. المنصة السحابية**
- (أ) Cloudflare Workers + D1 — **توصيتي** (لا يتوقف، مجاني فعلياً)
- (ب) Supabase — رغم توقفه بعد 7 أيام
- (ج) أخرى تفضّلها

**2. نطاق التحكم عن بُعد**
- (أ) عرض فقط: أسماء، تواصل، رسائل، إصدار — **توصيتي**
- (ب) إضافة تعطيل فوري عن بُعد (أنبّه: خطر عند اختراق حسابك)

**3. البداية**
- (أ) المراحل 1-4 (بوت + سحابة + heartbeat) — **توصيتي**
- (ب) الكل دفعة واحدة
- (ج) المرحلة 1 فقط لتجربتها

**4. الخصوصية**
- هل توافق على إضافة سطر في شاشة الترخيص يوضح للعميل أن التطبيق يرسل حالة الاشتراك؟ (**أنصح بشدة** — قانونياً وأخلاقياً)

أخبرني باختياراتك وأبدأ التنفيذ مع اختبارات تثبت كل جزء.
