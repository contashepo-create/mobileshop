# التحديث التلقائي — على Cloudflare الخاص بك (المستودع يبقى سرياً)

**التاريخ:** ٤ أغسطس ٢٠٢٦
**الحالة:** ✅ **مُنفَّذ ومُختبر** — يحتاج منك ٦ أوامر فقط للتفعيل

---

## ١. جواب سؤالك: نعم، هو نفسه ✅

فحصت الكود، والإجابة **نعم قاطعة**:

| | |
|---|---|
| بوت التليجرام يعمل على | `mobileshop-licensing.mobileshop2026.workers.dev` |
| المسار داخل الـ Worker | `case '/telegram'` في `server/worker.js` |
| نظام التراخيص | **نفس** الـ Worker |
| قاعدة بيانات D1 | **نفس** القاعدة `mobileshop` |
| **التحديثات الآن** | **نفس** الـ Worker ✅ |

**لا حساب جديد، ولا اشتراك جديد، ولا خدمة جديدة.** أضفت مسارات التحديث إلى نفس الملف الذي يشغّل البوت.

**والأهم:** المستودع يبقى **خاصاً (private)** — لن يستطيع أحد رؤية كودك أو نسخه.

---

## ٢. ما الذي بنيته

### في الـ Worker (`server/worker.js`)

| المسار | الوظيفة | الحماية |
|---|---|---|
| `GET /update/win32-x64/<الإصدار>/RELEASES` | يخبر البرنامج بوجود تحديث | `X-Client-Key` |
| `GET /update/win32-x64/<الإصدار>/<ملف>.nupkg` | تنزيل الحزمة | `X-Client-Key` |
| `POST /release` | نشر إصدار جديد | `X-Admin-Key` |

### مصيدة تقنية اكتشفتها وتجنّبتها

Windows لا يقرأ رابط التحديث مباشرة. برنامج **Squirrel** يضيف `/RELEASES` ويتوقع **ملفاً نصياً** بهذا الشكل بالضبط:

```
209C09975C14A1182A4C870A8525E62DDF71279E MobileShopERP-1.0.1-full.nupkg 1050
```

> لو أرجع الخادم **JSON** — وهو ما يكتبه معظم الناس أول مرة — فإن Squirrel **يتجاهله بصمت** ولا يحدث شيء ولا يظهر أي خطأ. الاختبار عندي يتحقق من **الصيغة حرفاً بحرف**.

### ميزة إضافية: التحكم بالاشتراك 💰

| حالة العميل | يستقبل التحديثات؟ |
|---|---|
| اشتراك ساري | ✅ نعم |
| اشتراك **منتهي** | ❌ لا (لكن برنامجه يظل يعمل كما هو) |
| جهاز **محظور** | ❌ لا |
| تثبيت جديد | ✅ نعم |

هذا **مستحيل** مع GitHub العام — أي شخص كان سيحمّل أي إصدار.

---

## ٣. عطلان حقيقيان اكتشفهما الاختبار في كودي أنا

### 🔴 الأول: كل عميل مُحدَّث كان سيرى خطأ خادم

```js
new Response('', { status: 204 })   // ❌ يرمي استثناءً
```

مواصفة Fetch **تمنع** وجود محتوى مع الرمز 204. الاستثناء كان يتحول إلى **خطأ 500** — أي أن **كل عميل على أحدث نسخة** (وهم الأغلبية، كل يوم) كان سيتلقى خطأ خادم بدلاً من «لا يوجد جديد».

### 🔴 الثاني: ثغرة قراءة أي ملف من المخزن

كنت أتحقق من اسم الملف فقط. لكن `URL` يختصر `../` **قبل** أن يراه الكود:

```
/update/win32-x64/1.0.0/../../SECRET/private.key.nupkg
        ↓ يصل مختصراً إلى ↓
/update/SECRET/private.key.nupkg      ← الاسم يبدو سليماً!
```

**قِسته فعلياً وسرّب الملف.** أصلحته بقائمة سماح للمنصات. أُثبت الآن أن الهجوم مغلق وأن التنزيل السليم ما زال يعمل.

---

## 🔻 ٤. الخطوات على جهازك — انسخ سطراً سطراً

**١) اذهب للمشروع:**
```powershell
cd "D:\programing\coding projects\mobile shop"
```

**٢) اسحب التعديلات:**
```powershell
git pull origin master
```

**٣) أنشئ مخزن الملفات (مرة واحدة فقط):**
```powershell
cd server
npx wrangler r2 bucket create mobileshop-updates
```

**٤) انشر الـ Worker المُحدَّث:**
```powershell
npx wrangler deploy
```

**٥) ارجع للمجلد الرئيسي:**
```powershell
cd ..
```

**٦) افتح ملف `.env` وأضف هذا السطر** (خذ القيمة من `ADMIN_KEY` الذي لديك):
```
MOBILESHOP_ADMIN_KEY=Gdea+r1P83v2YQkO1uXKMzoyj+Wt/S/cwFiRdAP6Ujw=
```

> ⚠️ تأكد أن `.env` يحتوي أيضاً على `MOBILESHOP_API_BASE` و `MOBILESHOP_CLIENT_KEY` — بدونهما لن يبحث البرنامج عن تحديثات إطلاقاً.

---

## ٥. كيف تُصدر تحديثاً (كل مرة)

**١) ارفع رقم الإصدار** — إلزامي، وإلا لن ينزّل أحد شيئاً:
```powershell
npm version 1.0.1 --no-git-tag-version
```

**٢) ابنِ المثبّت:**
```powershell
npm run make
```

**٣) انشر:**
```powershell
npm run release
```

الأمر الأخير يقوم بكل شيء تلقائياً:
- يحسب بصمة SHA-1 من الملف نفسه (لا تكتبها بيدك أبداً)
- يرفع الحزمة إلى R2
- يبلّغ الـ Worker
- **ويتحقق من النتيجة كما يفعل برنامج العميل** — فلا يقول «تم» إلا إذا كان التحديث يُقدَّم فعلاً
- يرسل لك إشعاراً على تليجرام 🚀

---

## ٦. متى يصل التحديث للعميل؟

| اللحظة | ما يحدث |
|---|---|
| بعد ٣ دقائق من فتح البرنامج | أول فحص |
| كل ٦ ساعات | فحص دوري |
| عند وجود تحديث | ينزّل **في الخلفية** والعميل يعمل عادي |
| بعد التنزيل | رسالة: «إعادة التشغيل الآن / لاحقاً» |
| **العميل يقرر** | البرنامج لا يغلق نفسه أبداً |

**لا إنترنت؟** لا رسالة خطأ إطلاقاً — يعمل المحل طوال اليوم دون أن يشعر بشيء.

---

## ٧. بيانات العميل عند التحديث — مضمونة ✅

| الخطوة | الضمان |
|---|---|
| ١ | نسخة احتياطية كاملة قبل أي تعديل |
| ٢ | ترقية القاعدة داخل معاملة واحدة |
| ٣ | فشل ⇐ **استرجاع تلقائي** |
| ٤ | قاعدة أحدث من البرنامج ⇐ يرفض الفتح بدل إتلافها |

---

## ٨. ما زال مطلوباً منك (أمني)

حتى مع بقاء المستودع خاصاً، هذان السرّان مكشوفان في تاريخ Git:

**١) ألغِ توكن البوت** — افتح BotFather وأرسل `/revoke`، ثم:
```powershell
cd server
npx wrangler secret put TG_BOT_TOKEN
```

**٢) ولّد مفاتيح ترخيص جديدة:**
```powershell
npm run license:init
```

---

## ٩. الاختبارات

- **مجموعة جديدة `verify_update_server.mjs`: ٥٠ فحصاً، ٠ فشل** — تُشغّل الـ Worker الحقيقي
- **٢٣ عطلاً مزروعاً: ٢٢ ضُبط، ١ أُثبت أنه مكافئ**
- الفحص الشامل: **٦٦ مجموعة، ٠ فشل**
