# 🔑 خطوات توليد مفتاح الترخيص — مصحّحة

**السبب في الخطأ الذي واجهته: خطأي أنا.** الأمر الأخير في رسالتي السابقة كان
`cd server`، فبقيت داخل مجلد الخادم، ثم طلبت منك تشغيل أمر موجود في المجلد
**الرئيسي**. رسالة `MODULE_NOT_FOUND` كانت نتيجة ذلك.

**أصلحت السبب** بإضافة أوامر `npm` تعمل من **أي مجلد** داخل المشروع.

---

## ✅ الأوامر الصحيحة — بالترتيب

### الخطوة ١: ولّد مفتاح الترخيص

```powershell
cd "D:\programing\coding projects\mobile shop"
npm run license:init
```

سيطبع شيئاً مثل:
```
=== Ed25519 LICENCE KEY PAIR GENERATED ===

PRIVATE key saved to: D:\...\scripts\.license-key

Paste this line into src/main/security/licenseCrypto.ts:

export let LICENSE_PUBLIC_KEY = 'ZPtCr6o+DZr5jhQo8rywZ0imDFovh5nkpte8nWIC00k=';
```

### الخطوة ٢: الصق المفتاح العام

افتح `src/main/security/licenseCrypto.ts`، ابحث عن السطر رقم **٦٣** تقريباً،
واستبدل السطر بالكامل بالسطر المطبوع.

> ✅ لاحظت أثناء الإعداد أن السكربت كان يطبع `const` بينما الملف يستخدم `let`
> — أي أن النسخ واللصق كان سيُنتج خطأ. **صحّحت السكربت**، فالسطر المطبوع الآن
> مطابق تماماً لما في الملف.

### الخطوة ٣: غيّر كلمة مرور المطور

```powershell
npm run dev:password -- "عبارة طويلة من عدة كلمات تختارها أنت"
```

والصق السطر المطبوع في `src/main/security/devAuth.ts`.

### الخطوة ٤: اضبط الخادم

```powershell
cd server
npx wrangler secret put LICENSE_PRIVATE_KEY
```

عندما يطلب القيمة، الصق **محتوى الملف** `scripts\.license-key`.
لرؤيته:
```powershell
type "D:\programing\coding projects\mobile shop\scripts\.license-key"
```

### الخطوة ٥: تحقق

```powershell
cd "D:\programing\coding projects\mobile shop"
npm run verify
npm start
```

---

## 🔐 إصدار كود لعميل — بعد الإعداد

```powershell
npm run license:new -- --device <معرّف الجهاز> --days 365
```

أمثلة:
| الغرض | الأمر |
|---|---|
| سنة | `npm run license:new -- --device abc123 --days 365` |
| شهر | `npm run license:new -- --device abc123 --days 30` |
| مدى الحياة | `npm run license:new -- --device abc123 --days 0` |
| حتى تاريخ | `npm run license:new -- --device abc123 --until 2027-01-31` |

> 💡 `--` قبل الخيارات **ضرورية** مع `npm run` لتمرير الوسائط للسكربت.

---

## ⚠️ الأهم على الإطلاق: احفظ المفتاح الخاص

الملف `scripts\.license-key` هو **الشيء الوحيد** الذي يستطيع إصدار تراخيص.

| | |
|---|---|
| ✅ | مستبعد من Git تلقائياً (تحققت من ذلك) |
| ✅ | صلاحياته محصورة بك وحدك |
| 🔴 | **إن فقدته، لن تستطيع إصدار أي كود جديد** وستضطر لتوليد زوج جديد وإعادة إصدار **كل** التراخيص القائمة |

**احفظ نسخة منه في مدير كلمات مرور أو مكان آمن خارج الجهاز — الآن.**

---

## ما تحققت منه فعلياً قبل كتابة هذا الدليل

| الاختبار | النتيجة |
|---|---|
| `npm run license:init` يولّد زوجاً حقيقياً | ✅ |
| المفتاح الخاص يُحفظ بصلاحيات `600` | ✅ |
| Git يتجاهله فعلاً | ✅ |
| كود مُصدَر بالمفتاح الجديد **يقبله التطبيق** | ✅ (٣٦٥ يوماً) |
| نفس الكود على **جهاز آخر** | ✅ مرفوض |
| الأوامر تعمل من **داخل مجلد `server`** | ✅ |
