# ✅ إعدادك مكتمل — وخطوة واحدة متبقية

**التاريخ:** ١ أغسطس ٢٠٢٦

نتيجة `check:env` عندك سليمة تماماً:

```
ok  MOBILESHOP_LICENSE_PUBLIC_KEY
ok  MOBILESHOP_DEV_PASSWORD_HASH
ok  MOBILESHOP_API_BASE
ok  MOBILESHOP_CLIENT_KEY
ok  scripts/.license-key present
```

---

## ⚠️ لكن هناك خطر واحد لا يكشفه `check:env`

**المفتاح الخاص موجود في ثلاثة أماكن يجب أن تتطابق:**

| المكان | الوظيفة |
|---|---|
| `scripts\.license-key` | يوقّع الأكواد التي تصدرها من جهازك |
| `LICENSE_PRIVATE_KEY` على Cloudflare | يوقّع الأكواد التي **يصدرها البوت** |
| `MOBILESHOP_LICENSE_PUBLIC_KEY` في `.env` | **يتحقق** منها داخل التطبيق |

عندما ولّدت زوجاً جديداً، تحدّث الأول والثالث — **لكن الخادم لا يتحدّث تلقائياً**.

### لماذا هذا خطير تحديداً؟

لا شيء يكتشف عدم التطابق:
1. البوت يصدر كوداً بمفتاح **قديم**
2. ترسله للعميل وأنت مطمئن
3. **التطبيق يرفضه** لأنه يتحقق بمفتاح **جديد**

والعطل يظهر في **أسوأ لحظة** — عميل دافع لا يستطيع التفعيل — ويبدو وكأنه مشكلة عنده لا عندك.

---

## 🔻 الحل: أمران فقط

### ١) تأكد أن مفتاحيك متطابقان

```powershell
cd "D:\programing\coding projects\mobile shop"
git pull origin master
npm run check:keypair
```

المتوقع:
```
ok  the private key matches the public key in .env
ok  a test code signs and verifies (138 characters)
```

> 🔴 لو ظهر **MISMATCH** فلا تُصدر أي كود قبل إصلاحه — الرسالة تشرح الحل.

### ٢) حدّث مفتاح الخادم

```powershell
cd server
npx wrangler secret put LICENSE_PRIVATE_KEY
```

وعندما يطلب القيمة، الصق محتوى المفتاح الخاص. لعرضه:

```powershell
type "D:\programing\coding projects\mobile shop\scripts\.license-key"
```

ثم انشر:
```powershell
npx wrangler deploy
```

---

## 🧪 اختبار نهائي قبل أول عميل حقيقي

جرّب الدورة كاملة على **جهازك أنت** قبل أي عميل:

**١)** شغّل البرنامج وخذ معرّف الجهاز من شاشة التفعيل.

**٢)** أصدر كوداً:
```powershell
npm run license:new -- --device <المعرّف> --days 365
```

**٣)** الصقه في شاشة التفعيل. يجب أن يُقبل ويعرض سنة كاملة.

**٤)** كرّر عبر **بوت تليجرام** — هذا ما يختبر مفتاح الخادم فعلياً.

> إن نجحت الطريقتان، فالسلسلة الثلاثية متطابقة وأنت جاهز.

---

## 📌 تذكير أخير عن النسخ الاحتياطي

`scripts\.license-key` هو **الشيء الوحيد** الذي يُصدر التراخيص.

| | |
|---|---|
| ✅ | مستبعد من Git |
| 🔴 | **إن فقدته:** لا كود جديد، ولا تجديد لأي عميل قائم، والحل الوحيد توليد زوج جديد وإعادة إصدار **كل** التراخيص |

**احفظ نسخة الآن** في مدير كلمات مرور أو مكان آمن خارج الجهاز — لا في مجلد المشروع، ولا في مكان يصله Git.
