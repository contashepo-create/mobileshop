# 🗑 تصفير قاعدة البيانات — الحماية الثلاثية وعلاقتها بالاشتراك

**التاريخ:** ٣١ يوليو ٢٠٢٦

---

## ١. سؤالك الأهم: **هل التصفير يؤثر على الاشتراك أو يُعتبر تلاعباً؟**

### ✅ الإجابة القاطعة: **لا. إطلاقاً.**

ولستُ أقولها من الذاكرة — فحصت الكود وأثبتّها باختبارات فعلية. إليك السبب:

### أ) الترخيص **لا يعيش في قاعدة البيانات أصلاً**
الترخيص مُخزَّن في **ملفات منفصلة** داخل مجلد بيانات البرنامج:

| الملف | محتواه |
|---|---|
| `license.dat` | كود التفعيل ومدته |
| `trial.dat` | بداية الفترة التجريبية |
| `device.id` | بصمة الجهاز |
| `lastaccess.dat` | آخر وقت تشغيل |

**التصفير يمسح جداول قاعدة البيانات فقط، ولا يقترب من هذه الملفات نهائياً.**

### ب) جدول `settings` **محفوظ** ولا يُمسح
وهو الذي يحمل ملخّص حالة الترخيص المخزّن محلياً.

### ج) الموضع الوحيد الذي يقرأ فيه الترخيصُ بياناتِ العمل
هو فحص **التلاعب بالساعة**: يقارن تاريخ النظام بأحدث فاتورة. فإن وُجدت فاتورة
بتاريخ *مستقبلي* بشكل غير منطقي، يُستنتج أن التاريخ عُدِّل.

**وهنا المفتاح:** بعد التصفير **لا توجد أي فاتورة**، فالاستعلام يُرجع `NULL`،
والشرط لا يتحقق أصلاً:

```js
if (latestActivity && isImplausiblyFuture(latestActivity, now))
```

أثبتُّ ذلك عملياً في الاختبار: بعد التصفير، الاستعلام يُرجع `null` — أي أن
قاعدة فارغة **لا يمكنها أن تتهمك بالتلاعب**.

### د) إشعار المطور **لا يمسّ الترخيص**
النقطة الجديدة `/database-reset` على الخادم **إشعار فقط**. لا تكتب شيئاً في
جدول `licenses`، ولا تُلغي تفعيلاً، ولا تنقص يوماً واحداً. تحققت من ذلك بفحص
صريح يرفض أي `INSERT/UPDATE/DELETE` على جدول التراخيص داخل تلك الدالة.

> ### 📌 الخلاصة
> **تصفير قاعدة بياناتك حقٌّ طبيعي لك، ولا يُعتبر تلاعباً، ولا ينقص من اشتراكك
> يوماً واحداً.** الشيء الوحيد الذي يُعتبر تلاعباً هو **إرجاع تاريخ الجهاز
> للخلف** لإطالة الاشتراك — وهذا شيء مختلف تماماً عن حذف بياناتك.

---

## ٢. الحماية الثلاثية الجديدة قبل التصفير

لن تُحذف صفٌّ واحد قبل **ثلاثة إثباتات مستقلة**:

| # | الإثبات | ماذا يمنع |
|---|---|---|
| ١ | **كلمة المرور** | من يجلس على الجهاز دون معرفة الحساب |
| ٢ | **رمز ٦ أرقام على تليجرام المحل** | من رأى كلمة المرور تُكتب لكنه لا يملك هاتف المالك |
| ٣ | **نسخة احتياطية مُتحقَّق منها** | فقدان البيانات نهائياً |

### الترتيب مقصود ومُختبَر
```
كلمة المرور ← رمز تليجرام ← نسخة احتياطية ← التحقق منها ← الحذف
```

**لماذا خطوتان منفصلتان؟** الرمز لا يُرسل إلا **بعد** التحقق من كلمة المرور.
بدون ذلك، أي شخص يصل للشاشة يستطيع إغراق هاتفك برسائل تصفير حتى تتجاهلها.

### 🛡 النسخة الاحتياطية **يُتحقق من صلاحيتها**
لا يكفي أن تُكتب. البرنامج يفتحها ويقرأ ترويستها ويتأكد أنها قاعدة SQLite
حقيقية وأن حجمها معقول. **إن فشل التحقق يُلغى التصفير بالكامل.**

> نسخة لم يتحقق منها أحد هي بالضبط النسخة التي تكتشف تلفها يوم تحتاجها.

تُحفظ باسم `before_reset_<التاريخ>.db` في مجلد `backups`.

---

## ٣. الإشعارات — لمن ولماذا

| الجهة | القناة | المحتوى |
|---|---|---|
| **المالك** | بوت **المحل** | تم التصفير، بواسطة مَن، متى، واسم ملف النسخة |
| **المطور (أنت)** | بوت **المطور** عبر الخادم | اسم المحل، اسم المستخدم، الوقت، بصمة الجهاز |

### 🔒 إشعارك **لا يحمل أي بيانات عمل**
لا عميل، ولا رصيد، ولا فاتورة، ولا سعر. فقط: **أن العملية حدثت**.
أضفت فحصاً يرفض أي كلمة من `customer/balance/invoice/price/amount/sales` في
حمولة الإشعار.

**فائدته لك:** لو اتصل بك عميل لاحقاً يشكو فقدان بياناته، لديك التاريخ والحساب
الذي نفّذها، وتعرف أن نسخة احتياطية مُتحقَّقاً منها موجودة على جهازه.

### كلا الإشعارين لا يمكن أن يُفشل العملية
بحلول وقت إرسالهما تكون البيانات قد حُذفت فعلاً. لو فشل الإنترنت، **لا يجوز**
إخبار المستخدم بأن التصفير فشل — لأنه سيعيده. السجل الدائم في `security_events`
هو المرجع.

---

## ٤. 🧪 التحقق — كما طلبت

### ٨٠ فحصاً لقسم التصفير، كلها ناجحة
- **٩ أعطال محقونة: ٩ اكتُشفت، ٠ نجت**

| # | العطل | النتيجة |
|---|---|---|
| ١ | تخطي رمز تليجرام | ✅ ٣ فحوص فشلت |
| ٢ | الحذف قبل النسخة الاحتياطية | ✅ |
| ٣ | عدم التحقق من صلاحية النسخة | ✅ |
| ٤ | إلغاء إشعار المطور | ✅ |
| ٥ | إلغاء إشعار المالك | ✅ |
| ٦ | إرسال الرمز دون كلمة مرور | ✅ |
| ٧ | **خلط الأغراض** (رمز كلمة المرور يُصفّر القاعدة) | ✅ بعد إصلاح فحصي |
| ٨ | تسريب بيانات عمل لإشعار المطور | ✅ |
| ٩ | إلغاء مصادقة نقطة الخادم | ✅ |

---

## ٥. 🔴 ثغرة تغطية اكتشفها اختبار الطفرات

العطل رقم ٧ هو الأخطر: **جعل رمزاً صادراً لاستعادة كلمة المرور صالحاً لتصفير
قاعدة البيانات**. تخيّل: المالك يطلب رمز كلمة مرور، فيراه أحدهم على الهاتف
ويستخدمه لمحو المحل كله.

هذا العطل **نجا** في المحاولة الأولى.

حققت فيه فوراً: شغّلت سيناريو حقيقياً وتبيّن أن **الكود سليم فعلاً** — الأغراض
معزولة بالفعل. لكن **لم يكن هناك أي فحص يثبت ذلك**، فبقيت الحماية بلا حارس.

أضفت القسم `[7b]` بعشرة فحوص سلوكية تثبت العزل في الاتجاهين، وأن رمز التصفير
له نفس الحدود (انتهاء صلاحية، ٥ محاولات، ٣ طلبات/ساعة، مربوط بالمستخدم،
استخدام واحد). فصار العطل يُكتشف.

> **المبدأ:** حماية موجودة في الكود لكن لا يحرسها اختبار = حماية مؤقتة، تختفي
> عند أول إعادة هيكلة.

---

## ٦. ⚠️ مطلوب منك

بما أنني أضفت نقطة `/database-reset` للخادم، **يجب إعادة نشره** وإلا لن تصلك
الإشعارات (والتصفير سيعمل عادياً، لكن بلا إشعار لك).
