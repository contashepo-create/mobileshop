# تقرير: تسريب المعلومات في رسائل الأخطاء

**التاريخ:** 5 أغسطس 2026
**المالك:** Acc. Mohamed Abdou
**النطاق:** كل مسارات الأخطاء في البرنامج + خادم Cloudflare

---

## ١. الخلاصة

| المؤشر | القيمة |
|---|---|
| مواضع كانت تُعيد الخطأ الخام | **31** |
| نقاط الخادم السحابي المتأثرة | **كل النقاط العامة** |
| ثغرة تُخرج خطأ غير مُلتقَط | **1** |
| اختبارات جديدة | **119 ✅** |
| طفرات محقونة / مُكتشَفة | **30 / 30** ✅ |

---

## ٢. ما الذي كان يظهر للمستخدم فعلياً

كانت ٣١ معالجة تنتهي هكذا:

```ts
catch (err: any) { return { success: false, message: err.message }; }
```

والواجهة تضع `result.message` مباشرة في رسالة منبثقة يقرأها من يقف عند الكاشير.

**قِستُ ما تقوله هذه الرسائل فعلياً — بتوليد أخطاء حقيقية، لا بالتخمين:**

```
ENOENT: no such file or directory, open '/home/user/.../mobile_shop.db'
EACCES: permission denied, open '/proc/1/mem'
UNIQUE constraint failed: customers.Phone
NOT NULL constraint failed: customers.Name
no such table: secret_table
near "SELEC": syntax error
Unknown named parameter 'saleId'
file is not a database
```

### ثلاث مشاكل منفصلة في هذه القائمة

| # | المشكلة | لماذا تهم |
|---|---|---|
| ١ | **مسارات مطلقة** | `ENOENT ... '/Users/mohamed/...'` تكشف اسم حساب النظام، وبنية المجلدات، **وأين تُحفظ دفاتر المحل**. وعلى جهاز كاشير مشترك، القارئ ليس بالضرورة صاحب المحل |
| ٢ | **بنية قاعدة البيانات** | `UNIQUE constraint failed: customers.Phone` تكشف اسم الجدول والعمود. تكرارها عبر عشرات العمليات = **خريطة مجانية للقاعدة** |
| ٣ | **لا فائدة منها للقارئ** | «Unknown named parameter 'saleId'» لا تقول لصاحب المحل شيئاً يفعله |

> الرسالة التي تفيد صاحب المحل والرسالة التي تفيد المهاجم **ليستا نفس الرسالة**.

---

## ٣. 🔴 الخادم السحابي: كل نقطة عامة كانت تكشف القاعدة

شغّلتُ دالة `fetch` **الحقيقية** للـ Worker مع قاعدة D1 تُخطئ. النتيجة المقاسة:

| النقطة | ما يراه أي شخص مجهول |
|---|---|
| `GET /health` | `{"ok":false,"error":"D1_ERROR: no such table: devices at /worker/db.js:412"}` |
| `GET /devices` | نفسها |
| `POST /heartbeat` | نفسها |
| `POST /registration` | نفسها |
| `GET /update/.../RELEASES` | نفسها |

> **`/health` لا تحتاج أي مفتاح.** أي شخص يرسل طلباً مشوَّهاً ويقرأ أسماء الجداول من الرد — **مسبار مجاني لبنية القاعدة**.

**بعد الإصلاح:**
```json
{"ok":false,"error":"internal error","ref":"KSMG7M"}
```
والتفاصيل الكاملة (مع الـ stack) تذهب إلى سجل الخادم بنفس الرقم المرجعي.

---

## ٤. الحل: وحدة مركزية واحدة

أنشأتُ `src/main/security/errorResponse.ts`:

| الوظيفة | ما تفعله |
|---|---|
| `looksTechnical()` | يتعرّف على **١٢ نمطاً**: أكواد errno، أكواد SQLITE_، قيود الجداول، المسارات (POSIX + ويندوز + UNC)، إطارات الـ stack، مواضع الملفات، تشخيصات ربط المعاملات |
| `safeFailure()` | يحوّل أي خطأ إلى رد آمن + **يسجّل التفاصيل الكاملة على الخادم** |
| `safeMessage()` | يفحص رسالة كتبها المطوّر بنفسه، تحسّباً لأن تكون التقطت مساراً بالتضمين |
| `userRefusal()` | يميّز الرسائل **المقصودة للمستخدم** |

### الرقم المرجعي

```
تعذّر إتمام العملية. لم يتم حفظ أي تغيير. ... (رمز: C9CW02)
```

وفي سجل الخادم:
```
[IPC:backup:restore] ref=C9CW02 Error: ENOENT: no such file or directory, open '/home/user/...'
```

> بدونه تصبح الرسالة العامة طريقاً مسدوداً للدعم: صاحب المحل يقول «فشل»، والمطوّر أمام سجل بأربعين سطراً لا يعرف أيها.

---

## ٥. ما تُرك عمداً — وهذا هو جوهر التصميم

**١) الرسائل المكتوبة للمستخدم.**
«لا يمكن حذف فاتورة الشراء - الصنف لم يعد بالمخزن» هي **التفسير الوحيد** الذي يحصل عليه المحل. استبدالها برسالة عامة كان سيحذف المعلومة الوحيدة المفيدة. هذه مُعلَّمة بـ `userRefusal` وتمرّ — **لكنها تُفحص أيضاً**، لأن بعضها يُبنى بالتضمين وقد يلتقط مساراً.

**٢) المسارات التي طلبها المالك بنفسه.**
`backup:create` يُعيد مسار الملف الذي حفظه في المجلد الذي **اختاره المالك في نافذة الحفظ، على جهازه**. إخباره بمكانه هو الميزة نفسها، لا تسريب.

---

## ٦. ثغرة إضافية: خطأ يخرج بلا التقاط

`maintenance:receive` كانت تربط المدخلات مباشرة في SQL بلا فحص:

```
CustomerID: {}  →  "Provided value cannot be bound to SQLite parameter 4."
```

خطأ **غير مُلتقَط** يعبر حدود IPC، ويسمّي موضع المعامل في الجملة. أُصلح **من المصدر** بفحص كل الحقول قبل أي كتابة.

---

## ٧. أخطاء في اختباري كشفها اختبار الطفرات

حقنتُ ٣٠ طفرة. **خمس نجت** — كل واحدة ضعف حقيقي:

| نجت | السبب | الإصلاح |
|---|---|---|
| **E04** حذف نمط مسارات POSIX | عيّناتي كانت تُلتقط بـ **٢-٣ أنماط معاً**، فحذف واحد لا يغيّر شيئاً | أضفتُ `تعذّر الوصول إلى /home/...` — يلتقطه نمط **واحد فقط** |
| **E06** حذف نمط الـ stack | نفس السبب | أضفتُ `at Database.prepare (native)` |
| **E17** تعطيل `safeMessage` | **لم يكن أي اختبار يستدعيها إطلاقاً** | ٥ فحوص جديدة |
| **E27** حذف تسجيل الحارس | لم أتحقق أن الحارس **يسجّل** ما يخفيه — إصلاح تحوّل إلى تستّر | فحص أن `console.error` **داخل** الـ catch |
| **E25** إزالة فحص العميل | **طفرة مكافئة**: فحص الوجود بعدها يرفض `{}` أصلاً | استُبدلت بصيغة تُزيل الفحصين معاً |

**النتيجة النهائية: 30 / 30 ✅**

---

## ٨. كل ملف عُدِّل

| الملف | المواضع |
|---|---|
| `server/worker.js` | **الالتقاط العام** لكل النقاط |
| `delete.handlers.ts` | 8 |
| `sales.handlers.ts` | 5 |
| `database.handlers.ts` | 5 |
| `license.handlers.ts` | 3 |
| `purchases.handlers.ts` | 3 |
| `settings.handlers.ts` | 3 |
| `backup.handlers.ts` | 2 |
| `inventory.handlers.ts` | 2 |
| `services.handlers.ts` | 1 |
| `transfers.handlers.ts` | 1 |
| `maintenance.handlers.ts` | فحص كامل للمدخلات |
| **`errorResponse.ts`** | **ملف جديد** |

---

## ٩. حالة الاختبارات

```
verify_error_disclosure.mjs ....... 119 / 119 ✅  (جديد)
اختبار الطفرات .................... 30 / 30 ✅
npm run verify .................... 76 حزمة، 0 إخفاق ✅
```

---

## 🔻 الأوامر

**١)**
```powershell
cd "D:\programing\coding projects\mobile shop"
```

**٢)**
```powershell
git pull origin master
```

**٣) انشر الخادم** (إصلاح تسريب D1 لا يعمل قبل النشر)
```powershell
cd server
npx wrangler deploy
```

**٤)**
```powershell
cd ..
```

**٥) شغّل وجرّب**
```powershell
npm run dev
```

**جرّب هذه:**
- احفظ نسخة احتياطية في مجلد عادي ← ينجح ويعرض المسار ✅
- حاول الاستعادة من ملف غير موجود ← «تعذّرت استعادة النسخة الاحتياطية (رمز: XXXXXX)» ✅ **بلا مسارات**
- أضف صنفاً بباركود مكرّر ← «الباركود موجود بالفعل» ✅ (رسالة مفيدة محفوظة)
- احذف فاتورة بيعت أصنافها ← الرسالة التفصيلية تظهر كما كانت ✅

---

## ⚠️ ما زال مطلوباً

| # | المطلوب | الأمر |
|---|---|---|
| ١ | **أبطل توكن بوت تليجرام** | BotFather ← `/revoke` ← `npx wrangler secret put TG_BOT_TOKEN` |
| ٢ | **دوّر مفتاح Supabase** | لوحة Supabase ← Settings ← API ← Reset service role key |
| ٣ | مفاتيح ترخيص جديدة | `npm run license:init` |
| ٤ | معرّفات تليجرام | `npx wrangler secret put TG_ADMIN_CHAT` ← `7232305465,1593943219` |
