# تقرير تدقيق المصادقة وإدارة الجلسات

**التاريخ:** ٥ أغسطس ٢٠٢٦ | **الإصدار:** `b2ec640`
**النتيجة:** ✅ **٣ ثغرات حقيقية اكتُشفت وأُصلحت** — والباقي سليم

| المؤشر | النتيجة |
|---|---|
| الفحوصات الجديدة | **٧١ فحصاً، ٠ فشل** |
| الأعطال المزروعة | **١٦ زُرعت، ١٦ ضُبطت** |
| الفحص الشامل | **٧٢ مجموعة، ٠ فشل** |
| أخطاء الأنواع | **٠** |

---

## 🔴 الثغرة الأولى: تسجيل الدخول بلا أي حد للمحاولات

**الملف:** `src/main/ipc/auth.handlers.ts` — سطر **41**

### الخطر
لا يوجد **أي** حد لعدد محاولات كلمة المرور. قِستُ على المعالج الحقيقي:

```
١٠٠ كلمة مرور خاطئة في ٧٬٧٩٧ مللي ثانية
≈ ١٣ محاولة/ثانية — بلا تأخير وبلا إيقاف، إلى الأبد
```

`bcrypt` بتكلفة ١٠ كان المُبطئ الوحيد — وهذا **ميزانية وقت، لا حماية**. قائمة بـ ١٠٠ ألف كلمة مرور شائعة تُستنفد في **ساعتين تقريباً** على جهاز متروك على الكاونتر ليلاً.

> والمفارقة: كونسول المطور كان محمياً بإيقاف منذ كتابته — أما الباب الذي يستخدمه المحل فعلياً فكان مفتوحاً.

### الإصلاح
ملف جديد `src/main/security/loginThrottle.ts`:

| القاعدة | القيمة |
|---|---|
| محاولات فاشلة متتالية | **٥** |
| مدة الإيقاف | **١٥ دقيقة** |
| موضع الفحص | **قبل** قاعدة البيانات و**قبل** bcrypt |

- **يُحتسب للمستخدمين غير الموجودين أيضاً** — وإلا لما تباطأ من يخمّن الأسماء، ولاكتُشف الحساب الحقيقي بمراقبة أيّ اسم يبدأ بالإيقاف
- **غير حسّاس لحالة الأحرف والمسافات** — `Admin` و ` admin ` لا يتجاوزان الإيقاف
- **النجاح يُصفّر العدّاد** — فلا تتراكم أخطاء مطبعية بريئة حتى تُقفل على الكاشير أثناء البيع
- الرسالة تخبر المستخدم بالمدة المتبقية بدل رفض غامض

---

## 🔴 الثغرة الثانية: كلمة مرور افتراضية منشورة تبقى فعّالة بعد الإعداد

**الملف:** `src/main/database/migrations/index.ts` — سطر **1620**

### الخطر
`seedData` يُنشئ حساب **`admin` / `admin123`** لتكون القاعدة قابلة للاستخدام قبل تشغيل المعالج. لكن المعالج يستبدله **فقط** إذا اختار المالك **نفس الاسم** — والمعالج يدعوه لاختيار اسمه الخاص.

**قياس فعلي** — بعد إعداد كامل وناجح باسم `mohamed`:

```
users بعد الإعداد : admin, mohamed
تسجيل الدخول admin/admin123 => *** ينجح بصلاحيات مدير كاملة ***
```

> كلمة مرور افتراضية منشورة هي **أول ما يجربه أي مهاجم** — وكانت ستُشحن مع كل نسخة.

### الإصلاح
`src/main/ipc/settings.handlers.ts` — عند اختيار اسم مختلف:
1. **تعطيل** الحساب المزروع (`IsActive = 0`)
2. **تشويش** كلمة مروره بـ ٣٢ بايت عشوائية

> **تعطيل وليس حذف:** `UserID = 1` مرتبط بصفوف مزروعة وبأي مستند أُنشئ قبل المعالج — والحذف سيفشل على المفتاح الأجنبي أو يُيتّم تلك السجلات.

---

## 🔴 الثغرة الثالثة: تعطيل المستخدم لا يُخرجه من النظام

**الملف:** `src/main/ipc/users.handlers.ts` — سطر **141** (وسطر **126** لتغيير الدور)

### الخطر
الجلسة تحفظ المستخدم وصلاحياته **في الذاكرة** ولا تعيد قراءة صفّه أبداً. `auth:login` يتحقق من `IsActive`، لكن الجلسة **المفتوحة بالفعل** لا تتحقق.

**قياس فعلي:**
```
١. الكاشير مسجّل دخول على جهاز ٧     ✓
٢. المالك يعطّل الحساب — IsActive = 0 في القاعدة ✓
٣. الجلسة ما زالت صالحة => *** الكاشير المفصول يواصل العمل ***
```

> وهذه **بالضبط** اللحظة التي عُطِّل الحساب من أجلها.

ونفس الشيء لتغيير الدور: مدير يُخفَّض إلى كاشير **يبقى مديراً** حتى يخرج بنفسه.

### الإصلاح
`users:delete` و `users:update` يستدعيان الآن `destroyAllSessionsForUser` — فينتهي الوصول فوراً ويعيد المستخدم الدخول بصلاحياته الجديدة.

---

## ✅ ما فُحص ووُجد سليماً

| المجال | النتيجة |
|---|---|
| تشفير كلمات المرور | **bcrypt** بتكلفة ١٠ مع salt في كل موضع |
| MD5 / SHA1 / نص صريح | **لا شيء** على أي مسار كلمة مرور |
| انتهاء الجلسة بالخمول | ✅ ٨ ساعات |
| **سقف زمني مطلق** | ✅ ١٦ ساعة — ويُفحص **قبل** تحديث آخر نشاط |
| ربط الجلسة | بمعرّف النافذة في العملية الرئيسية — الواجهة لا تستطيع انتحال هوية |
| تفويض القنوات | **رفض افتراضي** — كل الـ ١٧٢ قناة مربوطة، ٠ غير مربوطة |
| توكن المطور | ٣٢ بايت عشوائية، صلاحية ٣٠ دقيقة، تحدٍّ يُستخدم مرة واحدة |
| رموز الاسترداد | `crypto.randomInt`، مهلة، ٥ محاولات، ٣/ساعة، مقارنة زمنية ثابتة |
| كلمة المرور في المتصفح | لا تُحفظ إطلاقاً، والمفتاح القديم يُمحى |
| القنوات العامة (٢٧) | كلٌّ منها له حارسه الخاص (كلمة مرور / توكن / رمز) |

> **ملاحظة:** وجدت `MD5` في `license.handlers.ts:70` — لكنه **متجه تهيئة (IV)** لملف الترخيص للتوافق مع صيغة قديمة، **وليس تجزئة كلمة مرور**. لا خطر منه.

---

## الاختبار المضاد

زرعت **١٦ عطلاً متعمداً** — منها الثلاثة المكتشفة:

```
١٦ زُرعت → ١٦ ضُبطت (١٦/١٦) ✅
```

> اثنان نجيا في الجولة الأولى — وكلاهما كان **فحصاً يطابق النص بدل تشغيل الكود**. أعدت كتابتهما ليستدعيا الدالة الحقيقية. أضفت `__authorizeForTests` لأن فحص النص **لا يستطيع** التمييز بين `throw` و `return null` في فرع يبدو دفاعياً — و«القناة غير المربوطة تُرفض» هي الخاصية التي يقوم عليها نموذج التفويض كله.

---

## 🔻 الخطوات على جهازك

```powershell
cd "D:\programing\coding projects\mobile shop"
```
```powershell
git pull origin master
```

> ⚠️ **إن كنت قد أعددت النظام سابقاً باسم مستخدم غير `admin`** — فحساب `admin/admin123` ما زال فعّالاً في قاعدتك الحالية. عطّله يدوياً من شاشة المستخدمين، أو صفّر قاعدة البيانات (وهي تجريبية على أي حال).
