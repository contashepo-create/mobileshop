# 💾 كيف تستعيد النسخة الاحتياطية

**فحصت الكود فعلياً وجرّبت الاستعادة عملياً قبل كتابة هذا الدليل.**

---

## ١. عندك نوعان من النسخ

| النوع | متى تُنشأ | أين تُحفظ |
|---|---|---|
| **تلقائية** | كل يوم تلقائياً (عند فتح البرنامج + كل ساعة) | مجلد `backups` داخل بيانات البرنامج |
| **يدوية** | عندما تضغط «حفظ نسخة احتياطية» | المكان الذي تختاره أنت |

> ⚠️ **النسخ التلقائية تُحفظ آخر ٧ أيام فقط**، ثم يُحذف الأقدم. أي شيء أقدم من
> أسبوع لن تجده — إلا إذا كنت أخذت نسخة يدوية.

---

## ٢. 🔻 الاستعادة — الطريقة (٤ خطوات)

### الخطوة ١: افتح البرنامج وسجّل الدخول
> الاستعادة تحتاج صلاحية **«تعديل الإعدادات»**، أي **حساب المدير**.
> إن لم تستطع الدخول، استخدم استرداد كلمة المرور برمز تليجرام أولاً.

### الخطوة ٢: اذهب إلى
```
الإعدادات ← النسخ الاحتياطي
```

### الخطوة ٣: اضغط زر **«استعادة من نسخة»**
ستظهر رسالة تحذير — اضغط **موافق**، ثم اختر ملف النسخة (`.db`).

### الخطوة ٤: ⚠️ **أغلق البرنامج وافتحه من جديد**
هذه الخطوة **إلزامية**. البرنامج نفسه يقول لك:
> «تمت الاستعادة - يرجى إعادة تشغيل التطبيق»

قبل إعادة التشغيل ستظل ترى **البيانات القديمة** على الشاشة، لأن البرنامج ما زال
يقرأ من الذاكرة. لا تظن أن الاستعادة فشلت.

---

## ٣. 📁 أين تجد النسخ التلقائية؟

انسخ هذا في شريط عنوان **مستكشف الملفات** (File Explorer):

```
%APPDATA%\mobile-shop-erp\backups
```

ستجد ملفات بهذا الشكل:
```
auto_backup_2026-07-31.db
auto_backup_2026-07-30.db
auto_backup_2026-07-29.db
```

**الاسم يحمل تاريخ اليوم** — اختر اليوم الذي تريد الرجوع إليه.

> 💡 يمكنك أيضاً رؤية القائمة داخل البرنامج في
> **الإعدادات ← إدارة قاعدة البيانات**، مع الحجم والتاريخ.

---

## ٤. 🛟 شبكة الأمان — إن ندمت على الاستعادة

**البرنامج يحتفظ تلقائياً بنسخة من قاعدتك الحالية قبل أن يستبدلها.**

فحصت هذا في الكود وجرّبته: يُحفظ ملف اسمه:
```
mobile_shop.db.before-restore
```
في نفس مجلد قاعدة البيانات.

### للرجوع عن الاستعادة:
1. أغلق البرنامج تماماً
2. احذف (أو أعد تسمية) `mobile_shop.db`
3. أعد تسمية `mobile_shop.db.before-restore` إلى `mobile_shop.db`
4. افتح البرنامج

> ⚠️ لكن انتبه: هذا الملف **يُكتب فوقه في كل استعادة**. لا يوجد إلا آخر واحد.

---

## ٥. ✅ ما تحقّقت منه فعلياً في الكود

لم أكتفِ بقراءة الكود — شغّلت سيناريو استعادة كامل:

| ما جرّبته | النتيجة |
|---|---|
| محل فيه بيعتان (٣٬٥٠٠ ج) ← أخذت نسخة | ✅ |
| ثم بيعة جديدة (٩٬٩٩٩) بعد النسخة | ✅ صارت ٣ بيعات |
| ثم استعدت النسخة | ✅ رجعت **بيعتان و٣٬٥٠٠ بالضبط** |
| هل حُفظت نسخة تراجع؟ | ✅ وفيها الـ٣ بيعات كاملة |

**كذلك تحققت من ٤ حمايات موجودة في الكود:**

| الحماية | لماذا تهم |
|---|---|
| يتأكد أن الملف قاعدة SQLite حقيقية | لو اخترت ملف صورة بالخطأ، يرفض ولا يخرّب شيئاً |
| يغلق قاعدة البيانات قبل الاستبدال | يمنع تلف الملف أثناء الكتابة |
| يحذف ملفات `-wal` و `-shm` القديمة | 🔴 **الأهم:** بدونها قد يُعيد SQLite تشغيل معاملات القاعدة القديمة **فوق** النسخة المستعادة ويخلط البيانات |
| يستعيد إلى **المسار الفعلي** المستخدم | لو كانت قاعدتك على السيرفر/الشبكة، يستعيد هناك لا في مكان آخر |

---

## ٦. ⚠️ تحذيرات مهمة

| # | التحذير |
|---|---|
| ١ | **الاستعادة تمحو كل ما بعد تاريخ النسخة** — كل فاتورة ومصروف وقيد سُجّل بعدها يختفي |
| ٢ | **خذ نسخة يدوية قبل الاستعادة** ولو كانت البيانات الحالية تالفة — قد تحتاج شيئاً منها |
| ٣ | **إعادة التشغيل إلزامية** وإلا ستظن أن الاستعادة لم تنجح |
| ٤ | النسخ التلقائية **٧ أيام فقط** — للحفظ الطويل خذ نسخة يدوية على فلاشة أو سحابة |

---

## ٧. نصيحة محاسبية

النسخة الاحتياطية ليست تأميناً على البيانات فقط، بل على **الدفاتر**. أنصحك:

- **نسخة يدوية شهرية** تُحفظ خارج الجهاز (فلاشة/هارد خارجي/سحابة) — لأن عطل
  القرص الصلب يأخذ النسخ التلقائية معه، فهي على نفس الجهاز.
- **نسخة عند إقفال كل سنة مالية**، تُحفظ بشكل دائم ولا تُمحى.
