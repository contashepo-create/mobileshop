# تقرير الفحص الشامل الثاني — Mobile Shop ERP

**التاريخ:** 2026-07-27
**النطاق:** فحص مستقل كامل من الصفر — 90 ملف، 20,580 سطر، 55 جدول، 205 قناة IPC
**المنهج:** لم أعتمد على أي تقرير سابق. بنيت أدوات فحص جديدة وأعدت التدقيق بالكامل.

---

## المنهجية الجديدة

بدل قراءة الكود والاستنتاج، بنيت **محرك تدقيق مزدوج القيد** يُعيد تنفيذ كل عملية مالية
على قاعدة بيانات SQLite حقيقية، ويتحقق بعد **كل عملية** من المعادلة المحاسبية:

```
الأصول − الخصوم  =  رأس المال + صافي الربح
```

أي عملية تكسر هذه المعادلة تُبلَّغ فوراً مع مقدار الانحراف بالضبط. هذا كشف **7 أخطاء
محاسبية** لم تكن ظاهرة بالقراءة المجردة — لأن كل واحدة منها تبدو صحيحة في موضعها،
والخلل يظهر فقط عند موازنة النظام ككل.

**الأدوات المضافة:**

| الأداة | الوظيفة |
|---|---|
| `audit_accounting_engine.py` | 18 عملية مالية + فحص المعادلة بعد كل واحدة + تسوية كشف الحساب |
| `verify_migrations.py` | 14 فحص: ترقية قاعدة قديمة، تكرار الترحيل، حماية من فقدان البيانات |
| `build_real_db.py` | يبني قاعدة حقيقية من كود الترحيل ويحاكي تشغيل التطبيق مراراً |
| `verify_accounting.py` | 34 تأكيد محاسبي (من الجولة السابقة) |
| `verify_security.py` | 44 فحص أمني (من الجولة السابقة) |
| `verify_ipc_coverage.py` | يضمن أن كل قناة IPC لها قاعدة صلاحية |

`npm run verify` يشغّلها كلها.

---

## النتيجة النهائية

```
المحرك المحاسبي     18 عملية — كلها متوازنة (انحراف 0.00)
تسوية كشف الحساب    مطابق تماماً لرصيد العميل
الاختبارات          126 تأكيد — كلها ناجحة
TypeScript (main)   0 أخطاء
TypeScript (render) 0 أخطاء  (كانت 221)
تغطية IPC           205/205 قناة
الترحيل             0 أخطاء غير حميدة، 0 جداول يتيمة
```

---

# القسم الأول: الأخطاء المحاسبية المكتشفة والمُصلحة

## 1. تسرّب نقدي في كل عملية خدمة ⚠️ حرِج

**الاكتشاف:** المحرك أظهر انحراف `+3.00` في عملية تحويل رصيد.

```
العميل يدفع 105  →  الخزنة +105
الأصل 100 يخرج    →  الماكينة −100
صافي تغير الأصول  =  +5
الربح المسجَّل     =  +2
الفارق            =  3  ← نقد من العدم
```

**السبب:** `ServiceCost` (عمولة المزوّد) و `TransferCost` (رسوم الشبكة) كانا يُسجَّلان
في السجل ويُطرحان من الربح، لكن **لا يُخصمان من أي حساب فعلياً**.

**الأثر التراكمي:** محل يجري 50 عملية يومياً بمتوسط تكلفة 3 ج.م → **4,500 ج.م نقد وهمي شهرياً**.

**الإصلاح:** التكلفة تُخصم الآن من مصدر التمويل، وفحص كفاية الرصيد يشمل الإجمالي
(الأصل + العمولات) بدل الأصل وحده.

---

## 2. تكلفة قطع الصيانة من الواجهة ⚠️ حرِج

**الاكتشاف:** انحراف `−20.00` في صيانة الضمان.

```ts
// قبل — الواجهة تتحكم في التكلفة
let unitCost = data.UnitCost ?? 0;            // ← 80 من الواجهة
if (!unitCost) { unitCost = stock.CostPrice }
// المخزون ينخفض بـ CostPrice الحقيقي = 100
```

القطعة تُسجَّل بتكلفة 80 في `maintenance_parts`، بينما المخزون يفقد 100.
**الفارق 20 يتسرب من الميزانية في كل صيانة.**

**الإصلاح:** التكلفة المحمّلة = تكلفة المخزون المستهلَك دائماً. الواجهة لم تعد تتحكم بها.

---

## 3. التحويل بين المخازن يُهدر قيمة المخزون ⚠️ حرِج

**الاكتشاف:** اختبار مباشر — تحويل 4 قطع أهدر 160 ج.م.

```
مخزن 1: 10 قطعة × 100 = 1,000
مخزن 2:  5 قطعة ×  60 =   300     الإجمالي 1,300

بعد تحويل 4 قطع (بلا تحديث CostPrice):
مخزن 1:  6 × 100 = 600
مخزن 2:  9 ×  60 = 540            الإجمالي 1,140  ← ضاع 160
```

القطع تصل للمخزن الوجهة فتُقيَّم بسعره القديم. **التحويل المخزني يجب أن يكون محايد القيمة تماماً.**

**الإصلاح:** البضاعة تحمل تكلفة مصدرها، والوجهة يُعاد حساب متوسطها المرجّح.
تم التحقق: الفارق `0.000000`.

---

## 4. الراتب المستحق بلا التزام مقابل ⚠️ حرِج

**الاكتشاف:** انحراف `+3,000` عند إصدار راتب بلا دفع.

المصروف يُعترف به عند الإصدار (`Month`)، لكن الالتزام لا يُسجَّل إلا عند **الدفع**.
راتب مُصدَر وغير مدفوع = مصروف 3,000 بلا خصم مقابل → الميزانية ناقصة.

**الإصلاح:** `salaries:issue` يسجّل الالتزام الآن، و `salaries:pay` يُسدّده بدل إنشائه.
تم التحقق للحالات الثلاث: دفع كامل، دفع جزئي، بلا دفع.

---

## 5. السلفة تختفي بلا مصروف مقابل ⚠️ عالي

**الاكتشاف:** انحراف `−500` عند خصم سلفة من الراتب.

```
الموظف استلم فعلياً : 500 (سلفة) + 2,500 (راتب) = 3,000
المصروف المسجَّل    : 2,500 فقط
```

`NetSalary` صافٍ بعد السلفة، لكن السلفة كانت **أصلاً** في الميزانية. عند خصمها يختفي
الأصل بلا مصروف مقابل → **الربح مبالغ فيه بقيمة كل سلفة**.

**الإصلاح:** مصروف الرواتب = الاستحقاق الإجمالي (`NetSalary + AdvancesTotal`).
السلفة تُسوّى عند الإصدار لا عند الدفع.

---

## 6. كشف حساب العميل لا يطابق رصيده ⚠️ عالي

**الاكتشاف:** فارق `350` بين الكشف والرصيد الفعلي.

| المشكلة | الأثر |
|---|---|
| فاتورة الصيانة تُحتسب مرتين (في `sales` وفي `maintenance_deliveries`) | +350 |
| مبيعات الخدمات غائبة تماماً عن الكشف | −105 |
| المرتجع النقدي يُحتسب دائناً رغم أنه رُدّ نقداً | −150 |

**الإصلاح:**
- استبعاد الفواتير `Source='maintenance'` من قسم المبيعات
- إضافة `service_sales` للكشف
- عمودان جديدان `DebtRelief` / `CashRefund` على المرتجعات، والكشف يقيّد **إعفاء الدين فقط**

**النتيجة:** الكشف يطابق `customers.Balance` بالضبط — واختبار تسوية آلي يمنع التراجع.

---

## 7. المرتجع النقدي يقيّد العميل دائناً — عالي

مرتجع على فاتورة **مدفوعة نقداً** كان يظهر كرصيد دائن للعميل رغم أن المال رُدّ في يده.
مغطى الآن بعمودَي `DebtRelief`/`CashRefund` أعلاه.

---

# القسم الثاني: الثغرات الأمنية المكتشفة والمُصلحة

## 1. الاستيلاء على حساب المدير بلا مصادقة 🔴 حرِج

```ts
ipcMain.handle('setup:initialize', ...)   // قناة عامة، بلا مصادقة
  INSERT INTO users ... ON CONFLICT(Username) DO UPDATE SET PasswordHash = excluded.PasswordHash
```

القناة **عامة** (يحتاجها معالج الإعداد الأول) وتُحدِّث كلمة المرور عند التعارض.
أي شخص يصل لـ IPC يستدعيها باسم `admin` فيغيّر كلمة مرور المدير ويستولي على النظام.

**الإصلاح:** ترفض العمل نهائياً بعد اكتمال الإعداد + حد أدنى لطول كلمة المرور.

---

## 2. الكتابة الحرة في الإعدادات بلا مصادقة 🔴 حرِج

`setup:complete` كانت تكتب **أي مفتاح** يُرسَل إليها، بلا مصادقة:

```ts
for (const [key, value] of Object.entries(data)) {   // أي مفتاح!
  INSERT OR REPLACE INTO settings ...
}
```

مهاجم يستطيع تعطيل `allow_negative_stock`، تزوير `owner_capital`، أو تحويل `db_path`.

**الإصلاح:** قائمة بيضاء لمفاتيح بيانات الشركة فقط + إغلاق بعد الإعداد.

---

## 3. تسريب مفاتيح السحابة قبل تسجيل الدخول 🟠 عالي

`settings:getAll` عامة (تحتاجها شاشة الدخول لاسم المحل)، وكانت تُرجع **كل** الجدول
بما فيه `cloud_apikey` و `sync_*`.

**الإصلاح:** استبعاد `cloud_%` و `sync_%` و `db_path`.

---

## 4. ثغرة في الحارس نفسه 🟠 عالي

حقن `userId` الموثوق يعمل على الكائنات فقط. أربعة معالجات تأخذه **كوسيط منفصل**
فكانت تثق بما ترسله الواجهة:

`fiscalYear:close` · `maintenance:updateStatus` · `maintenance:removePart` · `rents:generatePayments`

**الإصلاح:** دالة `getCallerUserId(event)` تقرأ الهوية من الجلسة.

---

## 5. الحذف يترك سجلات يتيمة 🟠 عالي

| العملية | ما يصبح يتيماً |
|---|---|
| `delete:sale` | المرتجعات، تسليمات الصيانة، السندات المرتبطة |
| `delete:purchase` | مرتجعات المشتريات، السندات |
| `delete:maintenanceDelivery` | مرتجعات الصيانة |
| `delete:transfer` | سند عمولة التحويل (يبقى في الأرباح للأبد) |

المرتجع اليتيم يبقى يؤثر في التقارير بينما مصدره اختفى.

**الإصلاح:** الحذف يُرفض برسالة واضحة عند وجود تبعيات (بدل الحذف المتسلسل — أسلم
محاسبياً لأنه يُبقي أثر التدقيق). وحذف التحويل يزيل سند عمولته.

---

## 6. تصفير القاعدة بلا نسخة احتياطية 🟡 متوسط

`settings:resetDatabase` يمسح كل الجداول التشغيلية. أصبح يأخذ نسخة
`before_reset_<timestamp>.db` أولاً، ويفشل إن تعذّر ذلك.

---

# القسم الثالث: ثغرة الترحيل الحرجة

## نمط `CREATE → INSERT → DROP → RENAME` الأعمى 🔴 حرِج

كتلتان كانتا تُعيدان بناء الجداول في **كل إقلاع**:

```ts
db.exec(`CREATE TABLE IF NOT EXISTS sale_details_new (...)`);  // 11 عمود
db.exec(`INSERT INTO sale_details_new SELECT * FROM sale_details`);  // الحي 13 عمود ← يفشل
db.exec(`DROP TABLE sale_details`);
db.exec(`ALTER TABLE sale_details_new RENAME TO sale_details`);
```

**ما أثبته الفحص عملياً:**
- `INSERT ... SELECT *` يفشل فور اكتساب الجدول الحي عموداً جديداً (`Description`, `WarehouseID`)
- الجدول اليتيم `sale_details_new` يبقى في القاعدة **للأبد**، ويظهر في تصدير CSV
- كل إقلاع يحاول ويفشل ويسجّل خطأً

البيانات نجت **بالصدفة** فقط لأن `try` يوقف التسلسل قبل `DROP`. لكن ترتيب أعمدة مختلف
كان سيؤدي إلى نجاح `INSERT` جزئياً ثم `DROP` للجدول الحقيقي = **فقدان كامل**.

**الإصلاح:**
- إعادة البناء **مشروطة** (فقط عند الحاجة الفعلية عبر فحص `PRAGMA table_info`)
- النسخ **بأسماء الأعمدة** لا `SELECT *` — عمود إضافي لا يفسد شيئاً
- كل العملية في **معاملة واحدة** — استحالة الحذف بعد نسخ جزئي
- تنظيف أي جدول يتيم من محاولات سابقة

**التحقق:** 14 فحص، منها ترقية قاعدة قديمة بـ 25 سجل (نجت كلها) وتشغيل الترحيل 3 مرات.

---

# القسم الرابع: أخطاء الأنواع

## `global.d.ts` معطوب — 221 خطأ

```ts
import type { ElectronAPI } from '../preload/index';   // ← المسار غير موجود
```

الملف يستورد من مسار غير موجود، فكان `window.api` من نوع `any` ضمنياً — أي **لا فحص
أنواع إطلاقاً** على كل استدعاءات IPC في الواجهة.

بالإضافة: 27 ملف يستخدم `React.ChangeEvent` دون استيراد React (بسبب JSX التلقائي).

**الإصلاح:** تعريف بنيوي لـ `window.api` + تصدير عام لفضاء أسماء React.
**النتيجة: 221 خطأ → 0.**

---

# القسم الخامس: تحسينات إضافية

- **انتهاء الجلسة:** معترض عام في `main.tsx` يسجّل الخروج تلقائياً عند `UNAUTHENTICATED`
  بدل ترك المستخدم في صفحة كل طلباتها تفشل.
- **توقيت الإشعارات:** كان يُضيف `Z` لنص UTC أصلاً فينتج `Invalid Date` ويُسقط التأجيلات.
- **`rents.PropertyName`** عمود غير موجود (الصحيح `RentName`) — كان يُعطّل كشف الخزنة.
- **`maintenance_returns`** كان يُرشَّح بـ `t.Date` وهو alias غير موجود.

---

# ما لم يُنفَّذ — يحتاج قرارك

| البند | لماذا لم أنفّذه |
|---|---|
| **نظام الترخيص** | فيه خلل **وظيفي** لا أمني فقط: `license:activate` يقرأ الأكواد من ملف على **جهاز العميل**، بينما `generateCode` يكتبها على **جهازك**. الكود الذي تولّده لن يوجد أصلاً عند العميل. الحل الصحيح توقيع Ed25519 (مفتاح خاص لا يُشحن) — لكنه يغيّر سير عملك كلياً. أخبرني كيف تسلّم الأكواد وأصممه. |
| **تشفير `DevicePassword`** | كلمات مرور أجهزة العملاء نص صريح. الحل الأنظف SQLCipher للقاعدة كاملة — تغيير بنيوي يحتاج موافقتك. |
| **دفتر قيود** | التوصية الجذرية: اشتقاق الأرصدة من `ledger_entries` بدل تحديثها يدوياً. الأخطاء السبعة أعلاه كلها عرض لنفس السبب — البنية تسمح بها. أصلحت كل حالة وجدتها، لكن البنية تبقى عرضة لمثيلاتها. |
| **رفع النسخ للسحابة** | يرفع القاعدة كاملة لأي URL بلا تحقق. يحتاج قراراً حول مزوّد محدد. |

---

## ملاحظة ختامية

الأخطاء السبعة المحاسبية لم تكن ظاهرة بقراءة الكود — كل واحدة تبدو منطقية في موضعها.
ظهرت فقط عند موازنة النظام ككل بعد كل عملية. لهذا بنيت المحرك: **أي تعديل مستقبلي
يكسر التوازن سيُكتشف فوراً** بتشغيل `npm run verify`.

البيانات الحالية تجريبية كما ذكرت، فلا حاجة لتصحيح أثر رجعي.
