# 🔒 تقرير فحص أمني — ٢٣ نقطة

**التاريخ:** ٣١ يوليو ٢٠٢٦
**المنهج:** فحص الكود + **إثبات عملي بالتنفيذ** لكل ثغرة، لا تخمين.

---

## ⚠️ الخلاصة التنفيذية أولاً

فحصت النقاط الـ٢٣. النتيجة **صادمة في جزء منها**:

| التقييم | العدد | النقاط |
|---|---|---|
| 🔴 **ثغرة حرجة مؤكدة** | ٤ | ١، ٢، ٣، ٤، ٩ |
| 🟠 **ضعف حقيقي** | ٥ | ٨، ١٠، ١٢، ١٣، ١٤ |
| ✅ **آمن** | ٤ | ٥، ٦، ٧، ١١ |
| ❌ **غير موجود / ناقص** | ٧ | ١٥، ١٧، ١٨، ١٩، ٢٠، ٢٢، ٢٣ |
| 🟡 **موجود جزئياً** | ٣ | ١٦، ٢١، ٢٣ |

> ### 🔴 الأخطر على الإطلاق
> **نظام الترخيص مكسور بالكامل.** أي شخص يملك نسخة من برنامجك يستطيع أن يصنع
> لنفسه **ترخيصاً مدى الحياة في ثوانٍ**. أثبتُّ ذلك عملياً بالأسفل.

---

## 🔴 [١][٢][٤] التلاعب بالترخيص وتوليد أكواد — **مكسور تماماً**

### السبب الجذري
مفتاح **التوقيع** ومفتاح **التحقق** هما **نفس القيمة**:

```ts
// src/main/security/licenseCrypto.ts — مشحون داخل التطبيق!
export const VERIFIER_SECRET = 'w/Y8yrd9F9WSt1OMZWdM9Hx88g0tj1c6XRQEQbt+DI0=';
```

و`signCode()` و`verifyCode()` كلاهما يستخدم نفس المفتاح. هذا يعني:
**من يستطيع التحقق يستطيع التوقيع.**

### الإثبات العملي (نفّذته فعلاً)
استخرجت المفتاح من الكود وولّدت ترخيصاً أبدياً:

```
كود مزوّر مدى الحياة: 0000-YGHZ-WVHH-KKT4
هل يقبله التطبيق؟   : true   |   expiryDays = 0 (غير محدود)
```

**استغرق الأمر ثوانٍ.** والملف نفسه يعترف بالمشكلة في تعليقه:
> «The signing key must therefore NEVER be the value shipped to customers»

لكن `scripts/license-keygen.js --init` يطبع **نفس المفتاح** ليُلصق في التطبيق.
أي أن التصميم الصحيح مكتوب في التعليق، **ولم يُنفَّذ**.

### هل يحتاج ذكاء اصطناعي؟ (سؤالك ٤)
**لا يحتاج أصلاً.** لا حاجة لتحليل أكواد قديمة ولا لأي ذكاء اصطناعي — المفتاح
مكتوب حرفياً في الملف. أي شخص يفك ضغط `app.asar` يجده في دقيقة.

### ✅ الحل الوحيد الجذري: **تشفير غير متماثل**
- مفتاح **خاص** عندك فقط (لا يُشحن أبداً) يُوقّع الأكواد — Ed25519
- مفتاح **عام** داخل التطبيق يتحقق فقط، **ولا يستطيع التوقيع**

هذا يُنهي المشكلة نهائياً. **الحجم:** توقيع Ed25519 = ٦٤ بايت، أي كود أطول
(~٢٦ حرفاً بدل ١٦) — مقبول جداً مقابل إغلاق الثغرة.

---

## 🔴 [٩] إعادة الفترة التجريبية بلا حدود — **ثغرة مؤكدة**

### الإثبات العملي
```
أول تشغيل                : NEW 7-DAY TRIAL STARTED
حذف trial.dat فقط         : trial_expired (blocked)   ← الحماية تعمل هنا ✅
حذف مجلد userData كاملاً  : NEW 7-DAY TRIAL STARTED   ← 🔴 الثغرة
```

الكود يكتشف حذف `trial.dat` وحده (عبر `lastaccess.dat`)، لكن **كل ملفات الحالة
في نفس المجلد**. حذف المجلد كله يمحو الشاهد والمشهود عليه معاً.

**والأسوأ:** قاعدة البيانات `mobile_shop.db` في نفس المجلد — فالعميل ينسخها،
يحذف المجلد، يعيدها، ويحصل على **٧ أيام جديدة ببياناته كاملة**. بلا نهاية.

### الحل
مرساة خارج `userData` لا تُحذف مع البرنامج:
- سجل ويندوز `HKCU\Software\...` (يبقى بعد إزالة البرنامج)
- ملف مخفي في `ProgramData`
- **الأقوى:** تسجيل بصمة الجهاز على خادمك عند أول تشغيل — الخادم يرفض تجربة ثانية

---

## 🔴🟠 [٣][١٣] تعديل الملفات

| السؤال | الإجابة |
|---|---|
| هل يمكن حذف ملفات الترخيص والبدء من جديد؟ | 🔴 **نعم** (النقطة ٩) |
| هل يمكن التعديل عليها بذكاء اصطناعي؟ | 🔴 **نعم** — `SECRET_KEY` للتشفير مشحون أيضاً في الكود |
| هل يمكن تعديل ملفات البرنامج؟ | 🟠 **نعم** — `asar` ليس تشفيراً، يُفك بأمر واحد: `npx asar extract` |

> **حقيقة يجب قبولها:** أي كود يعمل على جهاز العميل يمكن تعديله. الحماية
> الحقيقية ليست إخفاء الكود، بل **ألا يحتوي على أسرار قابلة للاستغلال** — وهذا
> بالضبط ما يفعله التشفير غير المتماثل.

---

## ✅ [٥] بوت المطور والمفاتيح — **آمن جزئياً**

| السؤال | الإجابة | الدليل |
|---|---|---|
| هل يصل العميل لبوت المطور؟ | ✅ **لا** | `TG_BOT_TOKEN` غير موجود في `src/` إطلاقاً — فقط في أسرار Cloudflare |
| هل يعدّل عليه؟ | ✅ **لا** | يحتاج `ADMIN_KEY` وهو غير مشحون |
| هل يصل لمفاتيح في النسخة؟ | 🔴 **نعم** | `VERIFIER_SECRET`, `SECRET_KEY`, `CLIENT_KEY`, `DEV_PASSWORD_HASH` كلها مشحونة |
| لو وصل للريبو؟ | 🟠 **نعم يستطيع** | الكود كامل + المفاتيح. الريبو خاص لكن **لا تضع مفاتيح فيه أبداً** |

### 🔴 تنبيه عاجل منفصل
`DEV_PASSWORD_HASH` مشحون، وكلمة مرور المطور **٦ أرقام فقط**. كسرها بالقوة
الغاشمة **يستغرق ثوانٍ** على أي حاسوب. غيّرها لكلمة طويلة.

---

## ✅ [٦][٧] عزل العملاء — **آمن**

| السؤال | الإجابة |
|---|---|
| هل يصل عميل لعميل آخر؟ | ✅ **لا** |
| هل يتأثر عميل بجهاز عميل آخر؟ | ✅ **لا** |

**الأدلة:**
- `/devices` و`/config` و`/message` كلها تتطلب `ADMIN_KEY` (غير مشحون) ✅
- `heartbeat` يستخدم `CLIENT_KEY` لكنه **مقيَّد بالجهاز**: كل استعلاماته
  `WHERE device_id = ?` — لا يُرجع بيانات أجهزة أخرى ✅
- قاعدة بيانات كل عميل **محلية تماماً** على جهازه ✅

**الاستثناء الوحيد:** لو سُرّب `ADMIN_KEY` منك أنت، ينكشف كل شيء.

---

## 🟠 [٨][١٠] الجهاز ونقل الترخيص

### الإثبات العملي لبصمة الجهاز
```
الأساس              : e38d56fc93009047d35ed59fb38d2daf
إعادة تثبيت ويندوز  : SAME (لو بقي اسم الجهاز)
تغيير اسم الجهاز    : CHANGED ❗
تركيب USB WiFi      : CHANGED ❗
```

### إجابة سؤالك ١٠ بدقة
**لا.** البصمة = `MAC + موديل المعالج + اسم الجهاز`. و:
- **MAC** يمكن تغييره من إدارة الأجهزة في ثوانٍ (أو بتركيب كرت شبكة USB)
- **اسم الجهاز** يُغيَّر من الإعدادات في ثوانٍ

فهي **ليست ثابتة**، والأسوأ أنها تتغير **بالصدفة** — عميل يركّب USB WiFi يفقد
ترخيصه ويتصل بك غاضباً. **هذا خطر تجاري أكبر من الخطر الأمني.**

**الأفضل:** الرقم التسلسلي للوحة الأم أو القرص الصلب (`wmic baseboard get serialnumber`).

### [٨] نقل الترخيص لجهاز آخر
✅ **لا يمكن** — الكود مربوط بالبصمة داخل HMAC، وينهار على جهاز آخر.
🔴 **لكن** بالثغرة ١ يصنع ترخيصاً جديداً لأي جهاز، فالحماية بلا قيمة عملياً.

---

## ✅ [١١] المدة التجريبية
`const TRIAL_DAYS = 7;` — **نعم، ٧ أيام بالضبط.**

---

## 🟠 [١٢] بيانات التسجيل — **ناقصة جداً**

### الموجود حالياً
اسم المحل، اسم صاحب المحل، الهاتف، الإيميل، العنوان، الرقم الضريبي، اسم العميل.

### ❌ الناقص من طلبك
| المطلوب | الحالة |
|---|---|
| السن / تاريخ الميلاد | ❌ غير موجود |
| المحافظة / المدينة (منفصلة) | ❌ غير موجود |
| منع الإيميل الوهمي/المؤقت | ❌ **لا يوجد أي تحقق** |
| تسجيل IP تلقائياً | ❌ غير موجود |
| بيانات الجهاز تلقائياً | 🟡 `deviceId` فقط |
| **إجبار بيانات حقيقية** | ❌ **حقل واحد فقط `required`** (اسم المحل) |

**الخلاصة:** يستطيع العميل كتابة `أ` في كل الحقول و`a@a.a` كإيميل ويكمل.

---

## 🟠 [١٤] تصدير البيانات بعد انتهاء الاشتراك

✅ **التصدير موجود** (CSV لكل جدول أو الكل دفعة واحدة).
🟠 **لكن** يتطلب صلاحية `settings.edit` **وتسجيل دخول**، وعند انتهاء الترخيص
تُحجب الشاشات.

**رأيي المحاسبي:** يجب **دائماً** السماح بتصدير البيانات حتى بعد الانتهاء.
بيانات المحل **ملكه هو** لا ملكك. حجزها رهينةً يضر بسمعتك ويعرّضك قانونياً.
أوصي بإتاحة زر «تصدير بياناتي» يعمل حتى في وضع `expired`.

---

## ❌ [١٥] تأكيد الإيميل والهاتف — **غير موجود**

| الطريقة | الإجابة |
|---|---|
| تأكيد الإيميل بـ Firebase | ✅ ممكن ومجاني (١٠٬٠٠٠ شهرياً). لكن **Cloudflare أبسط عندك**: أرسل رمزاً بـ MailChannels مجاناً من الـWorker |
| تأكيد الهاتف بتليجرام | 🟡 **ممكن لكن ليس تأكيداً حقيقياً للرقم** |

### ⚠️ توضيح مهم جداً
تليجرام **لا يعطي البوت رقم الهاتف** إلا إذا ضغط المستخدم زر
`request_contact` بنفسه. عندها **نعم، تحصل على الرقم موثقاً من تليجرام مجاناً**
— وهذه أسهل وأرخص طريقة متاحة لك فعلاً.

**لكن:** تثبت أنه يملك حساب تليجرام بهذا الرقم، لا أنه يملك الشريحة الآن.
للتأكيد الحقيقي تحتاج SMS مدفوعاً.

---

## 🟡 [١٦] شعار المحل — **موجود جزئياً**
✅ يُطبع في الفاتورة (`logo_path` مع فلترة أمنية للمصدر).
❌ **لا يوجد حقل في الإعدادات لرفعه** — القيمة موجودة في قاعدة البيانات بلا واجهة.
❌ غير مستخدم في كشف الحساب والتقارير.

---

## ❌ [١٧] «طُبع بواسطة» — **غير موجود إطلاقاً**
لا يوجد أي أثر لاسم المستخدم الطابع في أي مخرج مطبوع.

**ملاحظة محاسبية:** هذه ليست رفاهية — معرفة من طبع كشف حساب أو فاتورة **جزء من
المسار التدقيقي**، وضرورية عند التحقيق في نزاع.

---

## ❌ [١٨] استخدامات إضافية لبوت المطور
غير مُنفَّذة. المتاح حالياً: إنشاء أكواد، عرض الأجهزة، الرسائل، الإعدادات،
الإحصائيات، وإشعار التصفير.

**أفكار عملية:** تنبيه انتهاء الاشتراكات قبل ٧ أيام، تقرير أسبوعي بالأجهزة
النشطة، تنبيه عند اكتشاف تلاعب، إحصاء الإصدارات المستخدمة.

---

## ❌ [١٩][٢٠] إعدادات الطباعة والفاتورة — **بدائية جداً**

`PrintSettings.tsx` = **١٢٨ سطراً** فيه معاينة وطباعة فقط، **بلا أي إعداد حقيقي**.

### الناقص للوصول لمستوى البرامج العالمية
| الفئة | العناصر |
|---|---|
| **الورق** | A4 / A5 / حراري ٨٠مم / ٥٨مم، الهوامش، الاتجاه |
| **الهوية** | الشعار وحجمه وموضعه، الألوان، الخطوط |
| **الترويسة/التذييل** | نص حر، شروط البيع، QR، أرقام التواصل |
| **الأعمدة** | إظهار/إخفاء/ترتيب/تسمية كل عمود |
| **المحتوى** | إظهار الضريبة، الخصم، الرصيد السابق، التوقيع |
| **متقدم** | قالب لكل نوع مستند، نسخ متعددة، «نسخة العميل/المحل» |

---

## 🟡 [٢١] تغيير اسم البرنامج عن بُعد — **موجود ويعمل**
✅ `app_name` و`app_edition` ضمن `REMOTE_MANAGED_KEYS`.
✅ يعمل لجهاز واحد (`target = deviceId`) أو للجميع (`target = 'all'`).
🟡 يُطبَّق عند المزامنة التالية (كل ٢٤ ساعة) لا فوراً.

---

## ❌ [٢٢] بيانات التسجيل في بوت المطور — **لا تصل**
`heartbeat` يرسل فقط: `deviceId`, `shopName`, `appVersion`, `platform`,
`licenseStatus`, `licenseExpiry`.

❌ **لا يُرسل:** اسم المالك، الهاتف، الإيميل، العنوان.

> ⚠️ **تنبيه قانوني:** إرسال بيانات شخصية للمالك يتطلب **موافقته الصريحة**.
> صرّح بذلك في معالج الإعداد صراحةً.

---

## 🟡❌ [٢٣] نقل قاعدة البيانات والمزامنة السحابية

| السؤال | الإجابة |
|---|---|
| نقل القاعدة لجهاز آخر؟ | ✅ **نعم** — عبر النسخة الاحتياطية أو تغيير المسار |
| العمل من المحل والبيت؟ | 🟡 **نعم عبر مجلد شبكة مشترك** (`db:createNetwork`) |
| هل السحابة تعمل كمزامنة؟ | 🔴 **لا! وهذا خطر حقيقي** |
| تضارب بين الأجهزة؟ | 🟠 **نعم محتمل** |

### 🔴 تحذير خطير: «الرفع السحابي» **ليس مزامنة**

فحصت الكود: يوجد `db:uploadToCloud` **فقط**. **لا يوجد أي كود للتنزيل أو الدمج
من السحابة إطلاقاً.**

**أي أنها نسخة احتياطية باتجاه واحد**، والاسم «مزامنة» **مُضلِّل وخطير**:
عميل يظن أن جهازيه متزامنان، فيعمل على الاثنين، ثم يكتشف أن كل جهاز له بيانات
مختلفة، وأن الرفع الأخير **داس** على الآخر.

### التضارب على مجلد الشبكة
SQLite عبر SMB **غير موصى به رسمياً** — قفل الملفات على الشبكة غير موثوق،
وWAL **لا يعمل** على مشاركات الشبكة. المخاطر: تلف القاعدة، أو خطأ
`database is locked` عند عمل جهازين معاً.

**الحل الصحيح للعمل متعدد الأجهزة:** خادم مركزي حقيقي (Postgres/MySQL) أو
تصميم مزامنة فعلي بسجل تغييرات — مشروع كبير منفصل.

---

## 📋 أولويات العمل — ترتيبي المهني

| # | المهمة | الخطورة |
|---|---|---|
| ١ | **تشفير غير متماثل للترخيص (Ed25519)** | 🔴 عاجل |
| ٢ | **إغلاق ثغرة إعادة التجربة** | 🔴 عاجل |
| ٣ | **تغيير كلمة مرور المطور** (٦ أرقام = ثوانٍ) | 🔴 عاجل |
| ٤ | **إزالة تسمية «مزامنة» أو تنبيه صريح** | 🟠 مهم |
| ٥ | بصمة جهاز مستقرة (لوحة أم/قرص) | 🟠 مهم |
| ٦ | إتاحة التصدير بعد انتهاء الاشتراك | 🟠 مهم |
| ٧ | إعدادات طباعة وفاتورة كاملة | 🟡 |
| ٨ | «طُبع بواسطة» + شعار في الإعدادات | 🟡 |
| ٩ | بيانات تسجيل كاملة + تحقق | 🟡 |

---

## 🔻 رأيي الصريح

**نقاط قوة حقيقية:** عزل العملاء ممتاز، بوت المطور محمي، الحماية من إرجاع
الساعة ذكية، والبنية نظيفة ومختبَرة (٤٨ مجموعة فحص).

**لكن نظام الترخيص لا يحمي شيئاً اليوم.** أي عميل تقني واحد يستطيع كسره في
دقائق ونشر الطريقة. النقطة ١ ليست تحسيناً — **هي شرط لبيع البرنامج أصلاً**.

**أوصي بالبدء بالنقاط ١ و٢ و٣ فوراً قبل أي ميزة جديدة.** أخبرني وأبدأ.
