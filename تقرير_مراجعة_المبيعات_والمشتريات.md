# مراجعة عميقة: المبيعات والمشتريات والمرتجعات

**الحالة:** ✅ كل التعديلات مدفوعة إلى `master`
**آخر commit:** `f3a8cb8`

---

## الطرق الخمس المستخدمة

راجعت هذه الأقسام بخمس طرق **لم تُستخدم من قبل**، لأن تكرار نفس الطريقة يجد نفس الأخطاء فقط:

| # | الطريقة | ماذا وجدت |
|---|---|---|
| ١ | **مدخلات عدائية** — إرسال بيانات مشوّهة كما يفعل واجهة متلاعَب بها | ✅ ٤٧ فحصاً نجحت بلا تعديل |
| ٢ | **التزامن** — عملية ثانية تقع داخل نافذة العملية الأولى | 🔴 **٥ ثغرات خطيرة** |
| ٣ | **اتفاق التقارير** — نفس المستند في ٦ تقارير | ✅ ٣٠ فحصاً متطابقة |
| ٤ | **نموذج قيد مزدوج مستقل** — ترحيل المستندات كما يفعل محاسب | ✅ مطابق تماماً |
| ٥ | **قراءة سطر بسطر** للمسارات التي لا يصلها الاختبار العشوائي | 🔴 **٣ أخطاء في الخصم** |

---

## أولاً: ثغرات التزامن 🔴

كل معالج يتحقق **قبل** فتح المعاملة:

```
... تحقق من المخزون / من الخزنة ...   ← قراءة بلا قفل
const tx = db.transaction(() => {...})  ← كتابة
```

بين القراءة والكتابة لا يوجد قفل. **وهذا ليس افتراضياً:** البرنامج يوفّر `db:createNetwork` — قاعدة بيانات مشتركة على الشبكة، أي أن كل نقطة بيع عملية منفصلة على نفس الملف.

أثبتُّ ذلك بتنفيذ كتابة منافِسة داخل النافذة:

| العملية | النتيجة قبل الإصلاح |
|---|---|
| `sales:create` | باع ٥ ورفّ فارغ → مخزون **−٥** |
| `purchases:create` | دفع ٩٠٠ من خزنة فارغة → نقدية **−٩٠٠** |
| `purchaseReturns:create` | ردّ ٥ قطع غير موجودة → مخزون **−٥** |
| `delete:purchase` | ألغى استلام بضاعة مباعة → مخزون **−٥** |
| `delete:saleReturn` | استرجع بضاعة أُعيد بيعها → مخزون **−٢** |

الخمسة الآن يُعيدون التحقق **داخل** المعاملة، حيث يمنع SQLite أي كاتب آخر.

> اكتشفت أيضاً أن `purchaseReturns:create` بلا `try/catch` إطلاقاً — أي رفض كان سيظهر كانهيار بدل رسالة عربية.

---

## ثانياً: أخطاء الخصم 🔴

الخصم يُسجَّل في **رأس الفاتورة**، لكن المرتجع كان يُقيَّم بسعر **السطر** (قبل الخصم):

```
١٠ كابل × ١٠٠ = ١٠٠٠ ، خصم ٢٠٠  →  العميل دفع ٨٠٠  (٨٠ للوحدة)
أرجع ٨ فقط                        →  استُرد ٨ × ١٠٠ = ٨٠٠
```

**العميل استرد الفاتورة كاملة واحتفظ بكابلين مجاناً.** الحارس القديم لم يمنعها لأن ٨٠٠ لا تتجاوز ٨٠٠ — يكشفها المرتجع الكامل فقط، وهو ما لا يجرّبه أحد.

ونفس الخطأ في المشتريات (أسقط دين المورد بالكامل مع بقاء بضاعة)، **وخطأ ثالث**: خصم الشراء لم يكن يُخفّض قيمة المخزون — شراء بـ٨٠٠ كان يُدخل بضاعة بـ١٠٠٠، أي **اختراع ٢٠٠ من قيمة المخزون لحظة الشراء**.

---

## ثالثاً: قرش عالق (وجده الاختبار العشوائي بعد إصلاحي)

فاتورة ٥٥.٠٠ على ٣ وحدات = ١٨.٣٣٣٣ للوحدة → مخزّنة ١٨.٣٣، فإرجاع الثلاثة يعطي ٥٤.٩٩ وتبقى الفاتورة مدينة بـ**٠.٠١ للأبد** — دين لا يُدفع ولا يُسدَّد. الآن آخر مرتجع يصفّي الباقي.

---

## رابعاً: طريقة جديدة — نموذج القيد المزدوج المستقل

`verify_trade_ledger_model.mjs` يرحّل كل **مستند** كقيد مزدوج — كما يفعل محاسب أمام كومة أوراق — ثم يقارن بالأرصدة التي يحدّثها البرنامج تدريجياً.

هذا **ليس** الفخ القديم (إعادة كتابة حسابات المعالج واختبار النسخة): الطرفان مستقلان فعلاً — أحدهما رصيد جارٍ يُحدَّث كل عملية، والآخر ترحيل جديد للأوراق النهائية. وانحراف الأرصدة الجارية عن المستندات هو **سبب معظم أخطاء هذا المشروع**.

النتيجة: ٢٥ بذرة × ٣٠٠ عملية — الخزنة والمحفظة وكل عميل ومورد وصنف **مطابق تماماً**.

---

## التحقق النهائي

| البند | النتيجة |
|---|---|
| مجموعات الفحص | **٢٧ خضراء** |
| اختبارات الانحدار | **٥٢ نجحت** |
| الاختبار العشوائي | ٦٠ بذرة × ٤٠٠ عملية — **٠ مخالفة** |
| النموذج التفاضلي | ٢٥ بذرة — **٠ مخالفة** |

---

## ملاحظات صريحة

- استبدلت **٣ اختبارات هشّة** كانت تطابق نص الكود حرفياً وتفشل عند أي إصلاح صحيح.
- **البيانات القديمة:** الفواتير المسجَّلة بخصم قبل هذا الإصلاح تحمل قيمة مخزون مضخّمة. حضرتك ذكرت أنها تجريبية؛ إن احتجت تصحيحها أكتب سكربت تصحيح.
- **لم تُفحص بهذا العمق بعد:** الصيانة، المرتبات، الجرد، النسخ الاحتياطي والاستعادة.
