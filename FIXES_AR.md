# تقرير الإصلاحات المنفَّذة — Mobile Shop ERP

**التاريخ:** 2026-07-27
**المرجع:** `SECURITY_AUDIT_AR.md`
**التحقق:** `npm run verify` — 34 اختبار محاسبي + 44 اختبار أمني + تغطية 205 قناة IPC

---

## ملخص

| المجال | الحالة |
|---|---|
| الثغرات الحرجة (4) | ✅ مُصلحة |
| الثغرات العالية (5) | ✅ مُصلحة |
| أخطاء محاسبية حرجة (5) | ✅ مُصلحة |
| أخطاء محاسبية عالية (4) | ✅ مُصلحة |
| أخطاء برمجية حرجة (3) | ✅ مُصلحة |
| أخطاء برمجية عالية (4) | ✅ مُصلحة |
| فحص الأنواع (TypeScript) | ✅ 0 أخطاء (main + renderer) |

---

# أولاً: الإصلاحات الأمنية

## 1. طبقة صلاحيات مركزية على كل قنوات IPC
**ملف جديد:** `src/main/security/ipcGuard.ts`

بدلاً من تعديل 205 معالج يدوياً (وهو ما يترك ثغرات عند إضافة معالج جديد)، تم **تغليف `ipcMain.handle` نفسها**:

```ts
installIpcGuard();  // في index.ts قبل تسجيل أي معالج
```

- **الرفض هو الأصل (deny by default):** أي قناة بلا قاعدة وصول تُرفض وتُسجَّل في اللوج.
- 19 قناة عامة (الترخيص، تسجيل الدخول، الإعداد الأولي)
- 36 قناة تتطلب تسجيل دخول فقط (قوائم منسدلة)
- 150 قناة مربوطة بصلاحية محددة

**اختبار التغطية:** `scripts/verify_ipc_coverage.py` يفشل البناء إذا أضيفت قناة بلا قاعدة.

## 2. جلسات في العملية الرئيسية + إصلاح مسار التدقيق
**ملفات جديدة:** `src/main/security/session.ts`, تعديل `auth.handlers.ts`

- الجلسة مرتبطة بـ `webContents.id` — الواجهة لا تستطيع انتحال مستخدم آخر.
- الصلاحيات الفعلية تُحسب في الخلفية: `صلاحيات الدور + منح فردي − حظر فردي`.
- **الحارس يستبدل `userId` القادم من الواجهة** بالمستخدم الحقيقي من الجلسة.
- أُزيل `userId: 1` من 11 موضعاً في الواجهة.
- انتهاء صلاحية بعد 12 ساعة خمول.

## 3. كلمة مرور المطور: bcrypt + توكن مؤقت
**ملف جديد:** `src/main/security/devAuth.ts`

| قبل | بعد |
|---|---|
| `btoa()` + عكس النص في الواجهة | bcrypt (cost 12) في العملية الرئيسية |
| `sessionStorage.dev_unlocked = 'true'` يفتح الكونسول | توكن عشوائي 32 بايت صالح 30 دقيقة |
| `data.devPassword !== '014253'` في 4 معالجات | `verifyDevToken()` في 4 معالجات |
| محاولات غير محدودة | 5 محاولات ثم قفل 15 دقيقة |

لا يوجد أي نص `014253` في الكود المصدري (يتحقق منه الاختبار الأمني).

**ملاحظة صريحة موثَّقة في الكود:** كلمة مرور ثابتة تُشحن لكل العملاء تبقى قابلة للكسر بالقوة الغاشمة إذا استُخرج الـ hash. الحل الجذري هو توقيع غير متماثل (Ed25519) — موثَّق في تعليقات `devAuth.ts` كخطوة تالية.

## 4. صفحة «نسيت كلمة المرور»
- تستخدم `users:listBasic` (id + اسم فقط) بدل `users:list` الذي يكشف الأدوار.
- التحقق من بيانات المطور يتم في الخلفية عبر `dev:login` ثم يُستخدم التوكن.
- **إعادة تعيين كلمة المرور تُبطل كل جلسات المستخدم** (`destroyAllSessionsForUser`).
- تم تعديل النص ليوضح أن هذه العملية تتطلب تدخل المطور، تماشياً مع ملاحظتك في `CODELY.md`.

## 5. حقن SQL
`statement.handlers.ts` و `reports.handlers.ts` كانا يُدرجان التواريخ نصياً:
```ts
parts.push(`${field} >= '${filters.fromDate}'`);   // قبل
```
تم استبدالها ببنّاء معاملات مرتبطة يعيد `{ sql, vals }` لكل عمود على حدة.

## 6. تصدير CSV
- التحقق من اسم الجدول مقابل `sqlite_master` (لا حقن، لا جداول وهمية).
- **حظر جدول `users`** (هاشات كلمات المرور) و `user_overrides`.
- معالجة حقن صيغ Excel: القيم التي تبدأ بـ `= + - @` تُسبق بعلامة اقتباس.

## 7. XSS في الطباعة
- دوال `esc()` / `num()` / `safeImageSrc()` على **كل** قيمة تدخل الـ HTML.
- `sandbox: true` على نافذتَي المعاينة والطباعة.
- `safeImageSrc` يمنع `javascript:` و `data:text/html` في مسار الشعار.

## 8. إزالة `executeJavaScript`
حُذفت قناة `sidebar:repairConfig` التي كانت تنفّذ JS في الواجهة من العملية الرئيسية.

## 9. تسجيل الدخول
- رسالة خطأ موحّدة (لا تكشف إن كان المستخدم موجوداً).
- مقارنة bcrypt وهمية عند عدم وجود المستخدم لمنع التمييز الزمني.

---

# ثانياً: الإصلاحات المحاسبية

## 1. ازدواج إيراد الصيانة ⚠️ الأخطر
تسليم الصيانة يُنشئ سجلاً في `maintenance_deliveries` **وفاتورة** في `sales`. التقارير كانت تجمع الاثنين.

```sql
-- بعد الإصلاح
WHERE IsVoided = 0 AND IsWarranty = 0 AND COALESCE(Source,'direct') <> 'maintenance'
```
طُبّق في `reports:profitLoss` و `reports:financialPosition` و COGS و كشف حساب الخزنة.
**النتيجة:** تسليم بـ 800 ج.م = إيراد 800 (كان 1600).

## 2. النقدية المزدوجة
```ts
if (data.PaymentMethodID)      { pm   += paid; }
else if (data.CashAccountID)   { cash += paid; }   // else if وليست if منفصلة
```
- طُبّق في `sales:create` و `maintenance:deliver`.
- الفاتورة تخزّن الحساب الذي استلم فعلاً (حتى يكون العكس متماثلاً).
- **الواجهة:** اختيار الماكينة يمسح اختيار الخزنة تلقائياً في صفحتَي المبيعات والصيانة.

## 3. المرتجعات (مبيعات ومشتريات)
المرتجع الآن **يُقسَّم**: يُلغي الدين المتبقي أولاً، والباقي فقط يُردّ نقداً.

| الحالة | إعفاء دين | ردّ نقدي |
|---|---|---|
| فاتورة 300 مدفوعة نقداً | 0 | 300 |
| فاتورة 300 آجلة بالكامل | 300 | 0 |
| فاتورة 500، مدفوع 300 | 200 | 300 |

كما يُرفض المرتجع الذي يتجاوز قيمة الفاتورة (مع احتساب المرتجعات السابقة).

## 4. إيراد الخدمات — مبدأ الوكيل مقابل الأصيل
تحويل 100 ج.م بعمولة 5:
- **قبل:** إيراد 105، تكلفة 103
- **بعد:** إيراد 5، تكلفة 3، ربح 2

`ChargeAmount - Amount` كإيراد، و `ServiceCost + TransferCost` كتكلفة. `servicesGross` متاح للعرض إن أُريد إظهار حجم التعامل.

## 5. ازدواج مصروف الإيجار
أُزيل `PartyType='rent'` من المصروفات العمومية (يُحتسب من `rent_payments` فقط).

## 6. أساس استحقاق الرواتب
كان الترشيح على `PaymentDate` (تبقى NULL حتى الصرف) فتختفي الرواتب المستحقة غير المصروفة. أصبح على `Month` (فترة الاستحقاق).

## 7. عكس تكلفة المرتجع الجزئي
كان الربط على `sale_details.SaleID` فيعكس تكلفة **الفاتورة كلها**. أصبح عبر `sale_return_details` — البنود المرتجعة فقط.

## 8. التسويات تدخل قائمة الدخل
العجز/الزيادة تُسجَّل الآن كسند `general` (عجز = مصروف، زيادة = إيراد) مع `CashAccountID = NULL` حتى لا يتحرك الرصيد مرتين.

## 9. فحص توازن الميزانية
`reports:financialPosition` يُرجع الآن `difference` و `isBalanced` صراحةً بدل إخفاء الفارق بين رقمين متضاربين لحقوق الملكية.

---

# ثالثاً: الأخطاء البرمجية

## 1. `PaymentSourceType` → `PaymentSource`
العمود غير موجود بهذا الاسم — كشف حساب الخزنة كان **يرمي استثناءً دائماً**. (مُتحقق منه عملياً: `no such column`)

## 2. JOIN خاطئ في تقرير المرتجعات
```sql
-- قبل: يربط رقم فاتورة برقم عميل
LEFT JOIN customers c ON r.SaleID = c.CustomerID
-- بعد
JOIN sales s ON r.SaleID = s.SaleID LEFT JOIN customers c ON s.CustomerID = c.CustomerID
```

## 3. أرقام المستندات
**ملف جديد:** `src/main/database/docNumber.ts` + جدول `document_sequences`

عدّاد تصاعدي لكل (جدول، تاريخ) لا يتراجع عند الحذف، مع شبكة أمان تتخطى أي رقم مستخدم. تُبذَر القيم من البيانات الحالية عند الترقية فلا يبدأ الترقيم من جديد.

## 4. `String.replace` يستبدل أول تطابق فقط
`' AND Date >= ? AND Date <= ?'.replace('Date','r.Date')` كان يترك `Date` الثانية غير مؤهَّلة → `ambiguous column`. استُبدل ببنّاء صريح لكل عمود. كذلك `replace(/Date/g,'t.Date')` كان يحوّل `PaymentDate` إلى `Paymentt.Date`.

## 5. المخزون يتجاهل المخزن
**ملف جديد:** `src/main/database/stock.ts` + عمود `WarehouseID` في `sale_details` و `sale_return_details`.

البيع يسجّل المخزن المصدر، والمرتجع/الحذف يعيد البضاعة **لنفس المخزن**.

## 6. ازدواج استرجاع قطع الصيانة
أسطر فاتورة الصيانة تحمل `WarehouseID = NULL` عمداً (هي للطباعة فقط)، ومنطق العكس يتخطاها — القطع تُسترجع من `maintenance_parts` فقط.

## 7. معالجات مسجَّلة خارج دالة التسجيل
`statement:getOperationDetail` و `cashAccount:statement` كانتا تُسجَّلان وقت `import`. نُقلتا إلى `registerStatementHandlers()`.

## 8. النسخ الاحتياطي و WAL
- `db.backup()` بدل `copyFileSync` (قاعدة البيانات في وضع WAL).
- الاستعادة تكتب على **المسار النشط** لا المسار الافتراضي، وتتحقق من ترويسة `SQLite format 3`، وتحفظ نسخة `.before-restore`، وتحذف ملفات `-wal`/`-shm` القديمة.
- التنظيف التلقائي يحذف ملفات `auto_backup_*.db` فقط (كان يحذف أي ملف أقدم من 7 أيام).
- عند الإغلاق: `wal_checkpoint(TRUNCATE)` بدل نسخ احتياطي غير متزامن قد لا يكتمل.

## 9. أخرى
- `maintenance:receive` صار داخل معاملة واحدة (عميل + تذكرة + سجل حالة).
- توقيت الإشعارات المؤجلة: مقارنة UTC متسقة (كان يضيف `Z` لنص UTC أصلاً → `Invalid Date`).
- `operations:log` و `ops.sort` آمنان ضد `Date = NULL`.
- تسوية المخزون تستهدف مخزناً محدداً بدل «أول صف».
- `maintenance_returns` كان يُرشَّح بـ `t.Date` وهو alias غير موجود في الاستعلام.
- `rents.PropertyName` غير موجود — الصحيح `RentName`.

---

# رابعاً: نظافة المستودع
- `.codely/`, `.codely-cli/` (سجلات محادثات), `electron_err.txt`, `electron_out.txt` (تكشف مسارات النظام) أُزيلت من التتبع وأُضيفت لـ `.gitignore`.
- `knex` أُزيلت من الاعتماديات (غير مستخدمة إطلاقاً).
- `npm run verify` لتشغيل كل الاختبارات.

---

# ما لم يُنفَّذ (يحتاج قراراً منك)

| البند | السبب |
|---|---|
| **توقيع تراخيص غير متماثل (Ed25519)** | يغيّر آلية توليد الأكواد لديك. النظام الحالي يقرأ الأكواد من ملف على جهاز العميل، ما يعني أن الكود المولَّد على جهازك لن يوجد أصلاً عند العميل — خلل وظيفي إضافة للأمني. يحتاج نقاشاً حول سير العمل المطلوب. |
| **تشفير `DevicePassword`** | كلمات مرور أجهزة العملاء ما زالت نصاً صريحاً. الحل الأنظف SQLCipher لقاعدة البيانات كاملة، وهو تغيير في البنية. |
| **دفتر قيود (`ledger_entries`)** | التوصية طويلة المدى: اشتقاق الأرصدة من دفتر حركات بدل تحديثها يدوياً في كل معالج. تغيير معماري كبير — الإصلاحات الحالية تعالج الأعراض بدقة لكن البنية تبقى عرضة لأخطاء مماثلة مستقبلاً. |
| **رفع النسخ للسحابة** | يرفع قاعدة البيانات كاملة لأي URL بلا تحقق. يحتاج قراراً حول مزوّد محدد. |
| **اختبارات وحدة و ESLint** | أضفت 3 سكربتات تحقق (Python، بلا اعتماديات). إطار اختبار كامل (Vitest) و إعداد ESLint يحتاجان تثبيت حزم. |

---

## تحذير مهم قبل التشغيل على بيانات حقيقية

الإصلاحات تصحّح **العمليات الجديدة**. البيانات الموجودة التي تأثرت بالأخطاء السابقة (نقدية مزدوجة، أرصدة عملاء خاطئة من المرتجعات، إيرادات مضاعفة) **لن تُصحَّح تلقائياً**.

الخطوات المقترحة:
1. خذ نسخة احتياطية كاملة قبل التحديث.
2. راجع أرصدة الخزن والماكينات مقابل الجرد الفعلي، واستخدم شاشة التسويات (التي أصبحت تسجّل الفروق في قائمة الدخل).
3. راجع أرصدة العملاء الذين لديهم مرتجعات سابقة.
4. قارن صافي الربح قبل/بعد — الانخفاض متوقع وهو **التصحيح** وليس خطأ.
