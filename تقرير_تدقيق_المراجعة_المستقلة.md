# تدقيق المراجعة المستقلة — ما صحّ وما لم يصحّ

**التاريخ:** 2026-07-30
**المنهج:** تحققتُ من **كل** بند بنفسي من الكود المصدري الحالي. لم أقبل أي ادعاء دون دليل، ولم أرفض أياً منها دون دليل.

---

## 1. البنود التي **أثبتُّ خطأها** (لا تحتاج عملاً)

| البند | الادعاء | الواقع من الكود |
|---|---|---|
| **الترخيص لا يعمل بين الأجهزة** — أولوية 1 | «الكود يُكتب في `activation_codes.dat` على جهاز المطور فلن يوجد عند العميل» | ❌ **مُصلَح مسبقاً**. `license:activate` لم يعد يبحث في أي ملف: `verifyCode(VERIFIER_SECRET, deviceId, raw)` يتحقق من توقيع HMAC **مضمَّن داخل الكود نفسه** ومربوط بمعرّف الجهاز. الكود يحمل حمولته (المدة + الرقم التسلسلي). يوجد تعليق صريح في `license.handlers.ts:451` يشرح هذا الإصلاح بالضبط |
| **لا توجد اختبارات آلية** — أولوية 2 | «لا ملفات `*.test.ts`» | ❌ **مُضلِّل**. البحث عن `*.test.ts` فقط أغفل **32 ملف فحص** في `scripts/` تعمل عبر `npm run verify` — تشمل اختبار طفرات، fuzz بـ 15,600 عملية، وفحوص تكافؤ محاسبي |
| **كلمة مرور `admin123`** | «المستخدمون قد ينسون تغييرها» | ⚠️ **مُضلِّل جزئياً**. معالج `setup:initialize` **يفرض** كلمة مرور ≥ 6 أحرف ويستبدل الهاش، ويُغلق نهائياً بعد الإعداد. البذرة لا تبقى إلا إذا تخطّى المستخدم المعالج |

**الخلاصة:** البند المُصنَّف **أولوية 1** في تلك المراجعة كان مُصلَحاً بالفعل. هذا بالضبط سبب قاعدة عدم الاعتماد على التقارير القديمة — لكنها تنطبق أيضاً على المراجعات الجديدة.

---

## 2. البنود التي **أثبتُّ صحتها وأصلحتها**

### 🔴 خطير — النسخ الاحتياطي اليومي كان يُنتج ملفاً **غير قابل للقراءة**

هذا ليس تحليلاً نظرياً. شغّلتُ قياساً فعلياً على قاعدة SQLite حقيقية في وضع WAL:

```
live database          : 500 sales, total 1252500
-wal file present      : true (24752 bytes)

fs.copyFileSync  (db:autoBackup) : UNREADABLE — no such table: sales
db.backup()      (index.ts)      : 500 sales, total 1252500

VERDICT
  copyFileSync lost 500 of 500 sales (1252500 in value)
  db.backup()  lost 0 of 500 sales
```

**النتيجة أسوأ مما ورد في المراجعة:** الخسارة ليست «جزئية» — النسخة **فقدت 100% من البيانات وحتى الجداول نفسها**، لأن المخطط ذاته كان لا يزال في ملف `-wal`.

الأخطر: `index.ts` كان **مُصلَحاً** بالفعل بـ `db.backup()`، بينما `database.handlers.ts` — وهو **الذي يستدعيه زر النسخ الاحتياطي في الواجهة** — بقي على `copyFileSync`. أي أن الإصلاح السابق طُبِّق في مكان وتُرك في المكان الذي يستخدمه المستخدم فعلياً.

### 🔴 خطير — `db:createNetwork` كان يُنشئ قاعدة مشتركة تالفة

نفس الخلل، لكن أثره أشد: الملف الناتج يصبح **القاعدة الحيّة** التي تتصل بها كل الأجهزة.

### 🟠 حذف ملفات غير مقصودة

المُنظِّف في `database.handlers.ts` كان يحذف **أي** ملف أقدم من 7 أيام في مجلد النسخ — بما فيها نسخة يدوية احتفظ بها المالك — ويتعطل عند وجود مجلد فرعي. (`index.ts` كان مُصلَحاً؛ هذا توأمه المنسي.)

### 🟠 `db:changePath` يقبل أي ملف

كان يتحقق من وجود الملف فقط. اختيار ملف خاطئ لا يفشل فوراً بل **عند الإقلاع التالي**، فيصبح البرنامج لا يفتح أصلاً. أضفتُ التحقق من بصمة `SQLite format 3` (نفس ما يفعله `backup:restore` بالفعل).

### 🟠 رفع القاعدة كاملة عبر `http`

أضفتُ رفضاً صريحاً لأي رابط غير `https`، مع رسالة عربية واضحة.

---

## 3. اختباري أنا كان خاطئاً — وصحّحتُه

فحصي الأول اتّهم `backup.handlers.ts` بنسخ قاعدة حيّة. قِستُ الأمر قبل تعديل الكود:

```
BEFORE close: -wal exists = true 12392b
AFTER  close: -wal exists = false
rollback copy taken after close: 400 rows -> COMPLETE (safe)
```

`closeDb()` يُنفّذ checkpoint ويحذف الـ `-wal`، فالنسخة **سليمة**. **الاختبار كان خاطئاً وليس الكود** — فصحّحتُ الاختبار ليقيس «هل القاعدة مفتوحة؟» لا مجرد وجود `copyFileSync`. اختبار يُدين كوداً سليماً أسوأ من عدم وجود اختبار.

---

## 4. الإثبات

أضفتُ `scripts/verify_backup_integrity.mjs` — **21 فحصاً**، نصفها **سلوكي فعلي** على قواعد SQLite حقيقية (يُثبت فقدان البيانات ثم يُثبت الإصلاح)، ونصفها بنيوي.

وأضفتُ **4 طفرات** تُعيد كل خلل:

```
RESULT: 22 caught, 0 survived
```

الفحص الكامل: **33 مجموعة خضراء، exit 0**.

---

## 5. بنود صحيحة لم أُنفّذها بعد (وسبب ذلك)

| البند | رأيي |
|---|---|
| `sandbox: false` | ✅ صحيح. لكن `better-sqlite3` وحوارات الملفات قد تتأثر — يحتاج اختباراً يدوياً على Windows لا أستطيع تنفيذه هنا. **لا أغيّر إعداداً أمنياً حسّاساً دون قدرة على إثبات أن البرنامج ما زال يعمل** |
| `DevicePassword` نص صريح | ✅ صحيح. SQLCipher هو الحل الأنظف ويتطلب إعادة بناء التبعيات + هجرة بيانات |
| دفتر قيود مركزي `ledger_entries` | ✅ صحيح معمارياً وأتفق معه، لكنه إعادة هيكلة كبرى تحتاج جولة اختبار مستقلة |
| `catch {}` الصامتة | ✅ صحيح جزئياً — أضفتُ تسجيلاً في مسار النسخ الاحتياطي. الباقي أقل خطورة |

---

## 6. الخلاصة

المراجعة المستقلة كانت **مفيدة**: كشفت خللاً حقيقياً وخطيراً في النسخ الاحتياطي لم يكن مرصوداً. لكن **بندها المُصنَّف أولوية 1 كان خاطئاً**، وبندين آخرين كانا مُضلِّلين.

الفارق الذي أضفتُه: **القياس بدل الاستنتاج**. الفرق بين «قد ينتج نسخة ناقصة» و«فقدت 500 من 500 عملية وأصبح الملف غير قابل للقراءة» هو الفرق بين ملاحظة وإنذار.

| # | ما أُصلح | الحالة |
|---|---|---|
| 1 | النسخ اليومي (WAL) | ✅ مُثبَت بالقياس |
| 2 | قاعدة الشبكة المشتركة | ✅ |
| 3 | حذف ملفات غير مقصودة | ✅ |
| 4 | التحقق من مسار القاعدة | ✅ |
| 5 | رفض الرفع عبر http | ✅ |
| 6 | تسجيل فشل النسخ | ✅ |
