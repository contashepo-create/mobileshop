# تقرير: فحص المكتبات (Dependencies) والثغرات المعروفة

**التاريخ:** 5 أغسطس 2026
**المالك:** Acc. Mohamed Abdou
**النطاق:** `package.json` + `package-lock.json` — **779 حزمة** بكل مستوياتها

---

## ١. الخلاصة

| المؤشر | قبل | بعد |
|---|---|---|
| **إجمالي الثغرات** | **34** | **2** |
| 🔴 حرجة (critical) | 1 | **0** |
| 🟠 عالية (high) | 27 | 2 (غير منطبقة — انظر البند ٥) |
| 🟡 متوسطة | 3 | **0** |
| ⚪ منخفضة | 3 | **0** |
| اختبارات جديدة | — | **59 ✅** |
| طفرات محقونة / مُكتشَفة | — | **29 / 29** ✅ |

> **لا يوجد مشروع Python** — لا `requirements.txt` ولا `pyproject.toml`. ملفات `.py` في `scripts/` هي أدوات فحص تعمل بمكتبات Python القياسية فقط.

---

## ٢. أخطر ما وُجد: البرنامج يعمل على Electron منتهي الدعم 🔴

| | |
|---|---|
| النسخة المستخدمة | **Electron 32.3.3** |
| **انتهاء الدعم** | **٤ مارس ٢٠٢٥** — أي منذ **سنة وخمسة أشهر** |
| عدد التحذيرات الأمنية | **١٧** |
| أخطرها | Use-after-free في نافذة الرسم — **CVSS 8.1** |

Electron يدعم **آخر ثلاث إصدارات رئيسية فقط**. النسخ 32 و38 و39 و40 كلها منتهية الدعم — **ولن تصلها أي ترقيعات أمنية أبداً**.

وهذه ليست مكتبة عادية: **Electron هو البرنامج نفسه الذي يشغّله العميل**، وبداخله متصفح Chromium كامل.

**تم الترقية إلى `43.3.0`** (أحدث خط مدعوم).

---

## ٣. الثغرة الحرجة: `tar` 🔴

| | |
|---|---|
| النسخة | 6.2.1 |
| عدد التحذيرات | **١٢** |
| أخطرها | كتابة ملفات عشوائية عبر hardlink **CVSS 8.2**، وسباق على APFS **CVSS 8.8** |

تصل عبر: `@electron-forge` ← `@electron/rebuild` ← `@electron/node-gyp` ← **`tar`**

تُستخدم وقت تجهيز النسخة (فكّ ملفات Electron المُنزَّلة) — فهي خطر على **جهاز المطور** لا على العميل، لكن اختراق جهاز البناء يعني اختراق كل نسخة تُصدَر بعده.

`npm audit` قال **`fixAvailable: false`** لكل هذه السلسلة. الحل كان `overrides` (البند ٦).

---

## ٤. المكتبة الوحيدة التي تصل للعميل: `react-router-dom` 🟠

من كل الثغرات الـ34، **واحدة فقط** في مكتبة تُشحن داخل البرنامج:

| | |
|---|---|
| النسخة | 6.30.4 |
| الثغرة | Open redirect ← **XSS** (GHSA-jjmj-jmhj-qwj2) |

**واكتشفتُ أن الخط 6.x مهجور تماماً:**

```
آخر إصدار 6.x = 6.30.4  ←  وهو نفسه المُصاب
التحذير يقول: الإصلاح في >= 7.18.0
```

**أي أنه لا يوجد إصلاح داخل 6.x إطلاقاً** — الترقية للإصدار الرئيسي 7 كانت الطريق الوحيد.

---

## ٥. الثغرتان المتبقيتان — ولماذا لا تنطبقان عليك

بعد الترقية بقي تحذيران على `react-router@7.18.2`:

**GHSA-qwww-vcr4-c8h2 — RSC Mode CSRF**

نص التحذير الرسمي حرفياً:

> **"This only affects your application if you are using the unstable RSC APIs"**
> (لا يؤثر على تطبيقك إلا إذا كنت تستخدم واجهات RSC غير المستقرة)

**فحصتُ الكود:** البرنامج يستخدم **الموجّه التصريحي فقط** — لا `createBrowserRouter`، لا `RouterProvider`، لا `loader`/`action`، لا `useFetcher`، لا SSR، لا RSC. **الثغرة لا تلمس هذا البرنامج.**

### واقتراح npm نفسه كان خاطئاً — وأثبتُّه بالقياس

`npm audit` يقترح النزول إلى `7.11.0`. جرّبتُها فعلياً:

| النسخة | عدد التحذيرات | تشمل |
|---|---|---|
| 7.18.2 (اخترتُها) | **1** | RSC فقط — **غير منطبقة** |
| **7.11.0 (اقتراح npm)** | **7** ❌ | **XSS + Open redirect** — **منطبقة فعلاً!** |

ثم `npm audit` على 7.11.0 يقترح… العودة إلى 7.18.2. **نصيحة دائرية.** الاختيار الصحيح هو البقاء على 7.18.2.

**وأضفتُ حارساً في الاختبارات:** لو أدخل أحد يوماً `createHashRouter` أو `RouterProvider`، يسقط الاختبار فوراً — لأن التبرير أعلاه يتوقف عن الصحة عندها.

---

## ٦. كل التغييرات

| المكتبة | من | إلى | السبب |
|---|---|---|---|
| **electron** | 32.3.3 | **43.3.0** | 17 تحذيراً + منتهي الدعم منذ 2025 |
| **react-router-dom** | 6.30.4 | **7.18.2** | XSS — و6.x مهجور |
| **better-sqlite3** | 11.10.0 | **12.11.1** | **إجباري**: Electron 41+ يستخدم Node 24، و`better-sqlite3@11` يعلن `20.x‖22.x‖23.x` فقط |
| **vite** | 5.4.21 | **6.4.3** | تجاوز `server.fs.deny` على ويندوز، CVSS 7.5 |
| **@electron-forge/** * | 7.4.0 | **7.11.2** | أحدث خط 7 |

### وستة `overrides` للمكتبات العميقة

`npm audit` قال «لا يوجد إصلاح» لأن `@electron-forge` يثبّت نسخاً قديمة لا يستطيع npm رفعها وحده:

```json
"overrides": {
  "tar": "^7.5.22",            // 🔴 حرجة
  "brace-expansion": "^2.1.4", // DoS
  "tmp": "^0.2.7",             // path traversal
  "fast-uri": "^3.1.5",        // host confusion
  "cacache": "^19.0.1",        // لا يوجد 18.x مُصلَح
  "make-fetch-happen": "^14.0.3"
}
```

---

## ٧. ⚠️ التغيير الجذري الوحيد الذي يخصّك

**سطر واحد فقط في كل المشروع.**

React Router v7 حذف خاصية `future` — لأن تلك الأعلام أصبحت **السلوك الافتراضي**:

```diff
- <HashRouter future={{ v7_startTransition: true, v7_relativeSplatPath: true }}>
+ <HashRouter>
```

**ولماذا لم يكسر هذا شيئاً؟** لأن العلمين كانا **مفعّلين بالفعل** في الكود — أي أن البرنامج كان يعمل بسلوك v7 منذ البداية. وهذا بالضبط مسار الترقية الذي توصي به الوثائق الرسمية: فعّل الأعلام في v6 ثم ارفع الإصدار.

### وأثبتُّ ذلك بالتشغيل الفعلي، لا بقراءة الوثائق

| ما فحصتُه | النتيجة |
|---|---|
| الرموز السبعة المستخدمة (`HashRouter`, `NavLink`, `Navigate`, `Outlet`, `Route`, `Routes`, `useNavigate`) | ✅ كلها موجودة في v7 |
| رسم شجرة الموجّه الحقيقية تحت v7 | ✅ نجح |
| `Outlet` يرسم الصفحات المتداخلة | ✅ |
| `NavLink` مع `isActive` يميّز الرابط النشط | ✅ `class="ACTIVE-LINK"` |
| روابط `#/` (ضرورية لأن Electron يحمّل من `file://`) | ✅ |
| **بناء الواجهة كاملة** بـ vite 6 + router 7 | ✅ **1605 وحدة** |
| **بناء العملية الرئيسية** | ✅ 61 وحدة |
| فحص الأنواع TypeScript | ✅ نظيف |

---

## ٨. فخّ تجنّبتُه

`npm audit fix --force` أراد **vite 8**. لو نفّذتُها لسحبت `@vitejs/plugin-react@6` الذي يشترط `vite ^8.0.0` — بينما المشروع يثبّت `plugin-react ^4.3.1`.

**جرّبتُها فعلياً:** فشلت بـ `ERESOLVE`.

الاقتران الصحيح الذي **يُثبَّت ويبني فعلاً** هو **vite 6 + plugin-react 4.7.0** (مداه يشمل `^6.0.0`). **أحدث رقم ≠ الرقم الصحيح.**

---

## ٩. خطأ في اختباري كشفه اختبار الطفرات

من 29 طفرة، **واحدة نجت**:

> **D19:** إضعاف `"tar": "^7.5.22"` إلى `"^6.2.1"` — أي إعادة الثغرة الحرجة — **مرّ الاختبار!**

**السبب:** كنتُ أقارن *ملف القفل* بـ *الـ override*، ولم أقارن *الـ override* بـ *الحد الأمني*. وبما أن ملف القفل ما زال يحمل 7.5.22، نجح الفحص. الإضعاف كان سيظهر فقط عند إعادة توليد ملف القفل — وهي اللحظة التي لا ينتبه فيها أحد.

**أُصلح** بإضافة مقارنة مباشرة بين قيمة الـ override والحد الأمني.

**النتيجة النهائية: 29 / 29 ✅**

---

## ١٠. حالة الاختبارات

```
verify_dependency_security.mjs .... 59 / 59 ✅  (جديد)
اختبار الطفرات .................... 29 / 29 ✅
npm audit ......................... 34 ← 2 (غير منطبقتين)
بناء الواجهة ...................... ✅ 1605 وحدة
بناء العملية الرئيسية ............. ✅ 61 وحدة
فحص الأنواع ....................... ✅ نظيف
```

---

## 🔻 الأوامر — واحداً في كل مرة

### ١) افتح المجلد
```powershell
cd "D:\programing\coding projects\mobile shop"
```

### ٢) اسحب التعديلات
```powershell
git pull origin master
```

### ٣) 🔴 **مهم** — امسح المكتبات القديمة
```powershell
rmdir /s /q node_modules
```
> إن رفض، أغلق البرنامج و VS Code ثم أعد المحاولة.

### ٤) ثبّت المكتبات الجديدة (سيستغرق دقائق — Electron كبير)
```powershell
npm install
```

### ٥) تأكد أن الثغرات اختفت
```powershell
npm audit
```
يجب أن ترى **2 high** فقط (وهما ثغرتا RSC غير المنطبقتين).

### ٦) شغّل البرنامج وجرّب
```powershell
npm run dev
```

**جرّب هذه للتأكد من أن الموجّه يعمل:**
- سجّل الدخول ← يجب أن ينقلك للوحة التحكم ✅
- اضغط كل قسم في الشريط الجانبي ← يجب أن ينتقل، والقسم الحالي يظهر مميّزاً ✅
- سجّل الخروج ← يجب أن يعود لشاشة الدخول ✅

### ٧) جرّب بناء النسخة النهائية
```powershell
npm run make
```
> هنا يُعاد بناء `better-sqlite3` للنسخة الجديدة من Electron. لو ظهر خطأ بناء، أخبرني.

---

## ⚠️ ما زال مطلوباً (من تقارير سابقة)

| # | المطلوب | الأمر |
|---|---|---|
| ١ | **أبطل توكن بوت تليجرام** | BotFather ← `/revoke` ← `cd server; npx wrangler secret put TG_BOT_TOKEN` |
| ٢ | **دوّر مفتاح Supabase** | لوحة Supabase ← Settings ← API ← Reset service role key |
| ٣ | انشر الخادم | `cd server; npx wrangler deploy` |
| ٤ | مفاتيح ترخيص جديدة | `npm run license:init` |
| ٥ | معرّفات تليجرام | `npx wrangler secret put TG_ADMIN_CHAT` ← `7232305465,1593943219` |
