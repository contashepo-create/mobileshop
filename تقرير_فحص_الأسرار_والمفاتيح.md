# تقرير: فحص المفاتيح والأسرار في الكود وتاريخ Git

**التاريخ:** 5 أغسطس 2026
**المالك:** Acc. Mohamed Abdou
**النطاق:** كل ملفات المشروع + ملفات الإعدادات + **كامل تاريخ Git (119 كوميت، 856 ملف)**

---

## ١. الخلاصة

| المؤشر | القيمة |
|---|---|
| كوميتات مفحوصة | **119** (كانت 4 فقط ظاهرة — انظر البند ٢) |
| كائنات Git مفحوصة | 1,731 (856 ملف نصي) |
| ثغرة **مؤكدة بالقياس** | **١** 🔴 |
| سر في التاريخ يحتاج إبطالاً | **١** 🟠 |
| ثغرات في `.gitignore` | **١٣** |
| اختبارات جديدة | **89 ✅** |
| طفرات محقونة / مُكتشَفة | **30 / 30** ✅ |

---

## ٢. أول اكتشاف: التاريخ كان مخفياً عني

النسخة التي أعمل عليها كانت **shallow clone** — أي أن Git كان يُظهر **٤ كوميتات فقط** من أصل **١١٩**.

```
.git/shallow موجود  →  115 كوميت غير مرئي
```

**لو فحصتُ دون الانتباه لذلك، لكان التقرير "نظيف" وهو كاذب.** جلبتُ التاريخ الكامل أولاً:

```
git fetch --unshallow origin   →  4 كوميت ← 119 كوميت
```

ثم فحصتُ **كل كائن** في التاريخ (وليس الملفات الحالية فقط) بكاشف أنماط + كاشف عشوائية (entropy).

---

## ٣. 🔴 الثغرة المؤكدة: مفتاح Supabase يُسلَّم للواجهة

### القناة: `db:getCloudSettings` — `src/main/ipc/database.handlers.ts:321`

كانت تُعيد **كل** صفوف `cloud_%` و`sync_%` كما هي إلى الواجهة.

**ما تم قياسه فعلياً بتشغيل المعالج الحقيقي:**

```json
{
  "cloud_api_key": "sbp_LIVE_SUPABASE_SERVICE_ROLE_KEY_abcdef123456",
  "cloud_url": "https://xyz.supabase.co",
  "sync_secret": "SYNC_SHARED_SECRET_9988"
}
```

### لماذا هذا خطير تحديداً

مفتاح `service_role` في Supabase **ليس** مفتاحاً محدود الصلاحية — هو يتجاوز Row Level Security بالكامل، أي **قراءة وكتابة كاملة** على المشروع، بما فيه الحاوية التي تحتوي **كل نسخة احتياطية من دفاتر المحل**.

وكان يُسلَّم للواجهة في كل زيارة لشاشة الإعدادات، فيستقر في:
- ذاكرة React
- قيمة حقل `<input>` في الـ DOM
- أي crash dump للواجهة
- **متناول أي سكربت يعمل في تلك النافذة** — ونوافذ `window.open` ترث الـ preload، أي أن نصاً مزروعاً في اسم عميل على كشف حساب مطبوع يصل إليه

### الدليل على أن هذا خطأ وليس تصميماً

القناة **الشقيقة** في **نفس الملف** تفعل الصواب:

```ts
// telegram:getSettings  (سطر 469) — صحيحة ✅
return {
  hasToken: Boolean(cfg.botToken),
  tokenHint: cfg.botToken ? `${cfg.botToken.split(':')[0]}:***` : '',
};
```

نفس الملف، نفس نوع البيانات السرية، **إجابتان مختلفتان**.

### الإصلاح

| قبل | بعد |
|---|---|
| `cloud_api_key: "sbp_LIVE_..."` | `hasCloudApiKey: true` + `cloud_api_key: "••••••••••••"` |
| المفتاح يُرسَل للواجهة ويعود منها | الواجهة تُرسل القناع؛ **المعالج يقرأ المفتاح من قاعدة البيانات** |
| `db:saveCloudSettings` يكتب **أي** مفتاح | مقصور على `^(cloud|sync)_[a-z0-9_]+$` |

**سلوك مُتحقَّق منه بالقياس:**

| السيناريو | النتيجة |
|---|---|
| فتح الشاشة | ✅ لا يخرج المفتاح إطلاقاً |
| الحفظ دون لمس حقل المفتاح | ✅ المفتاح المحفوظ **يبقى كما هو** |
| كتابة مفتاح جديد | ✅ **يُستبدل** (التدوير ممكن) |
| `db:testCloudConnection` / `db:uploadToCloud` | ✅ يقرآن المفتاح من القاعدة، والقناع لا يصل للشبكة أبداً |
| محاولة كتابة `db_path` أو `telegram_bot_token` عبره | ❌ **مرفوض** |

---

## ٤. 🟠 سر في التاريخ: توكن بوت تليجرام

| | |
|---|---|
| القيمة | `8877684899:AAHTZfkM...` |
| **الكوميتات المصابة** | **١٩** |
| أقدمها | `efd8c80` — 31 يوليو 2026 |
| الملفات | `verify_password_recovery.mjs`, `verify_phone_verify.mjs`, `verify_reset_database.mjs`, `verify_telegram_backup.mjs` |
| في الشجرة الحالية؟ | **لا** ✅ (أُزيل سابقاً) |

> **لا يمكن "إصلاح" سرٍّ نُشر.** إعادة كتابة التاريخ لا تساعد: المستودع مستنسخ ومُخزَّن مؤقتاً على GitHub. **الحل الوحيد هو الإبطال** — انظر الأوامر في النهاية.

---

## ٥. ما فُحص ووُجد **سليماً** ✅

هذه ليست حشواً — كل بند منها فُحص بالفعل وكان يمكن أن يكون ثغرة:

| البند | النتيجة |
|---|---|
| `.env` | **لم يُرفع أبداً** (0 كوميت) ✅ |
| `scripts/.license-key` (المفتاح الخاص للترخيص) | **لم يُرفع أبداً** (0 كوميت) ✅ |
| `scripts/issued-licenses.json` | لم يُرفع أبداً ✅ |
| `server/worker.js` + `wrangler.toml` | كل الأسرار عبر `wrangler secret put` — لا قيمة حرفية واحدة ✅ |
| **حزمة الواجهة (renderer)** | **لا وجود لـ `process.env` ولا `import.meta.env` إطلاقاً** ✅ |
| `vite.renderer.config.ts` | **لا يوجد `define`** → لا شيء يُحقَن في كود العميل ✅ |
| متغيرات `VITE_` | **لا يوجد أي منها** (Vite يكشفها للعميل تلقائياً) ✅ |
| مفتاح الترخيص في الكود | **النصف العام فقط** — يتحقق ولا يستطيع الإصدار ✅ |
| `assertProductionKeys()` | يمنع بناءً مُغلَّفاً على مفاتيح التطوير ✅ |
| `SECRET_KEY` / `SIGN_KEY` | داخل main فقط، **لا تصل للواجهة** ✅ |

---

## ٦. ثغرات `.gitignore` — ١٣ مساراً مكشوفاً

| المسار | لماذا يهم |
|---|---|
| **`server/.dev.vars`** 🔴 | **الأخطر.** ينشئه Wrangler عند أول `wrangler dev` ويحتوي `ADMIN_KEY` و`CLIENT_KEY` و**`LICENSE_PRIVATE_KEY`** و`TG_BOT_TOKEN` بصيغة `key=value`. يقع **داخل `server/`** بجوار ملفات مرفوعة |
| `.env.production` وأخواتها | القائمة القديمة غطّت `.env` و`.env.local` فقط |
| `.npmrc` / `.netrc` / `.git-credentials` | توكنات السجلات |
| `*.pfx` / `*.p12` / `*.pem` / `*.key` | شهادات توقيع الكود |
| `id_rsa` / `id_ed25519` | مفاتيح SSH |

**الإصلاح:** استُبدلت قائمة الأسماء بـ `.env.*` مع استثناء صريح `!.env.example`، وأُضيفت العائلات أعلاه. **تحقَّقتُ أن أي ملف مُتتبَّع حالياً لم يصبح متجاهَلاً.**

---

## ٧. أخطاء في اختباراتي أنا، كشفها اختبار الطفرات

حقنتُ 30 طفرة. أربع منها **نجت** في البداية — أي أن اختباري كان ضعيفاً:

| الطفرة | لماذا نجت | الإصلاح |
|---|---|---|
| **S23** حذف استدعاء `assertProductionKeys()` | الاختبار بحث عن النص `/assertProductionKeys/` — والاسم يظهر **مرتين** (تعريف + استدعاء). حذف الاستدعاء يُبقي التعريف، فمرّ الاختبار. **حارس معرَّف ولا يعمل أبداً** | يُعدّ التكرارات: `>= 2` |
| **S17** مفتاح Stripe حي | كلمة `abcdef` كانت ضمن علامات "هذا مزيّف" — وأي مفتاح عشوائي يحتوي هذه الحروف صدفة | حُذفت `abcdef` و`123456` و`test` من العلامات |
| **S25** إعادة التوكن المحروق | كُتب كـ `'8877...' + '_MPlD...'` فلم يطابقه `includes` | يُزال الدمج قبل الفحص |
| **S10** رجوع الرفع للثقة بالواجهة | لم يكن أي اختبار يستدعي `db:uploadToCloud` أصلاً | اعتراض `fetch` وفحص ترويسة `Authorization` |

**وخطأ إضافي في `.gitignore`:** الطفرة S29 (حذف `!.env.example`) نجت لأن `git check-ignore` يُجيب "غير متجاهَل" لأي ملف **مُتتبَّع بالفعل**، مهما كانت القواعد. الحل: `--no-index` + **قراءة القاعدة المطابِقة** لا كود الخروج (لأن `--no-index` يخرج بـ 0 عند مطابقة النفي أيضاً).

> النتيجة النهائية: **30 / 30 طفرة مُكتشَفة، صفر ناجية.**

---

## ٨. كل ملف وُجد فيه شيء

| الملف | ما وُجد | الحالة |
|---|---|---|
| `src/main/ipc/database.handlers.ts:321` | `cloud_api_key` + `sync_secret` يخرجان للواجهة | 🔴 **أُصلح** |
| `src/main/ipc/database.handlers.ts:331` | `db:saveCloudSettings` يكتب أي مفتاح | 🟠 **أُصلح** |
| `scripts/verify_password_recovery.mjs` | توكن حي (في التاريخ فقط) | 🟠 **يحتاج إبطالاً** |
| `scripts/verify_phone_verify.mjs` | توكن حي (في التاريخ فقط) | 🟠 **يحتاج إبطالاً** |
| `scripts/verify_reset_database.mjs` | توكن حي (في التاريخ فقط) | 🟠 **يحتاج إبطالاً** |
| `scripts/verify_telegram_backup.mjs` | توكن حي (في التاريخ فقط) | 🟠 **يحتاج إبطالاً** |
| `src/main/security/devAuth.ts:45` | bcrypt hash يُشحن في كل نسخة | 🟡 بالتصميم — بدّل العبارة |
| `src/main/security/licenseCrypto.ts:74` | مفتاح **عام** احتياطي | ✅ سليم |
| `src/main/security/deviceId.ts:46` | `SECRET_KEY` ثابت | 🟡 main فقط |
| `src/main/security/trialAnchor.ts:51` | `SIGN_KEY` ثابت | 🟡 main فقط |
| `.gitignore` | 13 مساراً مكشوفاً | ✅ **أُصلح** |

---

## 🔻 الأوامر — انسخها والصقها، أمراً واحداً في كل مرة

### الأمر ١ — افتح المجلد
```powershell
cd "D:\programing\coding projects\mobile shop"
```

### الأمر ٢ — اسحب التعديلات
```powershell
git pull origin master
```

### الأمر ٣ 🔴 **الأهم** — أبطل توكن البوت
افتح تليجرام → ابحث عن **@BotFather** → أرسل:
```
/revoke
```
اختر بوتك، وسيعطيك توكناً جديداً. **انسخه.**

### الأمر ٤ — ضع التوكن الجديد في الخادم
```powershell
cd server
npx wrangler secret put TG_BOT_TOKEN
```
سيطلب اللصق → الصق التوكن الجديد → Enter

### الأمر ٥ — انشر الخادم
```powershell
npx wrangler deploy
```

### الأمر ٦ — ارجع للمجلد الرئيسي
```powershell
cd ..
```

### الأمر ٧ — تأكد أن كل شيء سليم
```powershell
node --experimental-strip-types scripts/verify_no_secrets.mjs
```
يجب أن ترى: `PASSED all 89 checks`

---

## ⚠️ مطلوب أيضاً

| # | المطلوب | الأمر |
|---|---|---|
| ١ | **دوِّر مفتاح Supabase** (كان مكشوفاً للواجهة) — من لوحة Supabase: Settings ← API ← Reset service role key، ثم أدخله في شاشة الإعدادات | من داخل البرنامج |
| ٢ | مفاتيح ترخيص جديدة | `npm run license:init` |
| ٣ | غيّر كلمة مرور المطور لعبارة طويلة | `npm run dev:password -- "أربع كلمات غير مترابطة هنا"` |
| ٤ | معرّفات تليجرام | `cd server; npx wrangler secret put TG_ADMIN_CHAT` ← `7232305465,1593943219` |
| ٥ | لو شغّلت الإعداد باسم غير `admin` | حساب `admin`/`admin123` ما زال حيّاً — عطّله أو صفّر القاعدة |
