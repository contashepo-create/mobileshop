# 📤 النسخ الاحتياطي على تليجرام

**التاريخ:** ٣١ يوليو ٢٠٢٦

---

## ١. إجابة سؤالك: **لا، لم تكن موجودة**

سألت: «هل أضفت إعدادات التليجرام إلى الإعدادات؟»

فحصت الكود، والإجابة الصادقة: **لا**. الموجود سابقاً كان فقط **عرض حساب تليجرام
الخاص بالمطور** للتواصل (في صفحة «حول البرنامج») — ولا شيء يخص المالك إطلاقاً.

**الآن أضفتها** في: `الإعدادات ← النسخ الاحتياطي`

---

## ٢. لماذا هذه الميزة مهمة؟

النسخ التلقائية الحالية تُحفظ في:
```
%APPDATA%\mobile-shop-erp\backups
```

وهذا **على نفس القرص الصلب** الذي عليه قاعدة البيانات. معناه:

| الكارثة | النتيجة بالنسخ المحلية فقط |
|---|---|
| تلف القرص الصلب | 🔴 ضاعت القاعدة **وكل النسخ معاً** |
| سرقة الكمبيوتر | 🔴 ضاع كل شيء |
| فيروس فدية | 🔴 يشفّر القاعدة والنسخ معاً |

**النسخة على تليجرام هي الوحيدة التي تنجو من فقدان الجهاز نفسه.**

---

## ٣. 🔻 كيف تُفعّلها — خطوة بخطوة

### الخطوة ١: أنشئ بوت خاص بك
افتح تليجرام وابحث عن **@BotFather** ← أرسل:
```
/newbot
```
سيسألك عن اسم البوت ثم معرّفه (يجب أن ينتهي بـ `bot`).
سيعطيك في النهاية **رمز البوت** بهذا الشكل:
```
123456789:AAxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

> ⚠️ **استخدم بوتاً جديداً خاصاً بالنسخ الاحتياطي.** لا تستخدم بوت الترخيص.

### الخطوة ٢: احصل على معرّف المحادثة (Chat ID)
افتح محادثة **البوت الجديد** واضغط **Start**، ثم ابحث عن **@userinfobot**
وأرسل له أي رسالة — سيعطيك رقمك (`Id`).

> 💡 معرّفك المسجّل عندي سابقاً هو: `7232305465`

### الخطوة ٣: أدخلها في البرنامج
```
الإعدادات ← النسخ الاحتياطي ← نسخة احتياطية على تليجرام
```
- الصق **رمز البوت**
- اكتب **معرّف المحادثة**
- اضغط **حفظ الإعدادات**
- اضغط **اختبار الاتصال** ← يجب أن تصلك رسالة «✅ تم ربط النسخ الاحتياطي»

### الخطوة ٤: فعّل الإرسال التلقائي (اختياري)
✅ ضع علامة على **«إرسال النسخة اليومية تلقائياً»** — سترسل نسخة **مرة واحدة كل يوم**.

أو استخدم زر **«إرسال نسخة الآن»** يدوياً وقتما شئت.

---

## ٤. 🔒 الأمان — ماذا فعلت لحمايتك

### 🔴 ثغرتان حقيقيتان وجدتهما أثناء العمل وأغلقتهما

رمز البوت هو **مفتاح حامل** (bearer token): من يملكه يتحكم في البوت. وأثناء
التنفيذ اكتشفت طريقين كان يمكن أن يتسرب منهما:

| # | الثغرة | الخطورة |
|---|---|---|
| ١ | `settings:getAll` قناة **عامة تعمل قبل تسجيل الدخول** (لعرض شعار المحل في شاشة الدخول). أي مفتاح تُرجعه يمكن قراءته **بدون حساب** | 🔴 |
| ٢ | `settings:get` كانت تُرجع **أي مفتاح باسمه** بدون قيود — وهي عامة أيضاً | 🔴 |
| ٣ | جدول `settings` كان **قابلاً للتصدير إلى CSV** | 🔴 |

> **الأهم:** الثغرتان ١ و٢ كانتا موجودتين **قبل** هذه الميزة — كان يمكن قراءة
> `cloud_api_key` (مفتاح السحابة) بدون تسجيل دخول! أغلقتهما الآن كلتيهما.

**الإصلاح:** كل المفاتيح التي تبدأ بـ `telegram_` و `cloud_` و `sync_` صارت
محجوبة عن القنوات العامة، وجدول `settings` صار على قائمة منع التصدير.

### حمايات إضافية
| الحماية | التفصيل |
|---|---|
| الرمز **لا يعود للواجهة أبداً** | البرنامج يرسل `123456789:***` فقط، لا الرمز |
| **إخفاء الرمز من رسائل الخطأ** | تليجرام يُعيد الرابط الذي يحوي الرمز في بعض الأخطاء — يُمحى تلقائياً |
| **صلاحيات** | العرض يحتاج «عرض الإعدادات»، والإرسال يحتاج «تعديل الإعدادات» |
| **مغلقة افتراضياً** | لا تعمل إلا إذا فعّلتها بنفسك |
| حقل الرمز **password** | لا يظهر على الشاشة |

### ⚠️ ما لا أدّعيه
الرمز مُخزَّن **نصاً عادياً** في قاعدة البيانات المحلية، تماماً كمفاتيح السحابة
الموجودة أصلاً. تشفيره بمفتاح موجود على **نفس القرص** تمثيل لا أمان.

من يملك ملف قاعدة البيانات يملك **دفاترك كاملة** أصلاً. التخفيف الصادق: الرمز
**رمزك أنت**، ويمكنك إلغاؤه من @BotFather في ثوانٍ، ولا يمنح شيئاً غير ذلك البوت.

---

## ٥. ⚠️ حدود يجب أن تعرفها

| # | الحد |
|---|---|
| ١ | **٥٠ ميجابايت** هو أقصى حجم يقبله بوت تليجرام. البرنامج **يفحص الحجم قبل الرفع** ويخبرك برسالة عربية واضحة بدل خطأ تقني |
| ٢ | يُرفع **قاعدة البيانات كاملة** — كل العملاء والأسعار والأرصدة. لذلك الشاشة تحذّرك بوضوح |
| ٣ | الإرسال التلقائي **مرة واحدة يومياً** فقط (البرنامج يفحص كل ساعة، لكنه لا يرسل مرتين) |
| ٤ | لو فشل الرفع، **يعيد المحاولة في الساعة التالية** ولا يتخطى اليوم بصمت |

---

## ٦. 🧪 التحقق

### ٥٥ فحصاً، كلها ناجحة

### 🔴 عطل حقيقي اكتشفه الاختبار في كودي أنا
دالة إخفاء الرمز كانت تبدأ بـ:
```js
if (!token) return text;   // ❌
```
فإذا استُدعيت بدون رمز، **ترجع النص كما هو** — بما فيه الرابط الكامل
`https://api.telegram.org/bot<الرمز>/...` الذي يُعيده تليجرام في الأخطاء.

**أي أن الرمز كان يمكن أن يُطبع كاملاً في رسالة خطأ.** أصلحته: تنظيف الرابط
صار يحدث **دائماً** بلا شرط.

### ١١ عطلاً محقوناً — **١١ اكتُشفت، ٠ نجت**

| # | العطل | النتيجة |
|---|---|---|
| ١ | كشف الرمز عبر `settings:getAll` | ✅ |
| ٢ | إلغاء حجب المفاتيح السرية في `settings:get` | ✅ |
| ٣ | إعادة `settings` للتصدير | ✅ |
| ٤ | إرجاع الرمز للواجهة | ✅ |
| ٥ | حذف فحص الـ٥٠ ميجابايت | ✅ |
| ٦ | إعادة عطل الإخفاء | ✅ |
| ٧ | الإرسال كل ساعة بدل يومياً | ✅ |
| ٨ | الإرسال رغم الإيقاف | ✅ |
| ٩ | تعليم اليوم كمُرسَل رغم الفشل | ✅ |
| ١٠ | جعل القناة عامة قبل الدخول | ✅ |
| ١١ | عدم حذف النسخة المؤقتة | ✅ |

> **ملاحظة منهجية:** فحصان فشلا أول مرة. أحدهما كشف عطلاً حقيقياً (رقم ٦)،
> والآخر كان **خطأ في اختباري أنا** (توقّعت صيغة كتابة مختلفة). صحّحت كلاً منهما
> في موضعه الصحيح — لا أعدّل الكود ليوافق اختباراً خاطئاً.

---

## ٧. كيف تستعيد من نسخة تليجرام؟

1. افتح محادثة البوت على تليجرام
2. نزّل ملف `.db` المطلوب (اختر التاريخ من التسمية)
3. انقله إلى الكمبيوتر
4. اتبع خطوات الاستعادة العادية في `دليل_استعادة_النسخة_الاحتياطية.md`

> ⚠️ **حد التنزيل للبوت ٢٠ ميجابايت** عبر البوت نفسه، لكن **أنت تنزّل من تطبيق
> تليجرام العادي** لا من البوت — فلا يوجد حد عملي عليك.
