# تقرير: مراجعة سياسات الوصول وعزل بيانات العملاء

**التاريخ:** ٥ أغسطس ٢٠٢٦ | **الإصدار:** `a4a1a4b`
**النتيجة:** ⚠️ **الطلب كان مبنياً على افتراض غير صحيح — لكن السؤال الحقيقي خلفه كشف ثغرة فعلية وأُصلحت**

---

## ١. أولاً: لا يوجد RLS في هذا المشروع أصلاً

**RLS (Row Level Security)** خاصية في **PostgreSQL**. هذا المشروع **لا يستخدم PostgreSQL إطلاقاً**.

لم أكتفِ بالقول — **جرّبت الأوامر فعلياً** على محرك قاعدة البيانات المستخدم:

```
SQLite 3.53.4
ALTER TABLE t ENABLE ROW LEVEL SECURITY  ->  near "ENABLE": syntax error
CREATE POLICY p ON t USING (true)        ->  near "POLICY": syntax error
GRANT SELECT ON t TO authenticated       ->  near "GRANT":  syntax error
SELECT current_setting('...')            ->  no such function
```

| الفحص | النتيجة |
|---|---|
| ملفات `.sql` | **لا يوجد** |
| `CREATE POLICY` في أي مكان | **لا يوجد** |
| مكتبة PostgreSQL | **لا يوجد** |
| «Supabase» في الكود | مجرد **خيار لرفع نسخة احتياطية**، وليس قاعدة يُستعلم منها |

> لو كتبتُ «سياسات RLS» لهذا المشروع، لكان كوداً **لا يعمل ولا يُنفَّذ** — وهذا أسوأ من عدم كتابته، لأنه يمنح شعوراً زائفاً بالأمان.

---

## ٢. لكن السؤال الحقيقي مهم: هل يصل عميل لبيانات عميل آخر؟

هذا هو جوهر الطلب، وهو **ينطبق تماماً**. عندنا مخزنان:

| المخزن | المحرك | مشترك؟ |
|---|---|---|
| قاعدة المحل | SQLite | ❌ **ملف مستقل لكل محل على جهازه** — لا يوجد مستأجر ثانٍ لنعزله |
| قاعدة D1 السحابية | SQLite | ✅ **مشتركة بين كل العملاء** ← **هنا السؤال حقيقي** |

فحصتُ الثانية.

---

## ٣. 🔴 الثغرة: محل يستطيع الكتابة فوق سجل محل آخر

**الملف:** `server/worker.js` — نقطة `POST /registration`

### السبب
`CLIENT_KEY` **مشحون داخل كل نسخة من البرنامج**. فهو يُثبت «هذه نسخة من التطبيق»، **ولا يُثبت أبداً «أنا هذا المحل»**. ومعرّف الجهاز كان يأتي **من جسم الطلب** — أي أن المُرسِل هو من يختار السجل.

### القياس الفعلي (بالمفتاح المشحون فقط)

```
السجل الأصلي : {"company_name":"محل خالد","owner_name":"خالد","phone":"01011111111"}
بعد الهجوم   : {"company_name":"DEFACED","owner_name":"x","phone":"0"}
```

| الهجوم | النتيجة قبل الإصلاح |
|---|---|
| الكتابة فوق سجل محل آخر | 🔴 **نجح** |
| حذف `deviceId` تماماً | 🔴 صف بمفتاح **نص فارغ** يدهسه كل مجهول تالٍ |
| إغراق بـ ٢٠٠ صف | 🔴 **نجح** — و٢٠٠ إشعار تليجرام تدفن كل عميل حقيقي |

> النقاط الشقيقة (`heartbeat` و `database-reset`) كانت تفحص طول المعرّف — **هذه وحدها بلا أي فحص**.

---

## ٤. الإصلاح

| القبل | البعد |
|---|---|
| `deviceId` أي نص | **٣٢ حرفاً hex** بالضبط (كما يُنتجه `deviceId.ts`) |
| `ON CONFLICT DO UPDATE` | **`DO NOTHING`** — الكاتب الأول يفوز، ومسار التحديث **حُذف نهائياً** |
| بلا حد | **٥٠ تسجيلاً جديداً/يوم** كحد أقصى |
| إعادة التسجيل = كتابة فوقية | تُقبل بهدوء `alreadyRegistered: true` |

---

## ٥. منعطفان خاطئان — اكتشفتهما بالقياس لا بالتفكير

هذا الجزء مهم، لأن كل «إصلاح» منهما بدا صحيحاً على الورق:

### ❌ المحاولة ١: «الكاتب الأول يفوز» وحدها
**المشكلة:** مهاجم يخمّن معرّفاً **ويحجزه قبل صاحبه** — فيُمنع المحل الحقيقي من التسجيل **للأبد**.
> استبدلتُ تشويهاً بتعطيل خدمة. **قِسته ورأيته.**

### ❌ المحاولة ٢: «اشترط heartbeat سابقاً»
**المشكلة:** كسرَت حالتين حقيقيتين:
- المعالج ينتهي **قبل** أول heartbeat (تأخير ٣٠ ثانية)
- محل أطفأ التتبّع ⇐ **لا يرسل heartbeat أبداً**، لكنه قد يوافق على مشاركة بياناته

> كان العميل الصادق سيرى موافقته تُهمَل بصمت.

### ✅ الحل النهائي
صف الجهاز يُنشأ **تلقائياً عند الحاجة**، والحماية من **قاعدة الكتابة مرة واحدة + الحد اليومي**.

---

## ٦. ما فُحص ووُجد سليماً ✅

| الفحص | النتيجة |
|---|---|
| ردّ heartbeat يحوي اسم محل آخر | ✅ لا |
| إعدادات محل تصل لمحل آخر | ✅ لا (وأثبتُّ أن صاحبها **يستلمها فعلاً**) |
| المفتاح المشحون يسرد كل العملاء | ✅ مرفوض ٤٠١ |
| المفتاح المشحون يُصدر ترخيصاً | ✅ مرفوض ٤٠١ |
| المفتاح المشحون يبثّ إعدادات للجميع | ✅ مرفوض ٤٠١ |
| المفتاح المشحون ينشر تحديثاً مزيفاً | ✅ مرفوض ٤٠١ |
| قاعدة المحل تحوي جدول تراخيص | ✅ لا (فلا يُفعّل محلٌّ محلاً آخر) |

---

## ٧. الاختبار المضاد

```
١١ عطلاً مزروعاً → ١١ ضُبطت (١١/١١) ✅
```

> اثنان نجيا أولاً. أحدهما كشف أن اختبار التسريب **لم يكن يكتب أي صف أصلاً** (استخدمتُ شكل payload خاطئاً) — أي أنه كان سينجح ضد أي كود. أصلحتُه وأضفتُ تأكيداً أن الجهاز **الصحيح يستلم إعداده** — وإلا فإثبات «الآخر لا يراه» بلا معنى.

### واكتشفتُ فحصاً قديماً يحرس الثغرة نفسها

`verify_phase3_registration.mjs` كان يؤكد وجود `ON CONFLICT ... DO UPDATE` — **أي أنه كان يثبّت السلوك المعيب**. صحّحتُه، وتحققتُ أنه **يفشل الآن** لو أُعيد الـ upsert.

---

## ٨. حدّ صريح لم أخفِه

مفتاح مشترك + هوية يرسلها العميل **لا يمكن أن يكونا مصادقة حقيقية**. الإصلاح يُزيل الحالات التي **لا تحتاج أي معرفة** (المفتاح الفارغ، النفايات، الكتابة الفوقية، الإغراق)، ويحصر ما تبقّى في مهاجم **يعرف معرّف جهاز ضحية بعينه**.

الإغلاق الكامل يتطلب **توقيع الطلب بمفتاح الجهاز** — وهو تغيير بروتوكول على كل نسخة مثبّتة. سجّلتُه في الكود كما هو بدل التستّر عليه.

---

## ٩. الوضع النهائي

| المؤشر | النتيجة |
|---|---|
| فحوصات العزل | **٤١ فحصاً، ٠ فشل** |
| الفحص الشامل | **٧٣ مجموعة، ٠ فشل** |
| طفرات التجارة | **٥٥/٥٥** |
| العشوائي | **٣٩/٤٠** |
| أخطاء الأنواع | **٠** |

---

## 🔻 الخطوات على جهازك

**١)**
```powershell
cd "D:\programing\coding projects\mobile shop"
```

**٢)**
```powershell
git pull origin master
```

**٣)** انشر الـ Worker المُحدَّث (الإصلاح في السحابة، لا في التطبيق):
```powershell
cd server
npx wrangler deploy
```
