# تقرير: الحد من المحاولات على كل نقاط التحقق من الهوية

**التاريخ:** ٥ أغسطس ٢٠٢٦ | **الإصدار:** `6c38a6b`
**النتيجة:** ✅ **٤ نقاط مكشوفة اكتُشفت وأُغلقت**

---

## ١. الحصر: ١٥ نقطة تتحقق من سرّ

استخرجتُ كل قناة تتحقق من كلمة مرور أو رمز أو توكن — بقصّ جسم كل معالج بعمق الأقواس (لا بنافذة أسطر ثابتة، لأنها تخلط معالجاً بالذي يليه).

| الحالة | العدد |
|---|---|
| **كانت محمية** | ٢ |
| **كانت محمية عبر وسيط** | ٩ |
| 🔴 **مكشوفة تماماً** | **٤** |

---

## ٢. 🔴 النقاط الأربع المكشوفة

كلها كانت تستدعي `bcrypt.compareSync` مباشرة **بلا أي عدّاد**:

| القناة | الملف:السطر | ماذا تفعل عند النجاح |
|---|---|---|
| `db:exportForOwner` | `database.handlers.ts:589` | **تُصدّر قاعدة البيانات كاملة** |
| `users:adminResetPassword` | `users.handlers.ts:223` | **تُغيّر كلمة مرور أي مستخدم** |
| `settings:resetRequestCode` | `settings.handlers.ts:466` | الخطوة ١ من **مسح القاعدة** |
| `settings:resetDatabase` | `settings.handlers.ts:498` | الخطوة ٢ من **مسح القاعدة** |

### القياس الفعلي (على المعالجات الحقيقية)

```
db:exportForOwner          ٤٠ محاولة في ٣١١٤ ms  (~١٣/ثانية)
users:adminResetPassword   ٤٠ محاولة في ٣١٠٣ ms  (~١٣/ثانية)
settings:resetRequestCode  ٤٠ محاولة في ٣١٠٩ ms  (~١٣/ثانية)
settings:resetDatabase     ٤٠ محاولة في ٣١٢١ ms  (~١٣/ثانية)

بعد ١٦٠ محاولة خاطئة: لا إيقاف، لا تأخير، ولا أي أثر
```

### لماذا لم يكفِ حماية `auth:login`؟

> **كل واحدة من هذه النقاط لها حقل كلمة مرور خاص بها.** المهاجم الذي يُقفل من شاشة الدخول ينتقل ببساطة إلى إحداها — **وهي أثمن من الدخول العادي**: تصدير كل البيانات، أو تغيير كلمة مرور المدير، أو مسح المحل.

وأسوأ: `settings:resetDatabase` تُعيد فحص كلمة المرور مرة أخرى (لأن كل معالج يقف بذاته) — فكانت **بابين للتخمين لا باباً واحداً**.

---

## ٣. لماذا لم أستخدم "middleware"؟

**لا يوجد HTTP ولا عنوان IP هنا** — هذا اتصال IPC داخل تطبيق سطح مكتب واحد.

`installIpcGuard` يلفّ كل القنوات فعلاً، لكن وضع حدٍّ عام هناك كان سيَعُدّ **عمليات القراءة والبيع العادية** ويُبطئ المحل. الحدّ ينتمي إلى المعالجات التي تتحقق من سرّ — فعمّمتُ الوحدة الموجودة بدل إدخال آلية ثانية.

---

## ٤. ما نفّذته

### وحدة واحدة، ثلاثة نطاقات

| النطاق | يشمل | لماذا منفصل |
|---|---|---|
| `login` | الدخول العادي | **حتى لا يتوقف البيع** |
| `dangerous` | التصدير + تغيير كلمة المرور + مسح القاعدة | **مشترك** حتى لا ينتقل المهاجم بين الأبواب |
| `code` | الرموز المؤقتة | فوق الحد الموجود لكل رمز |

### العتبات (موحّدة)

| القاعدة | القيمة |
|---|---|
| محاولات فاشلة متتالية | **٥** |
| مدة الإيقاف | **١٥ دقيقة** |
| نافذة العدّ | **١٥ دقيقة متحركة** |
| النجاح | **يُصفّر السجل** |

- **الفحص قبل** قراءة القاعدة و**قبل** bcrypt — فالحساب المقفل لا يكلّف شيئاً ولا يكشف شيئاً
- **يُحتسب للأسماء غير الموجودة** — وإلا لما تباطأ من يخمّن الأسماء
- **يُحتسب لكلمة مرور صحيحة على حساب غير مدير** — وإلا كانت كلمة مرور الكاشير أداة تخمين مجانية
- **المعرّف هو رقم المستخدم** في كل الأبواب — فالإيقاف يتبع **الشخص** لا الحقل

### النتيجة بعد الإصلاح

```
db:exportForOwner          -> LOCKED OUT ✅ (المحاولة ٦)
users:adminResetPassword   -> LOCKED OUT ✅ (المحاولة ٦)
settings:resetRequestCode  -> LOCKED OUT ✅ (المحاولة ٦)
settings:resetDatabase     -> LOCKED OUT ✅ (مقفل مسبقاً — النطاق مشترك)
```

---

## ٥. ما تحققتُ منه حتى لا يتضرر المحل

| السيناريو | النتيجة |
|---|---|
| المالك يخطئ ٤ مرات ثم يُصيب | ✅ **مقبول** — لا إيقاف |
| نجاح ثم ٤ أخطاء أخرى | ✅ **مقبول** — النجاح صفّر العدّاد |
| قفل شاشة التصدير | ✅ **البيع يستمر** — النطاقات منفصلة |
| أخطاء التصدير | ✅ **لا تستهلك رصيد محاولات الدخول** |

> لو أُقفل البيع بسبب خطأ في شاشة التصدير، لتوقف المحل ١٥ دقيقة والزبائن واقفون — **وهذا أسوأ من الهجوم نفسه**.

---

## ٦. الاختبار المضاد

```
١٢ عطلاً مزروعاً → ٩ ضُبطت، ٣ أُثبت أنها مكافئة
```

الثلاثة الناجية كلها بسبب **ازدواج متعمد**: `db:exportForOwner` يفحص مرتين (بالاسم قبل قراءة الصف، وبرقم المستخدم بعدها). حذف أحدهما وحده لا يفتح الباب — **وحذف الاثنين معاً يُضبط**، وقد تحققتُ من ذلك بتنفيذه فعلياً لا بالافتراض.

**الفحص الجديد `[5b]` يعيد حصر النقاط من الكود المصدري في كل تشغيل** — فأي نقطة جديدة تُضاف بلا حماية **تُفشل البناء**. القائمة المكتوبة يدوياً كانت ستتقادم فوراً، وهي بالضبط المشكلة التي يمنعها هذا الفحص.

---

## ٧. الوضع النهائي

| المؤشر | النتيجة |
|---|---|
| فحوصات المصادقة | **٩٠ فحصاً، ٠ فشل** |
| الفحص الشامل | **٧٢ مجموعة، ٠ فشل** |
| طفرات التجارة | **٥٥/٥٥** |
| الفحص العشوائي | **٣٩/٤٠** |
| أخطاء الأنواع | **٠** |

---

## 🔻 الخطوات على جهازك

```powershell
cd "D:\programing\coding projects\mobile shop"
```
```powershell
git pull origin master
```

> ℹ️ الإيقاف مؤقت في الذاكرة — إعادة تشغيل البرنامج تُصفّره. هذه مقايضة مقصودة: البديل كتابة في القاعدة عند كل محاولة فاشلة، وهو بحد ذاته وسيلة لتعطيل الخدمة، ويضيف مسار كتابة إلى عملية **مسح القاعدة** نفسها.
