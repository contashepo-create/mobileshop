# كيف نُوصِل التحديثات للعملاء دون أن يفقدوا بياناتهم

**التاريخ:** 2026-07-30 · **الحالة:** البرنامج في وضع التطوير، لم يُوزَّع بعد

---

## القاعدة المُسجَّلة (رقم 8)

> **البرنامج حالياً في وضع التطوير ولم يُنشر بعد. أي بيانات موجودة الآن تجريبية واختبارية فقط — لا حاجة لترحيل بيانات أثري ولا لسكربتات تصحيح للبيانات القائمة.**

هذه القاعدة تُغيّر قراراً واحداً مهماً: **الآن هو الوقت الوحيد المتاح لإصلاح بنية الترحيل مجاناً.** بعد أول عميل حقيقي، كل خطأ في المخطط يصبح مشكلة دائمة.

---

## الجزء الأول: هل يفقد العميل بياناته؟ — قِستُ ذلك بنفسي

لم أجب من الذاكرة. بنيتُ قاعدة SQLite حقيقية فيها **250 سنداً**، ثم شغّلتُ دالة `rebuildTable` من كودك **بالحرف**:

```
العميل قبل الترقية: 250 سند، إجمالي 313750
--- ترقية تُعيد بناء الجدول بالكامل ---
بعد الترقية:       250 سند، إجمالي 313750
✅ لم تُفقد أي بيانات
```

ثم اختبرتُ **الفشل** عمداً (ترقية تالفة):

```
نجحت؟ false | السندات الآن: 250
✅ الفشل لم يمسّ البيانات (تراجع كامل)
```

**النتيجة: منطق الترحيل عندك سليم ومحمي بمعاملة.** السبب أن `rebuildTable` تنسخ الأعمدة **بالاسم** لا بالترتيب، وتلفّ كل شيء في `db.transaction` — فإما ينجح كله أو لا يحدث شيء.

### لماذا لا تُلغى القاعدة القديمة أصلاً

نظامك **لا يستبدل** قاعدة البيانات عند التحديث. ملف `.exe` الجديد يحلّ محلّ البرنامج فقط، بينما القاعدة تعيش في مكان منفصل تماماً:

```
C:\Users\<اسم المستخدم>\AppData\Roaming\MobileShopERP\mobile_shop.db
```

والترقية تعمل بأسلوب **«أضِف ولا تهدم»**:
- 54 جدولاً بصيغة `CREATE TABLE IF NOT EXISTS` — لا تلمس الموجود
- 45 عمود `ALTER TABLE ADD COLUMN` داخل `try/catch` — تفشل بصمت إن كان العمود موجوداً
- 4 حالات `rebuildTable` فقط للتغييرات الجذرية، وكلها بمعاملة آمنة

---

## الجزء الثاني: ثلاث ثغرات حقيقية اكتشفتُها بالقياس

رغم سلامة المنطق الأساسي، وجدتُ ثلاث مشكلات **يجب إصلاحها قبل أول عميل**:

### 🔴 1. النسخة الاحتياطية تُؤخذ **بعد** الترقية لا قبلها

```
src/main/index.ts:115  → runMigrations(db)     ← الترقية أولاً
src/main/index.ts:163  → autoBackup()          ← النسخة بعدها!
```

**الخطر:** لو أفسدت ترقيةٌ ما البيانات، النسخة الاحتياطية الوحيدة ستكون **للبيانات المُفسَدة بالفعل**. لا يوجد طريق للعودة.

**الإصلاح:** نسخة احتياطية **قبل** الترقية مباشرة، وفقط عند تغيّر رقم الإصدار.

### 🔴 2. لا يوجد رقم إصدار للمخطط

```
PRAGMA user_version → غير مستخدم إطلاقاً
```

قِستُ الأثر:

```
v2 أضافت عموداً، ثم فتح العميل v1 القديمة بالخطأ:
⚠️ v1 كتبت بنجاح — لا شيء يمنع التشغيل للخلف
```

**الخطر:** عميل يثبّت نسخة قديمة فوق قاعدة جديدة (أو يستعيد نسخة احتياطية قديمة) — يعمل البرنامج ويكتب بيانات ناقصة بصمت.

### 🟠 3. الترقيات ليست معاملة واحدة

كل `ALTER TABLE` في `try/catch` منفصل. لو انقطعت الكهرباء بين الترقية 20 و21، تبقى القاعدة **نصف مُرقّاة** بلا سجل يوضح أين توقفت.

---

## الجزء الثالث: كيف تفعلها البرامج العالمية

| البرنامج | الآلية |
|---|---|
| **VS Code / Slack / Discord** | Squirrel — تنزيل صامت في الخلفية، تثبيت عند إعادة التشغيل |
| **Chrome** | تحديث تفاضلي (delta) — ينزّل الفرق فقط لا البرنامج كله |
| **QuickBooks / Sage** | تحقق من إصدار المخطط + نسخة إجبارية قبل الترقية + شاشة «جارٍ تحديث بياناتك» |
| **WhatsApp Desktop** | مثل Squirrel، مع منع التشغيل للخلف |

**القاسم المشترك عند كل برنامج محاسبي جاد:**
1. رقم إصدار للمخطط مخزّن **داخل** القاعدة
2. نسخة احتياطية إجبارية **قبل** أي ترقية
3. رفض التشغيل إن كانت القاعدة **أحدث** من البرنامج
4. الترقية خطوة واحدة ذرّية

---

## الجزء الرابع: الخطة العملية لمشروعك

أنت تستخدم **`MakerSquirrel`** بالفعل — وهذه هي البنية الصحيحة تماماً لويندوز.

### المسار الموصى به: Squirrel + GitHub Releases

**لماذا هذا تحديداً لحالتك:**
- تدفع بالفعل على GitHub
- مجاني بالكامل
- التحديث يعمل تلقائياً في الخلفية
- العميل لا يفعل شيئاً سوى إعادة فتح البرنامج

### كيف ستبدو التجربة عند العميل

```
1. يفتح البرنامج ويعمل عادي
2. في الخلفية: يسأل GitHub «هل يوجد إصدار أحدث؟»
3. إن وُجد: ينزّله بصمت (لا يقاطع العمل)
4. رسالة لطيفة: «تحديث جديد جاهز — سيُطبَّق عند إعادة التشغيل»
5. عند إعادة الفتح: نسخة احتياطية → ترقية → البرنامج الجديد
```

### الخطوات التقنية

**1. رفع الإصدار في `package.json`**
```
"version": "1.0.1"
```

**2. إضافة `update-electron-app`** — سطران فقط في `src/main/index.ts`

**3. بناء ونشر**
```powershell
npm run make
```
ثم رفع الملفات الناتجة إلى GitHub Release باسم `v1.0.1`

**4. العميل يحصل عليه تلقائياً** خلال ساعات

---

## الجزء الخامس: سيناريو تعديل قاعدة البيانات (سؤالك المحوري)

**مثال واقعي:** تريد إضافة عمود «الرقم الضريبي» للعملاء.

### ما ستكتبه أنت

```ts
// في runMigrations، بنفس الأسلوب الموجود:
try {
  db.exec(`ALTER TABLE customers ADD COLUMN TaxNumber TEXT`);
} catch { /* العمود موجود مسبقاً */ }
```

### ما سيحدث عند العميل

| الخطوة | ماذا يحدث لبياناته |
|---|---|
| 1. تنزيل التحديث | لا شيء — القاعدة لم تُمسّ |
| 2. إعادة التشغيل | **نسخة احتياطية أولاً** (بعد الإصلاح المقترح) |
| 3. `runMigrations` | يُضاف العمود فارغاً للجميع |
| 4. البرنامج يفتح | **كل عميل، كل فاتورة، كل رصيد — كما هو** |

**العمود الجديد يُضاف فارغاً. الصفوف القائمة لا تُمسّ إطلاقاً.**

### الضمانات الثلاثة التي تمنع الكارثة

| # | الضمان | الحالة |
|---|---|---|
| 1 | القاعدة في مجلد منفصل عن البرنامج | ✅ موجود |
| 2 | الترقية «أضِف ولا تهدم» بمعاملة | ✅ موجود ومُثبَت بالقياس |
| 3 | نسخة احتياطية قبل الترقية + رقم إصدار | ❌ **ناقص — أوصي بإضافته** |

---

## توصيتي

**قبل أول عميل، أنفّذ هذه الثلاثة (يوم عمل واحد):**

1. **رقم إصدار للمخطط** (`PRAGMA user_version`) — يمنع التشغيل للخلف
2. **نسخة احتياطية قبل الترقية** — تُؤخذ فقط عند تغيّر الإصدار، مع إمكانية استعادة تلقائية عند الفشل
3. **`update-electron-app` + GitHub Releases** — التحديث التلقائي

وأضيف لها **فحصاً آلياً** يبني قاعدة بالإصدار القديم، يملؤها ببيانات، يُرقّيها، ويتحقق أن كل رقم بقي كما هو — بنفس منهج الفحص المتبع في المشروع.

**قل لي «نفّذ» وسأبدأ فوراً.**
