# نظام التفعيل — دليل الاستخدام

**النوع:** اشتراك بالمدة فقط — بلا أي فروق في المزايا بين العملاء.

---

## كيف يعمل باختصار

```
العميل                                    أنت
──────                                    ───
1. يفتح البرنامج
2. يضغط زر واتساب/تليجرام
   ↓ رسالة جاهزة بها معرّف جهازه
                            ────────→  3. تستلمها
                                        4. node scripts/license-keygen.js
                                             --device <المعرّف> --days 365
                            ←────────  5. ترسل له الكود (19 حرف)
6. يلصق الكود ويضغط "تفعيل" ✅
```

لا خادم، لا إنترنت، لا قاعدة بيانات مشتركة.

---

## الإعداد لمرة واحدة

```bash
node scripts/license-keygen.js --init
```

يطبع مفتاحاً سرياً ويحفظه في `scripts/.license-secret`، ويعطيك سطراً تضعه في
`src/main/security/licenseCrypto.ts`.

### ⚠️ ثلاث قواعد لا تُكسر

| القاعدة | السبب |
|---|---|
| **احفظ المفتاح في مدير كلمات مرور** | فقدانه = لا تستطيع إصدار أكواد جديدة |
| **لا ترفعه على Git إطلاقاً** | مضاف تلقائياً لـ `.gitignore` |
| **لا تغيّره بعد توزيع البرنامج** | كل الأكواد السابقة ستتوقف |

### إعداد تليجرام (اختياري لكن مُستحسن)

من **لوحة المطور ← بيانات البرنامج ← تليجرام**، أدخل اسم المستخدم مثل `@yourname`.

**الفرق مهم:**

| الحالة | ما يحدث عند الضغط |
|---|---|
| أدخلت `@username` | تفتح المحادثة **والرسالة مكتوبة جاهزة** ✅ |
| تركته فارغاً | تفتح المحادثة، والرسالة **تُنسخ للحافظة** ليلصقها العميل |

هذا قيد في تليجرام نفسه: روابط الأرقام لا تدعم النص المُسبق، بعكس أسماء المستخدمين.

---

## إصدار كود لعميل

```bash
# سنة
node scripts/license-keygen.js --device a1b2c3... --days 365

# شهر، مع ملاحظة تعريفية
node scripts/license-keygen.js --device a1b2c3... --days 30 --note "محل الأمل"

# حتى تاريخ محدد
node scripts/license-keygen.js --device a1b2c3... --until 2027-01-31

# مدى الحياة
node scripts/license-keygen.js --device a1b2c3... --days 0

# سجل كل ما أصدرته
node scripts/license-keygen.js --list
```

**المخرجات:**

```
  Device : a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6
  Expires: 2027-07-27
  Serial : #1

  CODE   : 1B60-0001-AFR9-9EAJ

--- Ready-to-send reply (Arabic) ---

كود التفعيل الخاص بك:

1B60-0001-AFR9-9EAJ

صالح حتى: 2027-07-27
انسخ الكود والصقه في شاشة التفعيل ثم اضغط "تفعيل".
```

انسخ القسم الأخير وأرسله كما هو.

> **بديل:** لوحة المطور ← أكواد التفعيل — تولّد نفس الكود. لكن استخدمها للطوارئ فقط،
> لأن الأفضل بقاء مفتاح التوقيع خارج نسخ العملاء.

---

## ما يراه العميل

رسالة الواتساب تُبنى تلقائياً:

```
السلام عليكم 👋
أرغب في تفعيل برنامج إدارة محلات الموبايلات.

🔑 معرّف الجهاز:
a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6

📌 الحالة الحالية: انتهت الفترة التجريبية

برجاء إرسال كود التفعيل. شكراً لحضرتك.
```

**معرّف الجهاز مُضمَّن تلقائياً** — لا يكتبه العميل ولا تسأله عنه.

---

## كيف صُمِّم الأمان

**بنية الكود (10 بايت → 19 حرف):**

```
[ تاريخ الانتهاء: 2 بايت ][ رقم الإصدار: 3 بايت ][ توقيع HMAC: 5 بايت ]
```

التوقيع = `HMAC-SHA256(المفتاح, معرّف الجهاز + الحمولة)`

معرّف الجهاز **لا يُرسَل داخل الكود** — البرنامج يعرف جهازه ويخلطه في التوقيع. لهذا
يبقى الكود قصيراً ولا يعمل على جهاز آخر.

| الهجوم | النتيجة |
|---|---|
| نسخ الكود لجهاز آخر | ❌ التوقيع لا يطابق |
| تعديل تاريخ الانتهاء | ❌ التوقيع ينكسر |
| تخمين كود | ❌ 1 من 1.1 تريليون + قفل بعد 10 محاولات |
| إرجاع تاريخ الجهاز | ❌ يُقارَن بأحدث فاتورة في قاعدة بياناته |
| إعادة استخدام نفس الكود | ❌ الرقم التسلسلي مسجَّل |

**كشف تلاعب التاريخ** يستخدم ثلاث إشارات، أقواها المقارنة بأحدث فاتورة — لأن العميل
لا يستطيع حذفها دون خسارة محاسبته كلها.

---

## المدة فقط — كما طلبت

الكود يحمل **تاريخ انتهاء ورقم إصدار**، ولا شيء غير ذلك:

```ts
{ expiryDays: number; serial: number }   // لا features، لا tier، لا modules
```

- ترخيص صالح → **البرنامج كامل المزايا**
- ترخيص منتهٍ → **شاشة التجديد فقط، والبيانات محفوظة**

صلاحيات الموظفين تبقى حيث يجب أن تكون: نظام `permissions` في قاعدة البيانات،
يديره صاحب المحل. **التجديد لا يغيّر صلاحيات أحد.**

اختبار آلي يمنع إدخال أي تمييز بالمزايا مستقبلاً.

---

## حالات شائعة

| الموقف | الإجراء |
|---|---|
| **تجديد** | نفس الأمر بمدة جديدة. الرقم التسلسلي يتغير فيُقبل الكود |
| **تغيير جهاز العميل** | معرّف الجهاز يتغير → أصدر كوداً جديداً للمعرّف الجديد |
| **«الكود لا يعمل»** | تأكد أن المعرّف المُرسل يطابق المعروض عنده حرفياً |
| **كود أقصر من الحالي** | يُرفض تلقائياً حتى لا يُخفَّض اشتراك بالخطأ |
| **فقدت المفتاح السري** | لا يمكن إصدار أكواد جديدة. الأكواد القائمة تعمل. **خذ نسخة احتياطية الآن** |

**تسامح في الإدخال:** الكود يقبل الأحرف الصغيرة، وبلا شرطات، ومع مسافات زائدة،
ويصحّح الخلط بين `O/0` و `I/1` تلقائياً.

---

## التحقق

```bash
npm run verify      # كل الاختبارات (6 مجموعات)
npm run license     # اختصار لأداة التوليد
```

`verify_licensing.js` يشغّل 30 تأكيداً: الارتباط بالجهاز، كشف التزوير، انتهاء الصلاحية،
التسامح في الكتابة، غياب أي تمييز بالمزايا، وصحة روابط التواصل.

---

## ملاحظة صريحة عن حدود هذا النظام

يستخدم **HMAC** (مفتاح متماثل)، أي أن المفتاح الذي يتحقق هو نفسه الذي يوقّع. لهذا
تبقى أداة التوليد على جهازك ولا تُشحن مع البرنامج.

**الأثر العملي:** مهاجم متمرّس يستخرج المفتاح من ملف البرنامج ويولّد أكواداً لنفسه.
هذه مقايضة مقصودة مقابل كود من 19 حرفاً يُرسَل عبر الواتساب.

**البديل الأقوى:** توقيع Ed25519 — مفتاحك الخاص لا يُشحن إطلاقاً، لكن الكود يصبح
**108 حرفاً** (اختبرته) فيتحول التسليم إلى ملف `.lic` بدل كود يُكتب.

قابل للترقية لاحقاً دون تغيير سير العمل — أخبرني إن أردت ذلك.
