# 🔴 إصلاح عطل: البرنامج لم يكن يفتح إطلاقاً

**التاريخ:** ٣١ يوليو ٢٠٢٦
**الحالة:** ✅ تم الإصلاح والدفع إلى GitHub
**الكوميت:** `e7fbe6f`

---

## ١. ما الذي حدث؟

عند تشغيل `npm start` كان البرنامج يموت فوراً بهذه الرسالة:

```
✖ Building src/main/index.ts target ›
Transform failed with 1 error:
src/main/database/migrations/index.ts:395:10:
ERROR: Expected ")" but found "sale_details"
```

النتيجة: **نافذة بيضاء / لا يفتح البرنامج**، لأن ملف العمليات الأساسي
(`main process`) لم يُبنَ أصلاً.

---

## ٢. سبب العطل بالضبط

في ملف `src/main/database/migrations/index.ts` سطر ٣٩٥، داخل **تعليق** مكتوب
وسط أوامر إنشاء الجداول:

```
-- `sale_details` with ItemID NULL. Declaring it NOT NULL here meant that
```

المشكلة في علامة الاقتباس المائلة **`` ` ``** (backtick).

هذه الأوامر مكتوبة داخل ما يسمى *template literal* في جافاسكربت، وهو نص يبدأ
وينتهي بعلامة `` ` ``. لذلك عندما وجد المترجم علامة `` ` `` داخل التعليق:

| ما قصده الكاتب | ما فهمه المترجم |
|---|---|
| تعليق يشرح الجدول | **نهاية النص!** |
| باقي أوامر SQL | كود جافاسكربت يجب تنفيذه |

فحاول المترجم قراءة `sale_details` ككلمة برمجية، فانهار البناء.

> ببساطة: **علامة ترقيم في تعليق أوقفت البرنامج بالكامل.** التعليق نفسه لا
> يؤثر على المحاسبة إطلاقاً — لكنه كسر البناء.

**متى دخل العطل؟** في الكوميت `c517426`، وكل كوميت بعده كان غير قابل للبناء.
تأكدت من ذلك بفحص كل كوميت على حدة:

| الكوميت | يبني؟ |
|---|---|
| `332b941` | ✅ |
| `c517426` | 🔴 **معطل** |
| `5f69c32` | 🔴 معطل |
| `6a4899a` | 🔴 معطل |
| `8cb3400` | 🔴 معطل |

---

## ٣. 🔴 الأخطر: لماذا لم تكتشفه ٤٣ اختباراً ناجحة؟

هذه هي المشكلة الحقيقية، وأخطر من العطل نفسه. اجتمع سببان:

### السبب الأول: `npm run verify` لم يكن يبني البرنامج أبداً

كل الاختبارات كانت إما تقرأ الكود **كنص**، أو تستورد ملفاً صغيراً واحداً.
**لا شيء** كان يسأل السؤال الذي يسأله المستخدم حين يكتب `npm start`:
*هل يُبنى البرنامج كاملاً؟*

### السبب الثاني: الاختبارات كانت تقرأ كوداً مختلفاً عن الذي يقرأه المترجم

منظومة الاختبار تستخرج الجداول بتعبير نمطي (regex):

```js
/db\.exec\(`([\s\S]*?)`\)/g
```

هذا التعبير **قفز فوق** علامة `` ` `` الشاردة والتقط الكتلة كاملة وصحيحة.

| | ما رآه |
|---|---|
| **الاختبارات** | ✅ الجدول كاملاً وصحيحاً |
| **المترجم الحقيقي** | 🔴 نص مبتور عند سطر ٣٩٥ |

فبنت الاختبارات **قاعدة بيانات سليمة من ملف لا يستطيع المترجم قراءته**.
الاختبارات والبرنامج كانا مختلفَين حول *ماذا يقول الكود أصلاً* — ولهذا بقيت
كلها خضراء.

> **ملاحظة مهمة على تقرير سابق:** كان مسجّلاً في المشروع أن هذا الملف
> «صحيح ولا يمكن استيراده بسبب مشكلة في مفسّر Node»، وبُني حل التفافي
> (regex) على هذا الاعتقاد. **المفسّر كان على حق، والحل الالتفافي هو الذي
> أخفى عطلاً حقيقياً.** تم تصحيح ذلك الآن.

---

## ٤. الإصلاح

### أ) إصلاح العطل نفسه
حذف علامات `` ` `` من التعليق (لا تغيير في السلوك — إنه تعليق)، مع ملاحظة
تشرح لماذا يجب ألا تعود.

### ب) بوابة بناء جديدة: `scripts/verify_build.mjs`

أضفتها **كأول خطوة** في `npm run verify` لتفشل فوراً، ومتاحة أيضاً وحدها عبر
`npm run build_check`:

| # | ما تفحصه |
|---|---|
| **١** | كل ملف `.ts/.tsx` تحت `src/` يُترجم (١٠٦ ملفات) |
| **٢** | البناء الكامل لـ `main` و `preload` و `renderer` تماماً كما يبنيها electron-forge |
| **٣** | لا توجد علامة `` ` `` شاردة داخل أي أمر SQL — بفحص مباشر حتى تكون الرسالة واضحة بدل `Expected )` |
| **٤** | منظومة الاختبار ترى **نفس** الجداول التي يراها المترجم حرفاً بحرف — حتى لا تختبر جداول وهمية مرة أخرى |

الفحص رقم **٤** هو الأهم: يمنع تكرار هذا الصنف من الأعطال من جذوره.

### ملاحظة تقنية على الفحص رقم ٢
جرّبت أولاً `--packages=external` ورفضتها: تبيّن أنها تتجاهل أيضاً المسارات
**المحلية**، فاستيراد ملف غير موجود كان «يُبنى بنجاح». استبدلتها بإضافة
(plugin) تستثني حزم npm فقط، فيُجبَر كل ملف من ملفاتنا على أن يُحَل فعلياً.

> **esbuild لا يحتاج تثبيت جديد** — حزمة `vite` تعتمد عليه أصلاً وهو موجود في
> `package-lock.json`. وإذا غاب، البوابة **تفشل بصوت عالٍ** ولا تتخطى بصمت،
> لأن التخطي الصامت هو ما سمح بهذا العطل أساساً.

---

## ٥. إثبات أن البوابة تعمل فعلاً

اختبار لا يستطيع الفشل = اختبار عديم القيمة. لذلك أعدت حقن العطل الأصلي
وأربعة أعطال أخرى للتأكد:

| # | العطل المحقون | النتيجة |
|---|---|---|
| ١ | العطل الأصلي (backtick في التعليق) | ✅ اكتُشف — ٥ فحوص فشلت |
| ٢ | استيراد ملف غير موجود في `renderer` | ✅ اكتُشف |
| ٣ | استيراد ملف غير موجود في `main` | ✅ اكتُشف |
| ٤ | استيراد ملف غير موجود في `preload` | ✅ اكتُشف |
| ٥ | خطأ نحوي في `sales.handlers.ts` | ✅ اكتُشف |
| ٦ | backtick في ملف SQL آخر (`settings`) | ✅ اكتُشف |

**٥/٥ اكتُشفت، ٠ نجت.**

---

## ٦. الحالة النهائية

| المؤشر | قبل | بعد |
|---|---|---|
| البرنامج يفتح | 🔴 **لا** | ✅ نعم |
| عدد مجموعات الفحص | ٤٣ | **٤٥** |
| فحوص فاشلة | ٠ (لكنها كاذبة) | **٠ (صادقة)** |
| اختبار الطفرات | ٥٥/٥٥ | **٥٥/٥٥ ✅** |
| الفحص العشوائي (fuzz) | ٣٩/٤٠ | **٣٩/٤٠ ✅ (لا تراجع)** |
| `npm run verify` | ٠ | **٠ ✅** |

البذرة ٢٩ (`seed 29`) ما زالت مفتوحة كما كانت — لم تتغير، ولم يحدث أي تراجع.

---

## ٧. ✅ حالة الدفع إلى GitHub

تم الدفع بنجاح إلى الفرعين:

| الفرع | الكوميت |
|---|---|
| `master` | `e7fbe6f` ✅ |
| `arena/019fa15d-mobileshop` | `e7fbe6f` ✅ |
