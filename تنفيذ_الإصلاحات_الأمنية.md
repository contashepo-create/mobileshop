# 🔐 تنفيذ الإصلاحات الأمنية — المرحلة الأولى

**التاريخ:** ٣١ يوليو ٢٠٢٦
**النطاق:** النقاط الحرجة ١، ٢، ٣، ٤، ٩ من تقرير الفحص + النقطة ٢٣ (تحذير)

---

## ١. 🔴 الأخطر: نظام الترخيص — من HMAC إلى Ed25519

### المشكلة
مفتاح التوقيع = مفتاح التحقق = **قيمة مشحونة داخل التطبيق**. أثبتُّ سابقاً أن
توليد ترخيص أبدي يستغرق ثوانٍ.

### الحل المُنفَّذ: **تشفير غير متماثل**

| | قبل | بعد |
|---|---|---|
| الخوارزمية | HMAC-SHA256 (متماثل) | **Ed25519** (غير متماثل) |
| مفتاح التوقيع | مشحون في التطبيق 🔴 | **عندك فقط، لا يُشحن** ✅ |
| مفتاح التحقق | نفس المفتاح 🔴 | **مفتاح عام — يتحقق ولا يوقّع** ✅ |
| هل يمكن التزوير؟ | 🔴 نعم في ثوانٍ | ✅ **مستحيل رياضياً** |

### الإثبات
```
التوقيع بالمفتاح العام المشحون : يفشل ✅
كود من مفتاح خاص مختلف        : مرفوض ✅
تمديد تاريخ الانتهاء            : مرفوض ✅
تغيير بايت واحد من التوقيع      : مرفوض ✅
الكود على جهاز آخر              : مرفوض ✅
```

### ⚠️ نتيجة جانبية: طول الكود
توقيع Ed25519 **لا يمكن تقصيره** (أثبتُّ ذلك قبل اعتماد التصميم). فالكود صار
**~١٣٨ حرفاً** بدل ١٦.

**ما فعلته:** غيّرت خانة الإدخال في شاشة التفعيل إلى مربع نص متعدد الأسطر مع
**زر «لصق من الحافظة»**. ترسل الكود نصاً على واتساب/تليجرام والعميل يلصقه.

### 🛟 التوافق مع الأكواد القديمة
**الأكواد المُصدَرة سابقاً ما زالت تعمل.** أبقيت مسار التحقق القديم موسوماً
`LEGACY`. السبب: رفضها يعني إيقاف عملاء دافعين بين ليلة وضحاها — وهذا أسوأ من
صيغة مكشوفة أصلاً.

**التوجيه بالطول:** ٦٩ بايت ← Ed25519، ١٠ بايت بالضبط ← القديم. فلا يمكن تمرير
كود جديد عبر الباب الضعيف، ولا العكس.

---

## ٢. 🔴 ثغرة إعادة الفترة التجريبية

### المشكلة
كل ملفات الحالة في مجلد واحد (`userData`). حذف الملف وحده مكتشَف، لكن **حذف
المجلد كله** يمحو الشاهد والمشهود عليه معاً ← تجربة جديدة بلا حدود.

### الحل: **مرساة خارج مجلد البرنامج**
`src/main/security/trialAnchor.ts` يكتب علامة موقّعة في **٣ أماكن مختلفة**:

| # | المكان (ويندوز) |
|---|---|
| ١ | `C:\Users\<اسمك>\.mobileshop-trial` |
| ٢ | `C:\ProgramData\MobileShopERP\.mobileshop-trial` |
| ٣ | `%LOCALAPPDATA%\MobileShopERP\.mobileshop-trial` |

**الخصائص:**
- موقّعة بـ HMAC ← تعديل التاريخ يُبطلها
- مربوطة بالجهاز ← لا يمكن نقل علامة مزيفة من جهاز آخر
- **تُداوي نفسها** ← حذف نسختين لا يفيد بعد أول تشغيل
- **الأقدم يفوز** ← لا يمكن إعادة ضبط الساعة بحذف بعضها

### 🎁 جانب عادل مقصود
لو مسح برنامج تنظيف مجلدك بالخطأ، **لا يُعاقَب**: البرنامج يستعيد تاريخ البداية
الأصلي ويعطيه ما تبقّى له فعلاً من أيام.

### الإثبات
```
حذف مجلد userData     : المرساة باقية ✅
حذف نسختين من ٣       : المرساة باقية + تُداوي نفسها ✅
تعديل التاريخ (أقدم)   : مُتجاهَل ✅
علامة من جهاز آخر      : مُتجاهَلة ✅
```

> **بصراحة:** هذا ليس حصيناً تماماً — لا شيء يعمل على جهاز العميل كذلك.
> لكنه يحوّل «احذف مجلداً واحداً» (وهو ما يقترحه أي ذكاء اصطناعي فوراً) إلى
> بحث متعمّد لن يقع فيه صاحب محل عادي بالصدفة.

---

## ٣. 🔴 كلمة مرور المطور

**المشكلة:** ٦ أرقام، وتجزئتها مشحونة في كل نسخة. مليون احتمال فقط — تُكسر
بلا اتصال في دقائق. الحد الزمني في البرنامج **لا يحمي** من هجوم على التجزئة
المستخرَجة.

**الحل:** أداة `scripts/dev-password.js` ترفض أي كلمة أقل من ١٢ حرفاً أو
أرقاماً فقط.

```powershell
node scripts/dev-password.js "عبارة طويلة من عدة كلمات"
```
ثم الصق السطر المطبوع في `src/main/security/devAuth.ts`.

> ⚠️ **لم أغيّر كلمتك بنفسي** — هي قرارك وحدك، ولو غيّرتها دون علمك لفقدت
> الوصول لوحدة التحكم. **نفّذ الأمر أعلاه بنفسك.**

---

## ٤. 🟠 «المزامنة السحابية» — تحذير صريح

فحصت الكود: يوجد **رفع فقط**، لا تنزيل ولا دمج. عميل يظن أن جهازيه متزامنان
سيعمل على الاثنين ثم يكتشف أن آخر رفع **داس** على الآخر.

**ما فعلته:** أضفت تحذيراً بارزاً في الشاشة نفسها يوضح أنها **نسخة احتياطية
باتجاه واحد وليست مزامنة**، ويوجّه من يريد العمل من جهازين إلى **قاعدة على
مجلد شبكة**.

---

## ٥. 🧪 التحقق

| المجموعة | الفحوص | الطفرات |
|---|---|---|
| `verify_license_ed25519.mjs` | **٣٦** | **٦/٦ اكتُشفت** |
| `verify_trial_anchor.mjs` | **٢٧** | **٦/٦ اكتُشفت** |

### 🔴 ثغرتان في اختباراتي أنا اكتشفهما حقن الأعطال

**الأولى — الأخطر:** عطل «أعد التوجيه إلى HMAC» (أي إعادة الثغرة الأصلية)
**نجا**. السبب: كل فحوصي كانت توقّع بمفتاح مؤقت، فتثبت مقاومة التزوير لكن
**لا تثبت أن كوداً حقيقياً ما زال يعمل**. وكسر ذلك = توقّف كل عميل دافع.
أضفت `__setPublicKeyForTests` لأقود الموجّه الحقيقي بكود يثق به فعلاً.

**الثانية:** فحص «تعديل تاريخ المرساة» استخدم تاريخاً **أحدث**، وقاعدة «الأقدم
يفوز» كانت ترفضه وحدها — فحذف فحص التوقيع بالكامل ظل ناجحاً. غيّرته لتاريخ
**أقدم** فصار العطل يُكتشف.

> **المبدأ:** اختبار لا يفشل عند تخريب الحماية ليس اختباراً. لم أقبل ناجياً
> واحداً دون تحقيق.

**وأصلحت فحصاً قديماً** في `verify_licensing.js` كان يشترط استخدام HMAC —
وهو ما أزلته. ذلك الملف كان **يعيد كتابة التشفير محلياً ويختبر نسخته هو**،
وهذا بالضبط سبب بقاء الثغرة الأصلية مخفية طويلاً.

---

## 🔻 المطلوب منك — بالترتيب

### ١) ولّد مفتاحك الخاص (مهم جداً)
```powershell
cd "D:\programing\coding projects\mobile shop"
node scripts/license-keygen.js --init
```
سيطبع سطراً يبدأ بـ `export const LICENSE_PUBLIC_KEY`. **الصقه في**
`src/main/security/licenseCrypto.ts` مكان السطر الموجود.

> 🔑 المفتاح الخاص يُحفظ في `scripts/.license-key` وهو **مستبعد من Git**.
> **احتفظ بنسخة منه في مدير كلمات مرور** — فقدانه يعني إعادة إصدار كل التراخيص.

### ٢) غيّر كلمة مرور المطور
```powershell
node scripts/dev-password.js "عبارة طويلة تختارها أنت"
```
والصق السطر في `src/main/security/devAuth.ts`.

### ٣) اضبط الخادم على المفتاح الجديد
```powershell
cd server
npx wrangler secret put LICENSE_PRIVATE_KEY
```
والصق **المفتاح الخاص** (محتوى `scripts/.license-key`).

### ٤) شغّل وتأكد
```powershell
cd ..
npm run verify
npm start
```

---

## ⏭️ المتبقي من التقرير

النقاط الباقية (٥ الجزئية، ٨، ١٠، ١٢، ١٤، ١٥–٢٢) أقل خطورة وسأنفّذها في
المرحلة التالية: بصمة جهاز مستقرة، إتاحة التصدير بعد الانتهاء، «طُبع بواسطة»،
حقل الشعار، إعدادات الطباعة والفاتورة، بيانات التسجيل، وتأكيد الهاتف.
