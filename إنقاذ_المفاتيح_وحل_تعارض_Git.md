# 🔑 إنقاذ مفاتيحك وحل تعارض Git

**التاريخ:** ٣١ يوليو ٢٠٢٦

---

## ما حدث — وهذه نتيجة جيدة وليست سيئة

```
error: Your local changes to the following files would be overwritten by merge:
        src/main/security/devAuth.ts
        src/main/security/licenseCrypto.ts
```

**Git رفض المتابعة لأنه اكتشف أن تعديلاتك ستُمحى.** هذا بالضبط ما حذّرتك منه،
وهو **يحميك الآن**.

> 🔴 **الخطر الحقيقي:** لو بحثت في الإنترنت عن حل، ستجد نصيحة شائعة:
> `git checkout -- .` أو `git reset --hard`.
>
> **لا تفعل ذلك.** هذان الأمران **يمحوان مفتاح الترخيص الخاص بك نهائياً**،
> وكل كود تفعيل أصدرته لعميل سيتوقف — لأنه موقّع بالنصف الخاص من ذلك المفتاح.

---

## ✅ الحل — بالترتيب، لا تتخطَّ خطوة

### الخطوة ١: أنقذ مفاتيحك أولاً

**اسحب أداة الإنقاذ دون أن تلمس ملفاتك:**

```powershell
cd "D:\programing\coding projects\mobile shop"
git fetch origin master
git checkout FETCH_HEAD -- scripts/rescue-keys.js
```

> هذا الأمر يجلب **ملفاً واحداً فقط** ولا يمسّ تعديلاتك.

### الخطوة ٢: اعرض ما سيُنقَذ

```powershell
node scripts/rescue-keys.js
```

سيعرض المفاتيح التي وجدها في ملفاتك (مختصرة للأمان). **تأكد أنها مفاتيحك.**

### الخطوة ٣: احفظها في `.env`

```powershell
node scripts/rescue-keys.js --write
```

يُنشئ أو يُحدّث `.env` — **ويحافظ على أي قيم موجودة فيه**.

### الخطوة ٤: الآن فقط، أرجِع الملفات واسحب

```powershell
git checkout -- src/main/security/devAuth.ts src/main/security/licenseCrypto.ts
git pull origin master
```

### الخطوة ٥: أكمل `.env`

افتح `.env` وتأكد أنه يحوي الأربعة:

```
MOBILESHOP_LICENSE_PUBLIC_KEY=<أُنقِذ تلقائياً>
MOBILESHOP_DEV_PASSWORD_HASH=<أُنقِذ تلقائياً>
MOBILESHOP_API_BASE=https://mobileshop-licensing.mobileshop2026.workers.dev
MOBILESHOP_CLIENT_KEY=uCubrNqc4TLTophMzNdx2zvBG2u3NypeMo3mbxDazOU=
```

### الخطوة ٦: تأكد

```powershell
npm run verify
npm start
```

---

## ⚠️ إن لم تجد الأداة أي مفاتيح

معناه أنك لم تلصق شيئاً بعد، أو أن التعديل ضاع. الحل:

```powershell
npm run license:init
npm run dev:password -- "عدة كلمات غير مترابطة"
```
وضع الناتج في `.env` مباشرة — **لا في ملفات الكود**.

---

## لماذا حدث هذا أصلاً؟

**السبب خطأ في تعليماتي.** طلبت منك اللصق في ملفين **يتتبعهما Git**، وهذا يعني
أن كل `git pull` إما يتعارض (كما حدث) أو **يمحو تعديلك بصمت**.

| ما يُمحى | الأثر |
|---|---|
| كلمة مرور المطور | إزعاج — تعيد توليدها |
| **مفتاح الترخيص** | 🔴 **كل عميل فعّلته يتوقف** |

والأسوأ أن العطل يظهر **لاحقاً عند العميل** لا عندك.

**أصلحت السبب:** القيمتان تُقرآن الآن من `.env` (المستبعد من Git أصلاً)،
فتنجوان من كل تحديث ولا تصلان لأي كوميت.

---

## ما تحققت منه قبل كتابة هذا الدليل

| الحالة | النتيجة |
|---|---|
| ملفات فيها مفاتيحك (وضعك الحالي) | ✅ استُخرجت وحُفظت |
| ملفات لم تُعدَّل | ✅ «لا يوجد شيء» — بلا إنذار كاذب |
| `.env` يحوي مفاتيح API مسبقاً | ✅ **دُمجت ولم تُمحَ** |
| القيم الافتراضية للمستودع | ✅ تُتجاهَل، لا تُنقَذ كأنها سر |

---

## 🛡 لماذا لن يتكرر هذا

أضفت حارساً: النسخة المُصدَّرة **ترفض العمل** بمفاتيح التطوير وتخبرك بالمتغيّر
الناقص. فحتى لو نسيت `.env` يوماً، لن تُشحن نسخة يستطيع أي عميل تزوير ترخيصها.
