# تقرير: التحقق من المدخلات في الخادم — لا تثق أبداً بما يرسله العميل

**التاريخ:** 5 أغسطس 2026
**المالك:** Acc. Mohamed Abdou
**النطاق:** كل قنوات IPC الـ 250، وكل شاشة إدخال في البرنامج

---

## ١. الخلاصة في سطرين

الشاشات ترفض أشياء كثيرة قبل الإرسال. **الخادم كان يقبلها كلها.**
تم فحص 250 قناة، وقياس 11 ثغرة حقيقية على الكود الفعلي، وإصلاحها جميعاً، وبناء 175 اختباراً يمنع رجوعها.

| المؤشر | القيمة |
|---|---|
| قنوات IPC مفحوصة | 250 |
| حواجز موجودة في الشاشات فقط | 106 |
| ثغرات مقيسة فعلياً (لا مُفترضة) | 11 |
| ملفات مُعدَّلة | 11 |
| اختبارات جديدة | **175 ✅** |
| طفرات محقونة / مُكتشَفة | **57 / 56** (واحدة مكافئة موثّقة) |

---

## ٢. لماذا هذا مهم أصلاً

الشاشة ليست حاجزاً أمنياً. أي شيء تستطيع الشاشة استدعاءه، يستطيع أي شخص استدعاءه من
`DevTools` على جهاز المحل نفسه — بسطر واحد:

```js
window.api.invoke('vouchers:create', { VoucherType:'RECEIPT', Amount:5000, ... })
```

وأخطر من ذلك: نوافذ الطباعة التي تُفتح بـ `window.open` **ترث نفس صلاحيات النافذة الأم**،
أي أن نصاً مزروعاً داخل اسم عميل يستطيع الوصول لكل القنوات الـ 250.

---

## ٣. الثغرتان الحرجتان 🔴

### 🔴 ١) سند بمبلغ يخرج من الخزنة ولا يظهر في أي تقرير

`vouchers:create` لم يكن يتحقق من **نوع السند** إطلاقاً. والكود يقول:

```ts
const sign = data.VoucherType === 'receipt' ? 1 : -1;
```

أي أن أي كلمة غير `receipt` بالضبط — **بما فيها `RECEIPT` بحروف كبيرة** — تسقط على `-1`.

**ما تم قياسه فعلياً على الكود الحقيقي:**

| الخطوة | النتيجة المقاسة |
|---|---|
| الخزنة قبل العملية | 10,000 |
| إرسال سند `VoucherType: "RECEIPT"` بمبلغ 5,000 | ✅ قُبل |
| الخزنة بعد العملية | **5,000** (خرج المبلغ!) |
| المستند مكتوب عليه | «سند **قبض**» (أي دخول أموال) |
| السندات التي تراها التقارير | **0 من 1** |
| مصروفات قائمة الدخل | **0** |

> **٥٠٠٠ جنيه خرجت من الخزنة، والمستند يقول عكس ذلك، ولا يوجد تقرير واحد في البرنامج
> يستطيع أن يشرح أين ذهبت.**

السبب أن كل التقارير والكشوف تبحث بـ `WHERE VoucherType = 'receipt'` أو `= 'payment'` —
فالصف لم يطابق أياً منهما وأصبح **غير مرئي تماماً**.

**الإصلاح:** قائمة سماح صارمة `['receipt','payment']`، بمطابقة حرفية دقيقة.

---

### 🔴 ٢) جهاز يُسلَّم للعميل ولا يمكن تحصيل قيمته أبداً

`maintenance:updateStatus` كان يقبل **أي نص** كحالة للتذكرة — بما فيها `delivered`.

و`maintenance:deliver` (وهو الوحيد الذي يُصدر الفاتورة ويحصّل النقود ويحسب عمولة الفني)
يرفض أي تذكرة حالتها `delivered` بالفعل.

**ما تم قياسه فعلياً:**

| الخطوة | النتيجة المقاسة |
|---|---|
| تذكرة صيانة بقيمة متفق عليها | 800 جنيه |
| `updateStatus(ticket, 'delivered', ...)` | ✅ قُبل — الحالة أصبحت «تم التسليم» |
| ثم `maintenance:deliver` (الفوترة الحقيقية) | ❌ «تم تسليم هذه التذكرة بالفعل» |
| عدد عمليات التسليم المسجَّلة | **0** |
| النقدية في الخزنة | **10,000 — لم تتغير** |
| رصيد العميل | **0** |

> **الجهاز تم إصلاحه وخرج من المحل، و٨٠٠ جنيه لن يستطيع المحل تحصيلها أبداً.**

**الإصلاح:**
- `updateStatus` مقصور على حالات الورشة: `received`, `inspecting`, `in_progress`, `ready`
- الحالات النهائية (`delivered`, `cancelled`, `returned`) لا تُبلَغ إلا عبر أزرارها الخاصة التي تُسجّل الفاتورة والمبالغ معها
- تذكرة مُسلَّمة أو ملغاة لا يمكن إعادة فتحها (وإلا أمكن تسليمها وفوترتها مرتين)

---

## ٤. باقي الثغرات المقيسة 🟠

| # | القناة | ما كان يُقبَل — **مقاس فعلياً** |
|---|---|---|
| ٣ | `settings:set` / `setMany` | كتابة `db_path`, `cloud_api_key`, `telegram_bot_token`, `setup_completed`, `owner_capital` — وكلها القراءة منها **ممنوعة** أصلاً! الباب مغلق والشباك مفتوح |
| ٤ | `customers/suppliers:create` | اسم فارغ `""`، واسم من ٨ مسافات، واسم بطول **٥ ملايين حرف** |
| ٥ | `customers/suppliers:updateStatus` | حالة `"GOD_MODE"` — وفحص حظر العميل يبحث عن `'suspended'`، فأي قيمة أخرى تعني «غير محظور» |
| ٦ | `users:create` | `kareem` و`kareem ` و`KAREEM` تتعايش كـ **٣ حسابات** لا يفرّق بينها إنسان — وإلغاء واحد يترك بابين خلفيين |
| ٧ | `employees:create` | راتب أساسي **-99,999**، وتاريخ تعيين `"not-a-date"`، وتاريخ `2026-02-31` |
| ٨ | `items:create` | نوع صنف `"WEAPON"`، و`IsSerialized: 99` (تعني «مُرقَّم» لفرع و«غير مُرقَّم» لفرع آخر) |
| ٩ | `serviceSales:create` | نوع خدمة `"ALIEN"` يُطبع للمستخدم كما هو، ورقم وجهة فارغ لتحويل رصيد |
| ١٠ | `advances/deductions:create` | موظف غير موجود → خطأ `FOREIGN KEY` خام يظهر كـ «تعذّر تنفيذ العملية» بلا تحديد الحقل |
| ١١ | كل البحث (`items/customers/suppliers:list`) | البحث بـ `%` يُرجع **كل** الجدول متجاوزاً كل الفلاتر؛ و٦٠٠٠٠ حرف يُسقط SQLite نفسه |
| ١٢ | كل حقول النص (70 معالجاً) | لا يوجد **أي** حد للطول: وصف سند بـ ١ ميجابايت، وقيمة إعداد بـ ٢ ميجابايت |

---

## ٥. قائمة القنوات المُعدَّلة

| الملف | القنوات |
|---|---|
| `vouchers.handlers.ts` | `vouchers:create` |
| `maintenance.handlers.ts` | `maintenance:updateStatus` |
| `hr.handlers.ts` | `customers:create/update/updateStatus/list`, `suppliers:create/update/updateStatus/list`, `employees:create/update` |
| `inventory.handlers.ts` | `items:create/update/quickCreate/list`, `warehouses:create/update/delete`, `categories:create/update` |
| `assets.handlers.ts` | `cashAccounts:create/update`, `paymentMethods:create/update` |
| `users.handlers.ts` | `users:create/update`, `roles:create/update`, `permissions:setForRole/setOverride/removeOverride` |
| `settings.handlers.ts` | `settings:set`, `settings:setMany` |
| `services.handlers.ts` | `serviceSales:create` |
| `payroll.handlers.ts` | `advances:create`, `deductions:create` |

**ملف جديد:** `src/shared/validate.ts` — مرجع واحد لكل قواعد التحقق.

---

## ٦. أخطاء ارتكبتُها أنا واكتشفها الفحص 🟠

هذا القسم مهم لأنه يثبت أن الاختبارات تعمل فعلاً:

**١) كتبتُ قوائم سماح من التخمين بدل قراءة الشاشة الحقيقية.**
ثلاث قوائم كانت **خاطئة تماماً** وكانت ستُعطّل ميزات تعمل:

| القائمة | تخميني الخاطئ | الصحيح من الشاشة |
|---|---|---|
| نوع الخزنة | `cash`, `bank` | **`safe`**, `bank` |
| نوع المخزن | `main`, `branch`, `maintenance`, `damaged` | `main`, `maintenance`, **`other`** |
| نوع طريقة الدفع | `wallet`, `bank`, `instapay`, `other` | **`pos_machine`, `digital_wallet`, `transfer`** |
| سبب الخصم | `damage`, `absence`, `late`, `penalty`, `other` | `absence`, **`negligence`**, `damage`, `other` |

> لو مرَّت هذه، لما استطاع المحل إنشاء **أي خزنة** أو **أي طريقة دفع**.
> اكتشفها اختبار قديم (`verify_master_data.mjs`) عند تشغيل الحزمة كاملة — وهذا بالضبط سبب
> تشغيل كل الاختبارات وليس الجديد فقط.

**٢) أدخلتُ خطأً جديداً في `permissions:setForRole`.**
قياس بـ `[1, 2, 1]` أعطى `UNIQUE constraint failed` من داخل الحلقة — وكان الدور سيفقد
**كل** صلاحياته. أُصلح بإزالة التكرار قبل الكتابة.

**٣) كنتُ أحذف المسافات داخل اسم المستخدم بصمت.**
`"ahmed ali"` كان يُحفظ `"ahmedali"` — نص مختلف عمّا كتبه المدير وعمّا سيحاول الدخول به.
الرفض الصريح أفضل من التعديل الصامت.

---

## ٧. ما لم أفعله عمداً

**لم أُهرِّب HTML عند الإدخال.** التهريب مكانه عند **الإخراج** (`src/shared/escapeHtml.ts`
مُستخدَم بالفعل في كل مسارات الطباعة). لو هرَّبتُ عند الإدخال، لأصبح اسم محل حقيقي مثل
«محل الأخوة & أولاده» مخزَّناً كـ `&amp;` ويُطبع هكذا للعميل إلى الأبد.

ما يُزال فعلاً هو ما لا مكان له في سجل تجاري أصلاً: رموز التحكم، ومنها
`U+202E` التي تعكس اتجاه العرض — وفي برنامج عربي هي **غير مرئية** للقارئ وتستطيع جعل
رقم على الفاتورة يُعرض كرقم آخر.

**لم أمنع مفاتيح `allow_negative_*`.** هي خطيرة، لكنها **أربعة مربعات اختيار حقيقية**
في شاشة الإعدادات العامة يملكها صاحب المحل. منعها كان سيُعطّل ميزة تعمل لسدّ ثغرة
تحكمها الصلاحيات أصلاً. بدلاً من ذلك: قيمتها الآن **`0` أو `1` فقط**.

---

## ٨. حالة الاختبارات

```
verify_server_side_validation.mjs .......... 175 / 175 ✅
اختبار الطفرات ............................. 56 / 57 مكتشفة
   الناجية الوحيدة: طفرة مكافئة موثّقة (لا يمكن لأي اختبار تمييزها)
verify_master_data.mjs ..................... 17 / 17 ✅ (بعد إصلاح تجهيزة خاطئة)
npm run verify ............................. قيد التشغيل
```

كل اختبار سلبي مقترن باختبار إيجابي: أي إصلاح «يرفض كل شيء» يسقط هنا أيضاً — لأن
مُتحقِّقاً يُعطّل المحل ليس إصلاحاً.

---

## 🔻 الأوامر — انسخها والصقها كما هي، أمراً واحداً في كل مرة

### الأمر ١ — افتح مجلد المشروع

```powershell
cd "D:\programing\coding projects\mobile shop"
```

### الأمر ٢ — اسحب التعديلات

```powershell
git pull origin master
```

### الأمر ٣ — شغّل البرنامج للتجربة

```powershell
npm run dev
```

**جرّب هذه بنفسك للتأكد أن كل شيء يعمل:**
- أضف عميلاً جديداً باسم عادي ← يجب أن ينجح ✅
- حاول إضافة عميل باسم فارغ ← يجب أن يرفض ويقول «اسم العميل مطلوب» ✅
- أنشئ خزنة جديدة ← يجب أن تنجح ✅
- أنشئ طريقة دفع جديدة ← يجب أن تنجح ✅
- اعمل سند قبض وسند صرف ← يجب أن ينجحا وتتحرك الخزنة في الاتجاه الصحيح ✅
- استلم جهاز صيانة وحرّك حالته ثم سلّمه ← يجب أن ينجح ويُحصِّل المبلغ ✅

---

## ⚠️ ما زال مطلوباً منك (من تقارير سابقة)

| # | المطلوب | الأمر |
|---|---|---|
| ١ | إلغاء توكن بوت تليجرام (مكشوف في تاريخ Git) | من BotFather: `/revoke` ثم `cd server` و `npx wrangler secret put TG_BOT_TOKEN` |
| ٢ | توليد مفاتيح ترخيص جديدة | `npm run license:init` |
| ٣ | نشر إصلاح عزل البيانات على Cloudflare | `cd server` ثم `npx wrangler deploy` |
| ٤ | ضبط معرّفات تليجرام | `npx wrangler secret put TG_ADMIN_CHAT` ← `7232305465,1593943219` |
| ٥ | لو شغّلت الإعداد باسم غير `admin` | حساب `admin`/`admin123` ما زال حيّاً — عطّله يدوياً أو صفّر القاعدة |
