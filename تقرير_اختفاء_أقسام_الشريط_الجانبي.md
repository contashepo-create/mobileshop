# تقرير: لماذا اختفت الأقسام من الشريط الجانبي — والإصلاح النهائي

**التاريخ:** ٣ أغسطس ٢٠٢٦
**الحالة:** ✅ تم الإصلاح والرفع — `830088b` على `master` و `arena/019fa15d-mobileshop`

---

## ١. الخلاصة في سطرين

الأقسام الخمسة **كانت موجودة فعلاً** في الإعدادات، لكن **ملف الرسم لم يكن يعرفها**، فتخطّاها بصمت.
النتيجة: الشريط رسم **٤ أقسام من ١٠**، واختفت **١٥ شاشة** من البرنامج دفعة واحدة.

---

## ٢. السبب الجذري — ولماذا تكرر ٣ مرات

القائمة كانت مكتوبة في **٤ أماكن منفصلة**، كل واحد يجب أن يطابق الثلاثة الآخرين، **ولا شيء يتحقق من ذلك**:

| # | الملف | ماذا يحدد |
|---|---|---|
| ١ | `sidebar.store.ts` → `defaultMainOrder` | ما هي الأقسام وما بداخلها |
| ٢ | `Sidebar.tsx` → `defaultItems` | **الأيقونة والاسم عند الرسم** |
| ٣ | `SidebarSettings.tsx` → `defaultSections` | ما يستطيع العميل ترتيبه |
| ٤ | `sidebar.store.ts` → `getDisplayLabel` | نسخة رابعة من الأسماء |

في التعديل السابق (`08131c7`) عدّلتُ **رقم ١ فقط**. أما `Sidebar.tsx` فظل يصف قسم `الحسابات` الواحد، فلم يتعرّف على أي من الخمسة، وهذا السطر ابتلعها:

```ts
if (!item) continue;   // ← يبدو كوداً دفاعياً عادياً... وهو يحذف قسماً كاملاً
```

**هذا هو مربط الفرس:** `continue` على اسم غير معروف لا يُفرّق عن كود دفاعي سليم. لا خطأ، لا تحذير، لا شيء.

### لماذا ليست أول مرة

| المرة | الشاشة | ماذا حدث |
|---|---|---|
| ١ | `/accounting/rent-parties` (`24bded2`) | أُضيفت للإعدادات فقط — **لم تظهر ولا مرة منذ إنشائها** |
| ٢ | الأقسام الخمسة (`08131c7`) | أُضيفت للإعدادات فقط — ١١ شاشة اختفت |
| ٣ | شاشات المرتجعات والسندات | نفس السبب |

---

## ٣. القياس الفعلي (لم أعتمد على القراءة)

شغّلت دالة الرسم الحقيقية `buildOrderedMenu` وطبعت نتيجتها:

```
الإعدادات تقول:  لوحة التحكم | المبيعات | المشتريات | السندات... | السنة المالية... |
                 المخازن... | الموارد البشرية | الأصول | التقارير | الإعدادات   (١٠)

الشريط رسم فعلاً: لوحة التحكم | الموارد البشرية | الأصول | التقارير | الإعدادات   (٥)

🔴 اختفى: المبيعات، المشتريات، السندات والرواتب والإيجارات،
          السنة المالية والأرصدة الافتتاحية، المخازن والتسوية الجردية
```

وللعميل الذي رتّب شريطه سابقاً، ضاعت **١٥ شاشة**: المبيعات، المشتريات، الصيانة، الرواتب، الإيجارات، تحويل وشحن، السنة المالية، التسوية الجردية، الأرصدة الافتتاحية، المخازن، والمرتجعات والسندات الجديدة.

---

## ٤. الإصلاح: قائمة واحدة فقط

أنشأت **`src/renderer/src/lib/navCatalog.ts`** — مصدر واحد لكل شاشات البرنامج.
الملفات الثلاثة تقرأ منه الآن، فحالة «الإعدادات تعرف قسماً والرسم لا يعرفه» **لم تعد موجودة أصلاً**.

إضافة شاشة جديدة = **سطر واحد** في هذا الملف. والفحص يرفض البناء إذا وُجد مسار بلا ذكر فيه.

### الترتيب الافتراضي (كما طلبت) ✅

| # | القسم | ما بداخله |
|---|---|---|
| ١ | لوحة التحكم | *(رابط مباشر)* |
| ٢ | **المبيعات** | مبيعات، تحويل وشحن، صيانة، مرتجعات المبيعات |
| ٣ | **المشتريات** | مشتريات، مرتجعات المشتريات |
| ٤ | **السندات والرواتب والإيجارات** | سندات القبض، سندات الصرف، رواتب وسلف، إيجارات، المؤجرون والمستأجرون |
| ٥ | **السنة المالية والأرصدة الافتتاحية** | السنة المالية، الأرصدة الافتتاحية |
| ٦ | **المخازن والتسوية الجردية** | المخازن والأصناف، التسوية الجردية |
| ٧ | الموارد البشرية | الموظفين، العملاء، الموردين |
| ٨ | الأصول | البنوك والخزائن، ماكينات الدفع، تحويلات بين الحسابات |
| ٩ | التقارير | التقارير العامة، كشف عميل، كشف مورد، كشف موظف |
| ١٠ | الإعدادات | الإعدادات العامة، قاعدة البيانات، الترخيص، النسخ الاحتياطي، حول البرنامج |

**وهو افتراضي فقط** — العميل يستطيع: إعادة الترتيب ⬆⬇، إعادة التسمية ✏، الترقية لقسم رئيسي ⭐، إنشاء أقسام خاصة 📁، النقل بينها، والعودة للافتراضي 🔄. كل ذلك مُثبت بالاختبار ويبقى بعد إغلاق البرنامج.

---

## ٥. أعطال أخرى اكتشفتها أثناء الفحص

| # | العطل | الخطورة | الحالة |
|---|---|---|---|
| ١ | تخطّي أي قسم لا يعرفه ملف الرسم | 🔴 | ✅ |
| ٢ | التخطيط المحفوظ **يفقد كل شاشة أُضيفت بعد حفظه** | 🔴 | ✅ |
| ٣ | حذف قسم خاص **يحذف الشاشات بداخله نهائياً** | 🔴 | ✅ |
| ٤ | شاشة قديمة داخل القسم المقسوم تُحذف مع اسمها | 🟠 | ✅ |
| ٥ | شاشة مُرقّاة **تتكرر مع كل تشغيل** | 🟠 | ✅ |
| ٦ | «إعادة تعيين» يشارك مصفوفات الافتراضي فيتلفها | 🟠 | ✅ |
| ٧ | شاشة منقولة أو قديمة **لا يمكن إعادة تسميتها** | 🟠 | ✅ |
| ٨ | `localStorage` تالف **يمسح كل ترتيب العميل** أو يُسقط الشريط | 🟠 | ✅ |
| ٩ | قسم فارغ لا يرسم شيئاً (نقرة ميتة) | 🟢 | ✅ |
| ١٠ | تكرار نفس المسار يرسمه مرتين | 🟢 | ✅ |

---

## ٦. لماذا لم يكتشفها الفحص السابق؟

`verify_sidebar_sections.mjs` كان **يقرأ** الإعدادات بتعبير نمطي ويتأكد أن الخمسة بداخلها. وكانت بداخلها فعلاً — **فنجح الفحص بينما الشريط مكسور**.

لم يُشغّل `buildOrderedMenu` ولا مرة، فأثبت **الإعدادات** لا **الشريط** — وهي بالضبط الفجوة التي عاش فيها العطل.

**الفحص الجديد `verify_sidebar_nav.mjs` يبني الملفات الحقيقية ويستدعيها:**

- **١٠٢ فحص، ٠ فشل**
- **٣٦ عطلاً مزروعاً — كلها ضُبطت (٣٦/٣٦)**، أولها العطل الأصلي نفسه
- الفحص الشامل: **٦٤ مجموعة، ٠ فشل** | طفرات التجارة **٥٥/٥٥** | العشوائي **٣٩/٤٠**

---

## 🔻 الخطوات على جهازك

انسخ الأوامر التالية **سطراً سطراً** في PowerShell:

**١) اذهب لمجلد المشروع:**
```powershell
cd "D:\programing\coding projects\mobile shop"
```

**٢) اسحب التعديل الجديد:**
```powershell
git pull origin master
```

**٣) شغّل البرنامج:**
```powershell
npm run dev
```

**٤) إن لم تظهر الأقسام فوراً** (بسبب ترتيب قديم محفوظ) — اضغط `F12` داخل البرنامج، والصق هذا في الـ Console ثم Enter:
```
localStorage.removeItem('sidebarConfig'); location.reload();
```

> ملاحظة: الخطوة ٤ ليست ضرورية غالباً — الإصلاح يُرقّي التخطيط القديم تلقائياً ويعيد كل شاشة مفقودة. لكنها تعطيك الترتيب الافتراضي نظيفاً إن أردت.
