# تقرير إصلاح: أخطاء الكونسول والشاشة البيضاء

**التاريخ:** 2026-07-30
**المالك:** Acc. Mohamed Abdou
**الخطأ المُبلَّغ عنه:** `DataTable.tsx:43 Uncaught TypeError: Cannot read properties of undefined (reading 'length')`

---

## 1. خطورة المشكلة (وليست مجرد رسالة في الكونسول)

المشكلة **ليست خطأً شكلياً**. تم التحقق برمجياً من أن التطبيق **لا يحتوي على أي `Error Boundary`** في شجرة المكوّنات كلها:

```
NOTE  error boundary present: NO — a render throw still unmounts the app
```

سلوك React الموثّق عند حدوث استثناء أثناء الرسم دون وجود `Error Boundary` هو **إزالة الجذر بالكامل** — أي أن النتيجة على شاشة المحل هي **نافذة بيضاء فارغة**، وليست جدولاً معطوباً. وهذا ما يفسّر السطر الأخير في الكونسول:

> `Consider adding an error boundary to your tree...`

---

## 2. السببان الجذريان (تم إثبات كل منهما عملياً)

### 🔴 السبب الأول — بيانات تقرير قديم تُرسم على تبويب جديد

`ReportsPage` يحتفظ بحالة واحدة `data` لتسعة تقارير مختلفة **أشكالها غير متوافقة**:

| التقرير | يُرجع `rows`؟ | يُرجع `totals`؟ |
|---|---|---|
| المبيعات / المشتريات / الصيانة | ✅ | ✅ |
| الموظفين | ✅ | ❌ |
| الأرباح والخسائر | ❌ | ❌ |
| المركز المالي | ❌ | ❌ |

الضغط على تبويب يُعيد الرسم **فوراً**، بينما إعادة الجلب تحدث **بعد ذلك** داخل `useEffect`. في تلك الرسمة الواحدة تُرسم بيانات التقرير **السابق** على واجهة التقرير **الجديد**، فتصبح `data.rows` قيمتها `undefined`.

تم إعادة إنتاج الخطأ الحرفي بمحاكاة ترتيب الرسم:

```
user is on الأرباح والخسائر, then clicks تقرير المبيعات
CRASH -> TypeError: Cannot read properties of undefined (reading 'length')
```

### 🔴 السبب الثاني — الرفض الأمني قيمته `truthy`

حارس صلاحيات الـ IPC يرد على قناة ممنوعة بـ:

```js
return { success: false, message: err.message, code: err.code };
```

وهذا **كائن**، أي `truthy`. لذلك الشاشات المكتوبة على النمط `{data && <DataTable data={data.rows} />}` **تتجاوز حارسها الخاص** وتُمرّر `undefined` إلى الجدول. ينطبق ذلك عند انتهاء الجلسة أو عند مستخدم لا يملك الصلاحية.

---

## 3. الإصلاحات المنفّذة

| # | الموضع | الخلل | الإصلاح |
|---|---|---|---|
| 1 | `DataTable.tsx` | `data.length` على 38 شاشة | تمرير عبر `asRows()` + نوع يعترف بـ `null` |
| 2 | `ReportsPage.tsx` | بيانات تقرير سابق | تخزين `{ type, data }` معاً ورفض غير المطابق |
| 3 | `ReportsPage.tsx` | سباق ردّ بطيء | التقاط `requested` قبل `await` |
| 4 | `Header.tsx` | جرس الإشعارات | فحص `isFailure` قبل الفتح |
| 5 | `PayrollPage.tsx` | تفاصيل الراتب | فحص الرفض + `asRows` |
| 6 | `SettlementPage.tsx` | تفاصيل التسوية + قوائم `.map()` مباشرة | فحص الرفض + `asRows` |
| 7 | `OpeningBalancePage.tsx` | `data.totals` بعد حارس `!data` | فحص الرفض |
| 8 | `MaintenancePage.tsx` | منضدة الفني + المعاينة | فحص الرفض + `asRows` |
| 9 | `AssetsPage.tsx` | كشف حساب الخزينة | فحص الرفض + `asRows` |
| 10 | كشوف العميل/المورد/الموظف | `operations` / `items` | `asRows` |

المبدأ المحاسبي المُتَّبع: **الشاشة التي تعذّرت قراءتها تعرض "لا توجد بيانات" ولا تخترع صفوفاً أبداً** — `asRows` تختبر `Array.isArray` فقط، فلا يمكن لكائن أن يتحول إلى صفوف وهمية.

---

## 4. إثبات أن الإصلاح حقيقي (اختبار الطفرات)

تمت إضافة `scripts/verify_renderer_crash.mjs` — **40 فحصاً**.

اختبار الطفرات كشف **ضعفاً في اختباري أنا**: الفحص كان يطابق **التعليق الشارح** لا الكود نفسه، فبقي أخضر رغم حذف الحماية:

```
SURVIVED a report is rendered against another report's payload
         NO SUITE DETECTED THIS
```

تم تصحيح ذلك بتجريد التعليقات قبل الفحص. النتيجة النهائية:

```
RESULT: 18 caught, 0 survived
```

الطفرات الثلاث الجديدة تُعيد الأخطاء الحقيقية وكلها تُكتشف الآن.

---

## 5. النتيجة النهائية

```
32 مجموعة فحص — كلها خضراء
verify_renderer_crash.mjs:  40 passed, 0 failed
verify_trade_mutation.mjs:  18 caught, 0 survived
fuzz sweep: 39/40 seeds clean (15,600 عملية)
```

---

## 6. ما زال مفتوحاً

| البند | الحالة |
|---|---|
| 🟠 لا يوجد `Error Boundary` | مُوصى به: يحوّل أي خطأ مستقبلي إلى بطاقة خطأ بدل شاشة بيضاء |
| 🟠 خلل المخزون التسلسلي (البذور 29/66/70/113) | مُشخَّص بالكامل، 6 محاولات أُلغيت، يحتاج إعادة هيكلة مخصصة |
