# 🔴 سبب رفض لوحة المطور لكلمة مرورك

**التاريخ:** ١ أغسطس ٢٠٢٦

---

## السبب — ليس خطأً منك

تجزئة bcrypt تبدأ دائماً بـ `$2a$12$...`، وملف `.env` **يُقرأ عبر أداة تُفسّر
علامة `$` كمتغيّر**. فتقرأ `$12$hUAAv...` على أنها إشارة لمتغيّر اسمه `12`،
وتستبدلها بلا شيء.

### الإثبات — نفّذته على Vite نفسه الذي يستخدمه مشروعك

```
القيمة في .env  :  $2a$12$hUAAvSomeRealLookingHash...Q9AZsC
ما يصل للتطبيق  :  $2a$12          ← مقطوعة!
```

فتقارن bcrypt كلمتك بتجزئة **مشوّهة**، فترفض **أي** كلمة مرور — بلا أي رسالة خطأ.

> ### 💡 ولهذا نجح كود التفعيل وفشلت كلمة المرور
> مفتاح الترخيص بصيغة base64 **ولا يحوي `$` إطلاقاً**، فوصل سليماً.
> التجزئة وحدها هي التي تضررت.

---

## ✅ الحل: base64 — لا تحوي `$` فلا يمكن تشويهها

جرّبت ثلاثة حلول واخترت الأضمن:

| الطريقة | النتيجة |
|---|---|
| وضعها بين علامتَي تنصيص `'...'` | 🔴 ما زالت مقطوعة |
| تهريب كل `$` بـ `\$` | 🟠 تعمل، لكن نسيان واحدة = عطل صامت |
| **base64** | ✅ **لا تحوي `$` أصلاً** |

وأضفت **حارساً**: أي تجزئة تصل مقطوعة **تُرفض ويُطبع سبب واضح** بدل استخدامها.

---

## 🔻 الخطوات — ٣ دقائق

**١) اسحب الإصلاح:**
```powershell
cd "D:\programing\coding projects\mobile shop"
git pull origin master
```

**٢) ولّد كلمة المرور من جديد:**
```powershell
npm run dev:password -- "نفس العبارة التي اخترتها"
```

سيطبع الآن سطراً جاهزاً:
```
MOBILESHOP_DEV_PASSWORD_HASH_B64=JDJiJDEyJEJJR1BFVTBNSkRJRE1p...
```

**٣) عدّل `.env`:**
- **احذف** السطر القديم `MOBILESHOP_DEV_PASSWORD_HASH=...`
- **أضف** السطر الجديد `MOBILESHOP_DEV_PASSWORD_HASH_B64=...`

**٤) تأكد:**
```powershell
npm run check:env
```
يجب أن ترى `ok  MOBILESHOP_DEV_PASSWORD_HASH_B64`.

**٥) شغّل وجرّب:**
```powershell
npm start
```
ادخل بـ `zerocold` + عبارتك. **يجب أن تُقبل الآن.**

---

## ما تحققت منه عملياً

| الاختبار | النتيجة |
|---|---|
| التجزئة تنجو من Vite بصيغة base64 | ✅ ٦٠ حرفاً كاملة |
| bcrypt يقبل الكلمة الصحيحة | ✅ |
| bcrypt يرفض كلمة خاطئة | ✅ |
| تجزئة مقطوعة | ✅ تُرفض مع رسالة واضحة |
| تجزئة خام مهرّبة يدوياً | ✅ ما زالت تعمل |

---

## 🔍 ملاحظة على منهج الاختبار

كتبت أولاً فحصاً يبحث في نص الملف عن نمط التحقق. ثم حقنت عطلاً يحذف الحارس —
**ونجا العطل**.

السبب: الملف يحوي **نفس النمط مرتين** (للصيغتين)، فحذف أحدهما ترك الآخر
والفحص بقي أخضر — بينما التجزئة المقطوعة صارت تُقبل فعلياً.

استبدلته بفحص **سلوكي** يُحمّل الوحدة الحقيقية بقيمة مقطوعة ويتأكد أنها تُرفض.
الآن يُكتشف العطل.

> **الدرس:** فحص النص يرى **وجود** الحماية، لا **عملها**. وعندما تتكرر الصيغة،
> يصبح الوجود بلا معنى.
